# Development / test dependencies. Install with:
#   python -m pip install -c constraints.txt -r requirements-dev.txt

-r requirements.txt

pytest>=8.0,<10.0.0
pytest-asyncio>=0.23,<2.0.0
pytest-cov>=5.0,<8.0.0
httpx>=0.27,<1.0.0  # re-stated so pytest fixtures can import it directly

# Lint + type-check gates (CI enforces these as required checks).
# Pin tightly: lint/type rules differ between minor versions, so an
# unpinned bump can suddenly fail an unrelated PR.
ruff>=0.15.0,<0.16.0
mypy>=1.20.0,<1.21.0
