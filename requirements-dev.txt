# Development / test dependencies. Install with:
#   python -m pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
httpx>=0.27  # re-stated so pytest fixtures can import it directly
