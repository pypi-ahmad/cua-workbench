=== DIRECTORY STRUCTURE ===

docker/
    Dockerfile
    agent_service.py
    entrypoint.sh


=== FILE CONTENTS AND EXPLANATIONS ===

=== FILE: docker/Dockerfile ===
--- SOURCE CODE ---
FROM ubuntu:24.04

ENV DEBIAN_FRONTEND=noninteractive
ENV DISPLAY=:99
ENV SCREEN_WIDTH=1440
ENV SCREEN_HEIGHT=900
ENV SCREEN_DEPTH=24
ENV AGENT_SERVICE_PORT=9222
ENV LIBGL_ALWAYS_SOFTWARE=1
ENV PLAYWRIGHT_BROWSERS_PATH=/ms-playwright
ENV PYTHONPATH=/app

# GUI/runtime dependencies + build prerequisites (single layer)
RUN apt-get update && apt-get install -y \
    xfce4 \
    xfce4-terminal \
    xfce4-goodies \
    dbus-x11 \
    x11vnc \
    xvfb \
    x11-utils \
    websockify \
    novnc \
    ffmpeg \
    scrot \
    wmctrl \
    xclip \
    xdotool \
    fonts-liberation \
    fonts-noto-core \
    fonts-dejavu-core \
    at-spi2-core \
    libatspi2.0-dev \
    gir1.2-atspi-2.0 \
    gir1.2-gtk-3.0 \
    python3-gi \
    python3-gi-cairo \
    python3 \
    python3-pip \
    python3-venv \
    wget \
    curl \
    gnupg \
    ca-certificates \
    socat \
    && rm -rf /var/lib/apt/lists/*

# Install Node.js LTS 20 (required for Playwright MCP via npx)
RUN curl -fsSL https://deb.nodesource.com/setup_20.x | bash - \
    && apt-get update \
    && apt-get install -y nodejs \
    && rm -rf /var/lib/apt/lists/*
RUN node -v && npm -v && npx -v

# Pre-install Playwright MCP server globally (avoids npx download at runtime)
RUN npm install -g @playwright/mcp@latest

# Create virtual environment WITH system site-packages access.
# python3-gi (GObject Introspection) and gir1.2-atspi-2.0 are apt-only
# packages — the venv must see them for the AT-SPI accessibility engine.


# RUN python3 -m venv --system-site-packages /opt/venv
# ENV PATH="/opt/venv/bin:$PATH"

# COPY requirements.txt /tmp/requirements.txt
# RUN pip install --no-cache-dir playwright -r /tmp/requirements.txt

# Install Playwright browsers + OS deps
RUN playwright


# --- Google Chrome (item 1) ---
RUN wget -qO- https://dl.google.com/linux/linux_signing_key.pub | gpg --dearmor -o /usr/share/keyrings/google.gpg \
 && echo "deb [arch=amd64 signed-by=/usr/share/keyrings/google.gpg] http://dl.google.com/linux/chrome/deb/ stable main" \
    > /etc/apt/sources.list.d/google-chrome.list \
 && apt-get update && apt-get install -y --no-install-recommends google-chrome-stable \
 && rm -rf /var/lib/apt/lists/*

# Ensure Chrome binary exists at the path Playwright 'chrome' channel expects.
# Prefer the real google-chrome if installed, otherwise fall back to the
# Playwright-bundled Chromium so that @playwright/mcp can always find a
# working browser at /opt/google/chrome/chrome.
RUN mkdir -p /opt/google/chrome \
 && if [ -x /usr/bin/google-chrome-stable ]; then \
      ln -sf /usr/bin/google-chrome-stable /opt/google/chrome/chrome; \
    elif [ -x /usr/bin/google-chrome ]; then \
      ln -sf /usr/bin/google-chrome /opt/google/chrome/chrome; \
    else \
      CHROMIUM=$(find /ms-playwright -name chrome -type f -executable 2>/dev/null | head -1); \
      [ -n "$CHROMIUM" ] && ln -sf "$CHROMIUM" /opt/google/chrome/chrome \
        || echo 'WARNING: no Chrome/Chromium binary found for symlink'; \
    fi

# --- Brave Browser (item 3) ---
RUN curl -fsSL https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg \
    -o /usr/share/keyrings/brave-browser-archive-keyring.gpg

RUN echo "deb [signed-by=/usr/share/keyrings/brave-browser-archive-keyring.gpg] https://brave-browser-apt-release.s3.brave.com/ stable main" \
    > /etc/apt/sources.list.d/brave-browser-release.list

RUN apt-get update && apt-get install -y --no-install-recommends \
    brave-browser \
    && rm -rf /var/lib/apt/lists/*

# --- Misc utilities (item 4) ---
RUN apt-get update && apt-get install -y --no-install-recommends \
    flameshot \
    xournalpp \
    htop \
    lsof \
    strace \
    && rm -rf /var/lib/apt/lists/*

# --- Firefox via official Mozilla APT repo (item 5, no Snap) ---
RUN install -d -m 0755 /etc/apt/keyrings \
 && curl -fsSL https://packages.mozilla.org/apt/repo-signing-key.gpg \
    | gpg --dearmor -o /etc/apt/keyrings/mozilla.gpg

RUN echo "deb [signed-by=/etc/apt/keyrings/mozilla.gpg] https://packages.mozilla.org/apt mozilla main" \
    > /etc/apt/sources.list.d/mozilla.list \
 && printf "Package: *\nPin: origin packages.mozilla.org\nPin-Priority: 1000\n" \
    > /etc/apt/preferences.d/mozilla

RUN apt-get update && apt-get install -y --no-install-recommends firefox \
    && rm -rf /var/lib/apt/lists/*

# --- Microsoft Edge (item 6) ---
RUN wget -qO- https://packages.microsoft.com/keys/microsoft.asc \
  | gpg --dearmor -o /usr/share/keyrings/microsoft.gpg

RUN echo "deb [arch=amd64 signed-by=/usr/share/keyrings/microsoft.gpg] https://packages.microsoft.com/repos/edge stable main" \
  > /etc/apt/sources.list.d/microsoft-edge.list

RUN apt-get update && apt-get install -y --no-install-recommends \
    microsoft-edge-stable \
    && rm -rf /var/lib/apt/lists/*

# --- Desktop apps and tools (item 8) ---
RUN apt-get update && apt-get install -y --no-install-recommends \
    vlc \
    libreoffice \
    libreoffice-gtk3 \
    evince \
    gedit \
    gnome-terminal \
    file \
    unzip \
    p7zip-full \
    jq \
    curl \
    wget \
    git \
    net-tools \
    iputils-ping \
    dnsutils \
    xdg-utils \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

# ── Pre-create Chrome profile to suppress first-run dialogs & keyring ────────
# Static profile creation only — NO browser execution during build.
RUN mkdir -p /tmp/chrome-profile/Default \
    && echo '{"browser":{"check_default_browser":false},"distribution":{"skip_first_run_ui":true}}' \
    > /tmp/chrome-profile/Default/Preferences \
    && touch /tmp/chrome-profile/First\ Run

# Setup working directory
WORKDIR /app

# Copy backend for shared tools (action aliases)
COPY backend /app/backend

# Copy agent service (Playwright-based control)
COPY docker/agent_service.py /app/docker/agent_service.py

# Copy entrypoint
COPY docker/entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh

# 5900 = VNC, 6080 = noVNC web UI, 8931 = Playwright MCP, 9222 = Agent Service API
EXPOSE 5900 6080 8931 9222

# Health check — agent_service /health endpoint
HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
    CMD curl -f http://localhost:9222/health || exit 1

ENTRYPOINT ["/entrypoint.sh"]
--- EXPLANATION ---
This is the source code for docker/Dockerfile.
Line 1: FROM ubuntu:24.04
Line 3: ENV DEBIAN_FRONTEND=noninteractive
Line 4: ENV DISPLAY=:99
Line 5: ENV SCREEN_WIDTH=1440
Line 6: ENV SCREEN_HEIGHT=900
Line 7: ENV SCREEN_DEPTH=24
Line 8: ENV AGENT_SERVICE_PORT=9222
Line 9: ENV LIBGL_ALWAYS_SOFTWARE=1
Line 10: ENV PLAYWRIGHT_BROWSERS_PATH=/ms-playwright
Line 11: ENV PYTHONPATH=/app
Line 13: # GUI/runtime dependencies + build prerequisites (single layer)
Line 14: RUN apt-get update && apt-get install -y \
Line 15: xfce4 \
Line 16: xfce4-terminal \
Line 17: xfce4-goodies \
Line 18: dbus-x11 \
Line 19: x11vnc \
Line 20: xvfb \
Line 21: x11-utils \
Line 22: websockify \
Line 23: novnc \
Line 24: ffmpeg \
Line 25: scrot \
Line 26: wmctrl \
Line 27: xclip \
Line 28: xdotool \
Line 29: fonts-liberation \
Line 30: fonts-noto-core \
Line 31: fonts-dejavu-core \
Line 32: at-spi2-core \
Line 33: libatspi2.0-dev \
Line 34: gir1.2-atspi-2.0 \
Line 35: gir1.2-gtk-3.0 \
Line 36: python3-gi \
Line 37: python3-gi-cairo \
Line 38: python3 \
Line 39: python3-pip \
Line 40: python3-venv \
Line 41: wget \
Line 42: curl \
Line 43: gnupg \
Line 44: ca-certificates \
Line 45: socat \
Line 46: && rm -rf /var/lib/apt/lists/*
Line 48: # Install Node.js LTS 20 (required for Playwright MCP via npx)
Line 49: RUN curl -fsSL https://deb.nodesource.com/setup_20.x | bash - \
Line 50: && apt-get update \
Line 51: && apt-get install -y nodejs \
Line 52: && rm -rf /var/lib/apt/lists/*
Line 53: RUN node -v && npm -v && npx -v
Line 55: # Pre-install Playwright MCP server globally (avoids npx download at runtime)
Line 56: RUN npm install -g @playwright/mcp@latest
Line 58: # Create virtual environment WITH system site-packages access.
Line 59: # python3-gi (GObject Introspection) and gir1.2-atspi-2.0 are apt-only
Line 60: # packages — the venv must see them for the AT-SPI accessibility engine.
Line 63: # RUN python3 -m venv --system-site-packages /opt/venv
Line 64: # ENV PATH="/opt/venv/bin:$PATH"
Line 66: # COPY requirements.txt /tmp/requirements.txt
Line 67: # RUN pip install --no-cache-dir playwright -r /tmp/requirements.txt
Line 69: # Install Playwright browsers + OS deps
Line 70: RUN playwright
Line 73: # --- Google Chrome (item 1) ---
Line 74: RUN wget -qO- https://dl.google.com/linux/linux_signing_key.pub | gpg --dearmor -o /usr/share/keyrings/google.gpg \
Line 75: && echo "deb [arch=amd64 signed-by=/usr/share/keyrings/google.gpg] http://dl.google.com/linux/chrome/deb/ stable main" \
Line 76: > /etc/apt/sources.list.d/google-chrome.list \
Line 77: && apt-get update && apt-get install -y --no-install-recommends google-chrome-stable \
Line 78: && rm -rf /var/lib/apt/lists/*
Line 80: # Ensure Chrome binary exists at the path Playwright 'chrome' channel expects.
Line 81: # Prefer the real google-chrome if installed, otherwise fall back to the
Line 82: # Playwright-bundled Chromium so that @playwright/mcp can always find a
Line 83: # working browser at /opt/google/chrome/chrome.
Line 84: RUN mkdir -p /opt/google/chrome \
Line 85: && if [ -x /usr/bin/google-chrome-stable ]; then \
Line 86: ln -sf /usr/bin/google-chrome-stable /opt/google/chrome/chrome; \
Line 87: elif [ -x /usr/bin/google-chrome ]; then \
Line 88: ln -sf /usr/bin/google-chrome /opt/google/chrome/chrome; \
Line 89: else \
Line 90: CHROMIUM=$(find /ms-playwright -name chrome -type f -executable 2>/dev/null | head -1); \
Line 91: [ -n "$CHROMIUM" ] && ln -sf "$CHROMIUM" /opt/google/chrome/chrome \
Line 92: || echo 'WARNING: no Chrome/Chromium binary found for symlink'; \
Line 93: fi
Line 95: # --- Brave Browser (item 3) ---
Line 96: RUN curl -fsSL https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg \
Line 97: -o /usr/share/keyrings/brave-browser-archive-keyring.gpg
Line 99: RUN echo "deb [signed-by=/usr/share/keyrings/brave-browser-archive-keyring.gpg] https://brave-browser-apt-release.s3.brave.com/ stable main" \
Line 100: > /etc/apt/sources.list.d/brave-browser-release.list
Line 102: RUN apt-get update && apt-get install -y --no-install-recommends \
Line 103: brave-browser \
Line 104: && rm -rf /var/lib/apt/lists/*
Line 106: # --- Misc utilities (item 4) ---
Line 107: RUN apt-get update && apt-get install -y --no-install-recommends \
Line 108: flameshot \
Line 109: xournalpp \
Line 110: htop \
Line 111: lsof \
Line 112: strace \
Line 113: && rm -rf /var/lib/apt/lists/*
Line 115: # --- Firefox via official Mozilla APT repo (item 5, no Snap) ---
Line 116: RUN install -d -m 0755 /etc/apt/keyrings \
Line 117: && curl -fsSL https://packages.mozilla.org/apt/repo-signing-key.gpg \
Line 118: | gpg --dearmor -o /etc/apt/keyrings/mozilla.gpg
Line 120: RUN echo "deb [signed-by=/etc/apt/keyrings/mozilla.gpg] https://packages.mozilla.org/apt mozilla main" \
Line 121: > /etc/apt/sources.list.d/mozilla.list \
Line 122: && printf "Package: *\nPin: origin packages.mozilla.org\nPin-Priority: 1000\n" \
Line 123: > /etc/apt/preferences.d/mozilla
Line 125: RUN apt-get update && apt-get install -y --no-install-recommends firefox \
Line 126: && rm -rf /var/lib/apt/lists/*
Line 128: # --- Microsoft Edge (item 6) ---
Line 129: RUN wget -qO- https://packages.microsoft.com/keys/microsoft.asc \
Line 130: | gpg --dearmor -o /usr/share/keyrings/microsoft.gpg
Line 132: RUN echo "deb [arch=amd64 signed-by=/usr/share/keyrings/microsoft.gpg] https://packages.microsoft.com/repos/edge stable main" \
Line 133: > /etc/apt/sources.list.d/microsoft-edge.list
Line 135: RUN apt-get update && apt-get install -y --no-install-recommends \
Line 136: microsoft-edge-stable \
Line 137: && rm -rf /var/lib/apt/lists/*
Line 139: # --- Desktop apps and tools (item 8) ---
Line 140: RUN apt-get update && apt-get install -y --no-install-recommends \
Line 141: vlc \
Line 142: libreoffice \
Line 143: libreoffice-gtk3 \
Line 144: evince \
Line 145: gedit \
Line 146: gnome-terminal \
Line 147: file \
Line 148: unzip \
Line 149: p7zip-full \
Line 150: jq \
Line 151: curl \
Line 152: wget \
Line 153: git \
Line 154: net-tools \
Line 155: iputils-ping \
Line 156: dnsutils \
Line 157: xdg-utils \
Line 158: ca-certificates \
Line 159: && rm -rf /var/lib/apt/lists/*
Line 161: # ── Pre-create Chrome profile to suppress first-run dialogs & keyring ────────
Line 162: # Static profile creation only — NO browser execution during build.
Line 163: RUN mkdir -p /tmp/chrome-profile/Default \
Line 164: && echo '{"browser":{"check_default_browser":false},"distribution":{"skip_first_run_ui":true}}' \
Line 165: > /tmp/chrome-profile/Default/Preferences \
Line 166: && touch /tmp/chrome-profile/First\ Run
Line 168: # Setup working directory
Line 169: WORKDIR /app
Line 171: # Copy backend for shared tools (action aliases)
Line 172: COPY backend /app/backend
Line 174: # Copy agent service (Playwright-based control)
Line 175: COPY docker/agent_service.py /app/docker/agent_service.py
Line 177: # Copy entrypoint
Line 178: COPY docker/entrypoint.sh /entrypoint.sh
Line 179: RUN chmod +x /entrypoint.sh
Line 181: # 5900 = VNC, 6080 = noVNC web UI, 8931 = Playwright MCP, 9222 = Agent Service API
Line 182: EXPOSE 5900 6080 8931 9222
Line 184: # Health check — agent_service /health endpoint
Line 185: HEALTHCHECK --interval=30s --timeout=5s --start-period=15s --retries=3 \
Line 186: CMD curl -f http://localhost:9222/health || exit 1
Line 188: ENTRYPOINT ["/entrypoint.sh"]


=== FILE: docker/agent_service.py ===
--- SOURCE CODE ---
"""Internal agent service — runs INSIDE the Docker container.

Provides a lightweight HTTP API with two automation modes:

  • browser  — Playwright (page.mouse.click, page.keyboard.type, page.goto)
  • desktop  — xdotool + scrot (works with ANY X11 application)

The backend selects the mode per-request via a `mode` field.  Screenshots
and actions are dispatched to the appropriate handler automatically.
"""

from __future__ import annotations

import base64
import json
import logging
import os
import shlex
import shutil
import signal
import subprocess
import sys
import time
from http.server import HTTPServer, BaseHTTPRequestHandler
from threading import Lock

logging.basicConfig(
    level=logging.INFO,
    format='{"timestamp": "%(asctime)s", "level": "%(levelname)s", "message": "%(message)s"}',
)
logger = logging.getLogger("agent_service")

try:
    from backend.tools.action_aliases import resolve_action
except ImportError:
    # Fallback if backend not available (e.g. local dev outside docker)
    logger.warning("backend.tools not found, alias resolution disabled")

    def resolve_action(a):
        """Identity fallback when backend.tools is unavailable."""
        return a

# ── Globals ───────────────────────────────────────────────────────────────────

_playwright = None
_browser = None
_context = None
_page = None
_lock = Lock()

SCREEN_WIDTH = int(os.environ.get("SCREEN_WIDTH", "1440"))
SCREEN_HEIGHT = int(os.environ.get("SCREEN_HEIGHT", "900"))
SERVICE_PORT = int(os.environ.get("AGENT_SERVICE_PORT", "9222"))
DEFAULT_MODE = os.environ.get("AGENT_MODE", "browser")  # "browser" or "desktop"
ACTION_DELAY = float(os.environ.get("ACTION_DELAY", "0.05"))


def _env_bool(name: str, default: bool = True) -> bool:
    """Parse a boolean-like environment variable."""
    raw = os.environ.get(name)
    if raw is None:
        return default
    return raw.strip().lower() in {"1", "true", "yes", "on"}


WINDOW_NORMALIZE_ENABLED = _env_bool("CUA_WINDOW_NORMALIZE", True)
WINDOW_NORMALIZE_X = int(os.environ.get("CUA_WINDOW_X", "100"))
WINDOW_NORMALIZE_Y = int(os.environ.get("CUA_WINDOW_Y", "80"))
WINDOW_NORMALIZE_W = int(os.environ.get("CUA_WINDOW_W", "560"))
WINDOW_NORMALIZE_H = int(os.environ.get("CUA_WINDOW_H", "760"))

# ── Security constants ────────────────────────────────────────────────────────

_MAX_BODY_SIZE = 1_000_000  # 1 MB request body limit

# Dangerous shell patterns blocked in run_command (defense-in-depth)
_BLOCKED_CMD_PATTERNS = (
    "rm -rf /",
    "rm -rf /*",
    "mkfs.",
    "dd if=/dev/",
    ":(){",
    "shutdown",
    "reboot",
    "halt ",
    "poweroff",
    "chmod -R 777 /",
    "> /dev/sda",
    "mv /* ",
    "mv / ",
)

# Allowed directories for file upload operations
_UPLOAD_ALLOWED_PREFIXES = ("/tmp", "/app", "/home")

# Strict allowlist of commands permitted in run_command
_ALLOWED_COMMANDS = frozenset({
    "ls", "cat", "head", "tail", "grep", "find", "wc", "echo",
    "pwd", "whoami", "id", "date", "env", "printenv",
    "which", "file", "stat", "df", "du", "free",
    "uname", "hostname", "uptime",
    "python3", "python", "pip", "pip3", "node", "npm", "npx",
    "curl", "wget",
    "xdg-open", "xdotool", "xclip", "scrot", "wmctrl",
    "xfce4-terminal", "xterm",
    # Desktop apps accessible via accessibility / run_command
    "gnome-control-center", "gnome-settings", "gnome-calculator",
    "gnome-text-editor", "gedit", "gnome-system-monitor",
    "xfce4-settings-manager", "xfce4-settings-editor",
    "xfce4-taskmanager", "thunar", "mousepad",
    "firefox", "google-chrome",
    # Browsers added via Dockerfile
    "brave-browser", "microsoft-edge", "microsoft-edge-stable",
    # Desktop apps added via Dockerfile
    "vlc", "libreoffice", "soffice",
    "evince", "gnome-terminal", "flameshot", "xournalpp",
    "htop",
})

# Ensure DISPLAY is set for all subprocesses (Critical Desktop Fix)
os.environ["DISPLAY"] = ":99"



# ── Playwright lifecycle ──────────────────────────────────────────────────────

def _init_browser():
    """Launch Playwright + Chromium synchronously."""
    global _playwright, _browser, _context, _page

    try:
        subprocess.run(["xclip", "-version"], stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
    except FileNotFoundError:
        logger.warning("xclip not found - clipboard actions (paste/copy) in desktop mode may fail")
    
    from playwright.sync_api import sync_playwright

    logger.info("Initializing Playwright...")
    _playwright = sync_playwright().start()

    _browser = _playwright.chromium.launch(
        headless=False,  # Visible on Xvfb
        args=[
            "--no-sandbox",
            "--disable-dev-shm-usage",
            "--disable-gpu",
            f"--window-size={SCREEN_WIDTH - 100},{SCREEN_HEIGHT - 80}",
            "--window-position=50,10",
            "--disable-extensions",
            "--disable-background-timer-throttling",
            "--disable-backgrounding-occluded-windows",
            "--disable-renderer-backgrounding",
            "--force-device-scale-factor=1",
            "--remote-debugging-port=9223",
        ],
    )

    _context = _browser.new_context(
        viewport={"width": SCREEN_WIDTH - 100, "height": SCREEN_HEIGHT - 80},
        device_scale_factor=1,
    )

    _page = _context.new_page()
    _page.goto("about:blank")

    logger.info("Playwright browser ready: %dx%d", SCREEN_WIDTH, SCREEN_HEIGHT)


def _get_page():
    """Return the active page, creating if needed."""
    global _page
    if _page is None or _page.is_closed():
        if _context:
            _page = _context.new_page()
            _page.goto("about:blank")
    return _page


def _shutdown_browser():
    """Tear down Playwright browser, context, and page."""
    global _playwright, _browser, _context, _page
    try:
        if _page and not _page.is_closed():
            _page.close()
        if _context:
            _context.close()
        if _browser:
            _browser.close()
        if _playwright:
            _playwright.stop()
    except Exception as e:
        logger.warning("Shutdown error: %s", e)


# ══════════════════════════════════════════════════════════════════════════════
#  Screenshots
# ══════════════════════════════════════════════════════════════════════════════

def _screenshot_playwright() -> str:
    """Capture current browser tab as PNG via Playwright."""
    page = _get_page()
    png_bytes = page.screenshot(type="png", full_page=False)
    return base64.b64encode(png_bytes).decode("ascii")


def _screenshot_desktop() -> str:
    """Capture the full Xvfb display via scrot (works with any app)."""
    subprocess.run(
        ["scrot", "-z", "-o", "/tmp/screenshot.png"],
        check=True, timeout=5,
    )
    with open("/tmp/screenshot.png", "rb") as f:
        return base64.b64encode(f.read()).decode("ascii")


# ══════════════════════════════════════════════════════════════════════════════
#  Actions — Playwright (browser mode)
# ══════════════════════════════════════════════════════════════════════════════

def _pw_click(x: int, y: int) -> dict:
    """Click at pixel coordinates via Playwright."""
    page = _get_page()
    page.mouse.click(x, y)
    return {"success": True, "message": f"Clicked at ({x}, {y})"}


def _pw_double_click(x: int, y: int) -> dict:
    """Double-click at pixel coordinates via Playwright."""
    page = _get_page()
    page.mouse.dblclick(x, y)
    return {"success": True, "message": f"Double-clicked at ({x}, {y})"}


def _pw_right_click(x: int, y: int) -> dict:
    """Right-click at pixel coordinates via Playwright."""
    page = _get_page()
    page.mouse.click(x, y, button="right")
    return {"success": True, "message": f"Right-clicked at ({x}, {y})"}


def _pw_type(text: str, coords: list = None, selector: str = None) -> dict:
    """Type text, ensuring focus if coordinates or selector are provided."""
    page = _get_page()
    
    # 1. Try to click coordinates or selector if provided to ensure focus
    if selector:
        try:
            page.click(selector)
            time.sleep(0.1)
        except Exception as e:
            logger.warning(f"Safe type click selector failed: {e}")
    elif coords and len(coords) >= 2:
        try:
            page.mouse.click(coords[0], coords[1])
            time.sleep(0.1)
        except Exception as e:
            logger.warning(f"Safe type click coords failed: {e}")

    # 2. Verify focus (optional but good for debugging)
    try:
        active_tag = page.evaluate("document.activeElement ? document.activeElement.tagName : ''")
        if active_tag == 'BODY':
            logger.warning("Typing with focus on BODY - input might fail")
    except Exception:
        pass

    # 3. Type directly (retry/fallback handled by executor)
    page.keyboard.type(text, delay=50)
    time.sleep(ACTION_DELAY)
    return {"success": True, "message": f"Typed: {text[:50]}", "error_type": None}


def _pw_scroll(x: int, y: int, direction: str) -> dict:
    """Scroll at the given coordinates in the specified direction."""
    page = _get_page()
    page.mouse.move(x, y)
    delta = -300 if direction == "up" else 300
    page.mouse.wheel(0, delta)
    return {"success": True, "message": f"Scrolled {direction} at ({x}, {y})"}


def _pw_navigate(url: str) -> dict:
    """Navigate the browser to *url*, prepending https:// if needed."""
    page = _get_page()
    if not url.startswith(("http://", "https://")):
        url = "https://" + url
    page.goto(url, wait_until="domcontentloaded", timeout=30000)
    return {"success": True, "message": f"Navigated to {url}"}


def _pw_key(key: str) -> dict:
    """Press a keyboard key or combo via Playwright."""
    page = _get_page()
    combo = _map_key_combo(key)
    page.keyboard.press(combo)
    return {"success": True, "message": f"Pressed key: {key}"}


def _pw_drag(x1: int, y1: int, x2: int, y2: int) -> dict:
    """Drag from (x1,y1) to (x2,y2) via Playwright mouse."""
    page = _get_page()
    page.mouse.move(x1, y1)
    page.mouse.down()
    page.mouse.move(x2, y2, steps=10)
    page.mouse.up()
    return {"success": True, "message": f"Dragged ({x1},{y1}) → ({x2},{y2})"}


def _pw_hover(x: int, y: int) -> dict:
    """Move the mouse to (x,y) without clicking."""
    page = _get_page()
    page.mouse.move(x, y)
    return {"success": True, "message": f"Hovered at ({x}, {y})"}


def _pw_middle_click(x: int, y: int) -> dict:
    """Middle-click at pixel coordinates via Playwright."""
    page = _get_page()
    page.mouse.click(x, y, button="middle")
    return {"success": True, "message": f"Middle-clicked at ({x}, {y})"}


def _pw_fill(selector: str, text: str) -> dict:
    """Fill an input using Playwright's reliable fill() — clears first.

    On selector failure, auto-discovers available form fields and returns
    them in the error message so the model can retry with the correct selector.
    """
    page = _get_page()
    try:
        page.fill(selector, text, timeout=5000)
        return {"success": True, "message": f"Filled '{selector}' with: {text[:50]}"}
    except Exception as fill_err:
        # Auto-discover available form fields so the model can self-correct
        try:
            fields_json = page.evaluate(
                "[...document.querySelectorAll('input,textarea,select')]"
                ".map(e=>({tag:e.tagName,name:e.name,id:e.id,type:e.type,"
                "placeholder:e.placeholder||''}))"
            )
            field_hints = json.dumps(fields_json, separators=(",", ":"))
            if len(field_hints) > 1500:
                field_hints = field_hints[:1500] + "…]"
        except Exception:
            field_hints = "(could not discover fields)"
        return {
            "success": False,
            "message": (
                f"fill('{selector}') failed: {fill_err}. "
                f"Available fields: {field_hints}"
            ),
        }


def _pw_hotkey(keys: list[str]) -> dict:
    """Press multiple keys simultaneously (e.g. ['ctrl','shift','t'])."""
    page = _get_page()
    combo = "+".join(_map_key_combo(k) for k in keys)
    page.keyboard.press(combo)
    return {"success": True, "message": f"Hotkey: {'+'.join(keys)}"}


def _pw_clear_input(selector: str) -> dict:
    """Clear an input field by filling it with an empty string."""
    page = _get_page()
    page.locator(selector).fill("")
    return {"success": True, "message": f"Cleared input: {selector}"}


def _pw_select_option(selector: str, value: str) -> dict:
    """Select a <select> option by value via Playwright."""
    page = _get_page()
    page.select_option(selector, value, timeout=5000)
    return {"success": True, "message": f"Selected '{value}' in {selector}"}


def _pw_paste(text: str) -> dict:
    """Clipboard-based paste (reliable fallback for typing)."""
    page = _get_page()
    page.evaluate(f"navigator.clipboard.writeText({json.dumps(text)})")
    page.keyboard.press("Control+v")
    time.sleep(0.1)
    return {"success": True, "message": f"Pasted: {text[:50]}"}


def _pw_copy() -> dict:
    """Copy the current selection to clipboard via Ctrl+C."""
    page = _get_page()
    page.keyboard.press("Control+c")
    return {"success": True, "message": "Copied selection to clipboard"}


def _pw_reload() -> dict:
    """Reload the current page."""
    page = _get_page()
    page.reload(wait_until="domcontentloaded", timeout=30000)
    return {"success": True, "message": "Page reloaded"}


def _pw_go_back() -> dict:
    """Navigate back one page."""
    page = _get_page()
    page.go_back(wait_until="domcontentloaded", timeout=30000)
    return {"success": True, "message": "Navigated back"}


def _pw_go_forward() -> dict:
    """Navigate forward one page."""
    page = _get_page()
    page.go_forward(wait_until="domcontentloaded", timeout=30000)
    return {"success": True, "message": "Navigated forward"}


def _pw_new_tab(url: str = "") -> dict:
    """Open a new browser tab, optionally navigating to *url*."""
    global _page
    target = url or "about:blank"
    if url and not url.startswith(("http://", "https://")):
        target = "https://" + url
    page = _context.new_page()
    page.goto(target)
    _page = page
    return {"success": True, "message": f"Opened new tab: {target}"}


def _pw_close_tab() -> dict:
    """Close the currently active browser tab."""
    global _page
    page = _get_page()
    page.close()
    pages = _context.pages
    _page = pages[-1] if pages else None
    return {"success": True, "message": "Closed current tab"}


def _pw_switch_tab(identifier: str) -> dict:
    """Switch tab by index (0-based) or partial title/URL match."""
    global _page
    pages = _context.pages
    # Try numeric index first
    try:
        idx = int(identifier)
        if 0 <= idx < len(pages):
            _page = pages[idx]
            _page.bring_to_front()
            return {"success": True, "message": f"Switched to tab {idx}: {_page.url}"}
    except ValueError:
        pass
    # Search by title or URL
    needle = identifier.lower()
    for p in pages:
        if needle in p.title().lower() or needle in p.url.lower():
            _page = p
            _page.bring_to_front()
            return {"success": True, "message": f"Switched to tab: {_page.url}"}
    return {"success": False, "message": f"No tab matching '{identifier}'"}


def _pw_scroll_to(selector: str) -> dict:
    """Scroll the element matching *selector* into view."""
    page = _get_page()
    page.evaluate(f"document.querySelector({json.dumps(selector)})?.scrollIntoView({{behavior:'smooth',block:'center'}})")
    return {"success": True, "message": f"Scrolled to: {selector}"}


def _pw_get_text(selector: str) -> dict:
    """Extract text content from the element matching *selector*."""
    page = _get_page()
    text = page.text_content(selector, timeout=5000) or ""
    return {"success": True, "message": f"Text: {text[:500]}"}


def _pw_find_element(description: str) -> dict:
    """Find element by text content and return its bounding box."""
    page = _get_page()
    loc = page.get_by_text(description, exact=False).first
    box = loc.bounding_box(timeout=5000)
    if box:
        cx = int(box["x"] + box["width"] / 2)
        cy = int(box["y"] + box["height"] / 2)
        return {"success": True, "message": f"Found '{description}' at center ({cx}, {cy}), box={box}"}
    return {"success": False, "message": f"Element '{description}' not found"}


def _pw_evaluate_js(script: str) -> dict:
    """Evaluate arbitrary JavaScript in the page context."""
    page = _get_page()
    result = page.evaluate(script)
    return {"success": True, "message": f"JS result: {str(result)[:500]}"}


def _pw_wait_for(selector: str) -> dict:
    """Wait up to 10 s for a DOM element matching *selector* to appear."""
    page = _get_page()
    page.wait_for_selector(selector, timeout=10000)
    return {"success": True, "message": f"Element appeared: {selector}"}


def _pw_screenshot_region(x: int, y: int, w: int, h: int) -> str:
    """Capture a region of the page as base64 PNG."""
    page = _get_page()
    png_bytes = page.screenshot(
        type="png",
        clip={"x": x, "y": y, "width": w, "height": h},
    )
    return base64.b64encode(png_bytes).decode("ascii")


def _pw_screenshot_viewport() -> str:
    """Capture the current viewport as base64 PNG."""
    page = _get_page()
    png_bytes = page.screenshot(type="png", full_page=False)
    return base64.b64encode(png_bytes).decode("ascii")


def _pw_screenshot_element(selector: str) -> str:
    """Capture a specific element as base64 PNG."""
    page = _get_page()
    try:
        element = page.locator(selector).first
        if element.is_visible():
            png_bytes = element.screenshot(type="png")
            return base64.b64encode(png_bytes).decode("ascii")
    except Exception as e:
        logger.warning(f"Failed to screenshot element {selector}: {e}")
    return ""  # Return empty string on failure, handled by caller


def _pw_scroll_into_view(selector: str) -> dict:
    """Scroll the element into the visible viewport if needed."""
    page = _get_page()
    try:
        page.locator(selector).first.scroll_into_view_if_needed(timeout=5000)
        return {"success": True, "message": f"Scrolled into view: {selector}"}
    except Exception as e:
        return {"success": False, "message": f"Failed to scroll to {selector}: {e}"}


def _pw_set_viewport(width: int, height: int) -> dict:
    """Resize the Playwright viewport to *width* x *height*."""
    page = _get_page()
    page.set_viewport_size({"width": width, "height": height})
    return {"success": True, "message": f"Viewport set to {width}x{height}"}


def _pw_zoom(level: float) -> dict:
    """Set zoom level (e.g. 1.0, 1.5, 0.8). Playwright uses CSS zoom or scale."""
    page = _get_page()
    page.evaluate(f"document.body.style.zoom = '{level}'")
    return {"success": True, "message": f"Zoom set to {level}"}


def _pw_block_resource(resource_type: str) -> dict:
    """Block resource types (image, font, css, etc.)."""
    page = _get_page()
    normalized = (resource_type or "").strip().lower()
    if not normalized:
        return {"success": False, "message": "block_resource requires a resource type"}

    page.route(
        "**/*",
        lambda route: route.abort()
        if route.request.resource_type == normalized
        else route.continue_(),
    )
    return {"success": True, "message": f"Blocked resource type: {resource_type}"}


def _pw_export_page_pdf() -> str:
    """Export page as PDF (base64)."""
    page = _get_page()
    pdf_bytes = page.pdf()
    return base64.b64encode(pdf_bytes).decode("ascii")


def _pw_get_html(selector: str = None) -> dict:
    """Return inner HTML of *selector*, or full page HTML if omitted."""
    page = _get_page()
    if selector:
        html = page.inner_html(selector, timeout=5000)
    else:
        html = page.content()
    return {"success": True, "message": f"HTML: {html[:500]}"}


def _pw_get_attribute(selector: str, attribute: str) -> dict:
    """Read a DOM attribute from the element matching *selector*."""
    page = _get_page()
    val = page.get_attribute(selector, attribute, timeout=5000)
    return {"success": True, "message": f"Attribute {attribute}: {val}"}


def _pw_query_selector(selector: str) -> dict:
    """Check whether a DOM element matching *selector* exists."""
    page = _get_page()
    element = page.query_selector(selector)
    if element:
        return {"success": True, "message": f"Element found: {selector}"}
    return {"success": False, "message": f"Element not found: {selector}"}


def _pw_query_all(selector: str) -> dict:
    """Count all DOM elements matching *selector*."""
    page = _get_page()
    elements = page.query_selector_all(selector)
    return {"success": True, "message": f"Found {len(elements)} elements matching: {selector}"}


def _pw_get_bounding_box(selector: str) -> dict:
    """Return the bounding box of the first element matching *selector*."""
    page = _get_page()
    element = page.query_selector(selector)
    if element:
        box = element.bounding_box()
        return {"success": True, "message": f"Bounding box for {selector}: {box}"}
    return {"success": False, "message": f"Element not found: {selector}"}


def _pw_get_visible_elements(selector: str) -> dict:
    """Count visible elements matching *selector*."""
    page = _get_page()
    elements = page.query_selector_all(selector)
    visible_count = sum(1 for el in elements if el.is_visible())
    return {"success": True, "message": f"Found {visible_count} visible elements matching: {selector}"}


def _pw_evaluate_on_selector(selector: str, script: str) -> dict:
    """Evaluate *script* in the context of the element matching *selector*."""
    page = _get_page()
    try:
        result = page.eval_on_selector(selector, script)
        return {"success": True, "message": f"JS result on {selector}: {str(result)[:500]}"}
    except Exception as e:
        return {"success": False, "message": f"Failed to evaluate on {selector}: {e}"}


def _pw_wait_for_navigation() -> dict:
    """Block until the page reaches the 'load' state."""
    page = _get_page()
    page.wait_for_load_state("load", timeout=30000)
    return {"success": True, "message": "Navigation complete"}


def _pw_upload_file(selector: str, file_path: str) -> dict:
    """Set input file on the element matching *selector* (path-restricted)."""
    page = _get_page()
    # Directory traversal protection
    resolved = os.path.realpath(file_path)
    if not any(resolved.startswith(p) for p in _UPLOAD_ALLOWED_PREFIXES):
        return {"success": False, "message": f"Upload restricted to {_UPLOAD_ALLOWED_PREFIXES}"}
    page.set_input_files(selector, file_path)
    return {"success": True, "message": f"Uploaded {file_path} to {selector}"}


def _pw_download_file(selector: str) -> dict:
    """Download a file (stub — not yet implemented)."""
    return {"success": False, "message": "Not implemented yet"}


def _pw_set_cookies(cookies: list) -> dict:
    """Add cookies to the current browser context."""
    global _context
    _context.add_cookies(cookies)
    return {"success": True, "message": "Cookies set"}


def _pw_get_cookies() -> dict:
    """Retrieve all cookies from the current browser context."""
    global _context
    cookies = _context.cookies()
    return {"success": True, "message": f"Got {len(cookies)} cookies"}


def _pw_clear_cookies() -> dict:
    """Clear all cookies from the current browser context."""
    global _context
    _context.clear_cookies()
    return {"success": True, "message": "Cookies cleared"}


def _pw_new_context() -> dict:
    """Create a fresh Playwright browser context (clears state)."""
    global _playwright, _browser, _context, _page
    if _context:
        _context.close()
    _context = _browser.new_context(
        viewport={"width": SCREEN_WIDTH - 100, "height": SCREEN_HEIGHT - 80},
        device_scale_factor=1,
    )
    _page = _context.new_page()
    _page.goto("about:blank")
    return {"success": True, "message": "New context created"}


def _pw_switch_context(identifier: str) -> dict:
    """Switch browser context (stub — not yet implemented)."""
    return {"success": False, "message": "Not implemented yet"}


def _pw_close_context() -> dict:
    """Close the current browser context and release its page."""
    global _context, _page
    if _context:
        _context.close()
    _context = None
    _page = None
    return {"success": True, "message": "Context closed"}


# ══════════════════════════════════════════════════════════════════════════════
#  Actions — xdotool (desktop mode, works with any X11 app)
# ══════════════════════════════════════════════════════════════════════════════

def _xdo(args: list[str]) -> str:
    """Run an xdotool command, return stdout.

    The ``--sync`` flag is automatically stripped because it hangs
    indefinitely in Xvfb environments that lack a compositor (the X
    event that ``--sync`` waits for is never delivered).  A small
    ``time.sleep`` replaces it so callers stay unchanged.
    """
    had_sync = "--sync" in args
    if had_sync:
        args = [a for a in args if a != "--sync"]

    result = subprocess.run(
        ["xdotool"] + args,
        capture_output=True, text=True, timeout=10,
    )
    if result.returncode != 0:
        raise RuntimeError(f"xdotool {' '.join(args)} failed: {result.stderr.strip()}")

    # Compensate for removed --sync with a short delay
    if had_sync:
        cmd = args[0] if args else ""
        if cmd == "windowactivate":
            time.sleep(0.3)   # window activation needs more time
        else:
            time.sleep(0.05)  # mousemove / other commands

    return result.stdout.strip()


def _xdo_search_window_ids(identifier: str) -> list[str]:
    """Return xdotool window IDs matching *identifier* by name."""
    if not identifier:
        return []
    result = subprocess.run(
        ["xdotool", "search", "--name", identifier],
        capture_output=True,
        text=True,
        timeout=5,
    )
    if result.returncode != 0:
        return []
    return [wid.strip() for wid in result.stdout.splitlines() if wid.strip()]


def _xdo_get_window_geometry(wid: str) -> dict:
    """Get window geometry via xdotool getwindowgeometry --shell."""
    raw = _xdo(["getwindowgeometry", "--shell", wid])
    geometry: dict[str, int] = {}
    for line in raw.splitlines():
        if "=" not in line:
            continue
        key, value = line.split("=", 1)
        if key in {"X", "Y", "WIDTH", "HEIGHT"}:
            try:
                geometry[key] = int(value.strip())
            except ValueError:
                continue
    return geometry


def _xdo_normalize_window(wid: str) -> str:
    """Move/resize window to deterministic geometry for stable coordinates."""
    if not WINDOW_NORMALIZE_ENABLED:
        return "window normalization disabled"
    try:
        subprocess.run(
            ["wmctrl", "-ir", wid, "-b", "remove,maximized_vert,maximized_horz"],
            check=False,
            capture_output=True,
            text=True,
            timeout=5,
        )
        _xdo(["windowmove", wid, str(WINDOW_NORMALIZE_X), str(WINDOW_NORMALIZE_Y)])
        _xdo(["windowsize", wid, str(WINDOW_NORMALIZE_W), str(WINDOW_NORMALIZE_H)])
        geo = _xdo_get_window_geometry(wid)
        if geo:
            return (
                f"normalized to x={geo.get('X')}, y={geo.get('Y')}, "
                f"w={geo.get('WIDTH')}, h={geo.get('HEIGHT')}"
            )
        return "normalized"
    except Exception as e:
        return f"normalization skipped: {e}"


def _expand_app_launch_candidates(app_name: str) -> list[str]:
    """Expand semantic app names into concrete launch candidates."""
    requested = (app_name or "").strip()
    if not requested:
        return []

    lowered = requested.lower()
    candidates: list[str] = [requested]

    if any(term in lowered for term in ("calculator", "calc", "xcalc", "kcalc", "galculator")):
        candidates.extend([
            "gnome-calculator",
            "galculator",
            "xfce4-calculator",
            "mate-calc",
            "kcalc",
            "xcalc",
        ])

    if any(term in lowered for term in ("file explorer", "files", "file manager", "explorer", "nautilus", "thunar", "pcmanfm")):
        candidates.extend([
            "nautilus",
            "thunar",
            "pcmanfm",
            "dolphin",
            "nemo",
            "xfe",
        ])

    # Preserve order while deduping
    seen = set()
    deduped: list[str] = []
    for item in candidates:
        key = item.strip().lower()
        if key and key not in seen:
            deduped.append(item.strip())
            seen.add(key)
    return deduped


def _xdo_click(x: int, y: int) -> dict:
    """Click at (x,y) via xdotool."""
    _xdo(["mousemove", "--sync", str(x), str(y)])
    _xdo(["click", "1"])
    return {"success": True, "message": f"Clicked at ({x}, {y})"}


def _xdo_double_click(x: int, y: int) -> dict:
    """Double-click at (x,y) via xdotool."""
    _xdo(["mousemove", "--sync", str(x), str(y)])
    _xdo(["click", "--repeat", "2", "--delay", "80", "1"])
    return {"success": True, "message": f"Double-clicked at ({x}, {y})"}


def _xdo_right_click(x: int, y: int) -> dict:
    """Right-click at (x,y) via xdotool."""
    _xdo(["mousemove", "--sync", str(x), str(y)])
    _xdo(["click", "3"])
    return {"success": True, "message": f"Right-clicked at ({x}, {y})"}


def _xdo_type(text: str) -> dict:
    """Type text via xdotool with modifier-key safety.

    Strategy:
    1. Try ``xdotool type`` (sends KeyPress/KeyRelease per character).
    2. If the focused window is an Athena-widget app (e.g. xcalc) that
       ignores synthetic type events, fall back to sending individual
       ``xdotool key`` events per character which uses keysym dispatch
       and works more reliably with legacy X11 toolkit widgets.
    """
    # 1. Ensure window focus
    wid = ""
    try:
        wid = _xdo(["getwindowfocus"]).strip()
        if wid:
            _xdo(["windowactivate", "--sync", wid])
    except Exception:
        pass

    # 2. Clear potentially stuck modifier keys
    try:
        _xdo(["keyup", "shift"])
        _xdo(["keyup", "ctrl"])
        _xdo(["keyup", "alt"])
    except Exception:
        pass
    time.sleep(ACTION_DELAY)

    # 3. Try normal xdotool type first
    try:
        _xdo(["type", "--clearmodifiers", "--delay", "25", "--", text])
    except Exception as exc:
        logger.warning("xdotool type failed (%s), falling back to key-per-char", exc)
        _xdo_type_key_per_char(text)
        return {"success": True, "message": f"Typed (key-per-char fallback): {text[:50]}"}

    # 4. Post-type verification: send key-per-char as reinforcement for
    #    Athena/Xaw widget apps (xcalc, xedit, etc.) which silently
    #    ignore xdotool-type events.  This is cheap and idempotent for
    #    apps that already accepted the type events (the duplicate input
    #    can be cleared by the agent if needed).
    #    We only do this when the text is short (≤40 chars) to avoid
    #    doubling long pastes.
    if len(text) <= 40:
        try:
            win_name = _xdo(["getwindowfocus", "getwindowname"]).strip().lower()
        except Exception:
            win_name = ""
        # Heuristic: Athena-widget apps typically have generic titles
        _ATHENA_HINTS = ("xcalc", "calculator", "xedit", "bitmap", "editres")
        if any(h in win_name for h in _ATHENA_HINTS):
            logger.info("Athena-widget window detected ('%s') — reinforcing with key-per-char", win_name)
            _xdo_type_key_per_char(text)

    return {"success": True, "message": f"Typed: {text[:50]}"}


# Character-to-xdotool-keysym map for key-per-char fallback
_CHAR_KEYSYM: dict[str, str] = {
    " ": "space", "!": "exclam", '"': "quotedbl", "#": "numbersign",
    "$": "dollar", "%": "percent", "&": "ampersand", "'": "apostrophe",
    "(": "parenleft", ")": "parenright", "*": "asterisk", "+": "plus",
    ",": "comma", "-": "minus", ".": "period", "/": "slash",
    ":": "colon", ";": "semicolon", "<": "less", "=": "equal",
    ">": "greater", "?": "question", "@": "at", "[": "bracketleft",
    "\\": "backslash", "]": "bracketright", "^": "asciicircum",
    "_": "underscore", "`": "grave", "{": "braceleft", "|": "bar",
    "}": "braceright", "~": "asciitilde",
    "\n": "Return", "\t": "Tab",
}


def _xdo_type_key_per_char(text: str) -> None:
    """Send *text* one character at a time via ``xdotool key``.

    This bypasses the ``xdotool type`` path which relies on XStringToKeysym
    translation that some legacy Athena/Xaw widgets silently ignore.
    """
    for ch in text:
        if ch in _CHAR_KEYSYM:
            keysym = _CHAR_KEYSYM[ch]
        elif ch.isalpha():
            # xdotool key accepts lowercase letter names directly
            keysym = ch.lower()
        elif ch.isdigit():
            keysym = ch  # xdotool handles "0".."9" directly
        else:
            keysym = ch  # last resort — pass through
        try:
            _xdo(["key", "--clearmodifiers", keysym])
        except Exception:
            logger.warning("key-per-char: failed to send keysym '%s' for char '%s'", keysym, ch)
        time.sleep(0.03)  # 30 ms inter-key delay


def _open_terminal() -> dict:
    """Launch a terminal emulator (xfce4-terminal or xterm fallback)."""
    terminal_candidates = ["xfce4-terminal", "xterm"]
    for terminal in terminal_candidates:
        try:
            subprocess.Popen(
                [terminal],
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env={**os.environ, "DISPLAY": ":99"},
            )
            time.sleep(1)
            return {"success": True, "message": f"Opened terminal ({terminal})"}
        except FileNotFoundError:
            continue
        except Exception as e:
            logger.warning("Failed to open %s: %s", terminal, e)
            continue
    return {"success": False, "message": "No terminal emulator available (tried xfce4-terminal, xterm)"}


def _xdo_scroll(x: int, y: int, direction: str) -> dict:
    """Scroll at (x,y) via xdotool button events."""
    _xdo(["mousemove", "--sync", str(x), str(y)])
    btn = "4" if direction == "up" else "5"
    _xdo(["click", "--repeat", "5", "--delay", "40", btn])
    return {"success": True, "message": f"Scrolled {direction} at ({x}, {y})"}


def _xdo_scroll_up() -> dict:
    """Scroll up at the current cursor position."""
    _xdo(["click", "--repeat", "5", "--delay", "40", "4"])
    return {"success": True, "message": "Scrolled up"}


def _xdo_scroll_down() -> dict:
    """Scroll down at the current cursor position."""
    _xdo(["click", "--repeat", "5", "--delay", "40", "5"])
    return {"success": True, "message": "Scrolled down"}


def _xdo_window_minimize(identifier: str) -> dict:
    """Minimise the window matching *identifier*."""
    wids = _xdo(["search", "--name", identifier]).split("\n")
    if wids and wids[0]:
        _xdo(["windowminimize", wids[0]])
        return {"success": True, "message": f"Minimized window: {identifier}"}
    return {"success": False, "message": f"Window not found: {identifier}"}


def _xdo_window_maximize(identifier: str) -> dict:
    """Maximise the window matching *identifier* via wmctrl."""
    wids = _xdo(["search", "--name", identifier]).split("\n")
    if wids and wids[0]:
        subprocess.run(["wmctrl", "-ir", wids[0], "-b", "add,maximized_vert,maximized_horz"], check=False, timeout=5)
        return {"success": True, "message": f"Maximized window: {identifier}"}
    return {"success": False, "message": f"Window not found: {identifier}"}


def _xdo_window_move(identifier: str, x: int, y: int) -> dict:
    """Move the window matching *identifier* to (x,y)."""
    wids = _xdo(["search", "--name", identifier]).split("\n")
    if wids and wids[0]:
        _xdo(["windowmove", wids[0], str(x), str(y)])
        return {"success": True, "message": f"Moved window {identifier} to {x},{y}"}
    return {"success": False, "message": f"Window not found: {identifier}"}


def _xdo_window_resize(identifier: str, w: int, h: int) -> dict:
    """Resize the window matching *identifier* to w x h."""
    wids = _xdo(["search", "--name", identifier]).split("\n")
    if wids and wids[0]:
        _xdo(["windowsize", wids[0], str(w), str(h)])
        return {"success": True, "message": f"Resized window {identifier} to {w}x{h}"}
    return {"success": False, "message": f"Window not found: {identifier}"}


def _xdo_search_window(identifier: str) -> dict:
    """Search for a window by name and return its window IDs."""
    try:
        wids = _xdo(["search", "--name", identifier]).split("\n")
        if wids and wids[0]:
            return {"success": True, "message": f"Found window: {identifier} (wids: {wids})", "wids": wids}
    except Exception:
        pass
    return {"success": False, "message": f"Window not found: {identifier}"}


def _xdo_keydown(key: str) -> dict:
    """Hold a key down via xdotool."""
    combo = _map_key_combo_xdotool(key)
    _xdo(["keydown", combo])
    return {"success": True, "message": f"Key down: {key}"}


def _xdo_keyup(key: str) -> dict:
    """Release a held key via xdotool."""
    combo = _map_key_combo_xdotool(key)
    _xdo(["keyup", combo])
    return {"success": True, "message": f"Key up: {key}"}


def _xdo_type_slow(text: str) -> dict:
    """Type text with a larger inter-key delay (150 ms)."""
    _xdo(["type", "--clearmodifiers", "--delay", "150", "--", text])
    return {"success": True, "message": f"Typed slow: {text[:50]}"}


def _xdo_key(key: str) -> dict:
    """Press and release a key combo via xdotool."""
    combo = _map_key_combo_xdotool(key)
    _xdo(["key", "--clearmodifiers", combo])
    return {"success": True, "message": f"Pressed key: {key}"}


def _xdo_drag(x1: int, y1: int, x2: int, y2: int) -> dict:
    """Drag from (x1,y1) to (x2,y2) via xdotool."""
    _xdo(["mousemove", "--sync", str(x1), str(y1)])
    _xdo(["mousedown", "1"])
    _xdo(["mousemove", "--sync", str(x2), str(y2)])
    _xdo(["mouseup", "1"])
    return {"success": True, "message": f"Dragged ({x1},{y1}) → ({x2},{y2})"}


# ── Deterministic browser launch (shared by xdotool open_url) ─────────────

# Pre-created profile directory (seeded at build-time in Dockerfile)
_CHROME_PROFILE_DIR = "/tmp/chrome-profile"

# Chrome flags that suppress ALL first-run UI, keyring dialogs, and sync prompts
_CHROME_FLAGS: list[str] = [
    "--no-sandbox",
    "--no-first-run",
    "--disable-first-run-ui",
    "--disable-sync",
    "--disable-extensions",
    "--disable-default-apps",
    "--disable-popup-blocking",
    "--disable-translate",
    "--disable-background-networking",
    "--password-store=basic",          # avoid gnome-keyring / kwallet prompts
    "--disable-infobars",
    "--no-default-browser-check",
    f"--user-data-dir={_CHROME_PROFILE_DIR}",
    f"--window-size={SCREEN_WIDTH},{SCREEN_HEIGHT}",
]

# Known modal window titles that should be auto-dismissed after browser launch
_KNOWN_MODAL_TITLES = (
    "Welcome to Google Chrome",
    "Sign in to Chrome",
    "Chrome is being controlled by automated test software",
    "Choose password for new keyring",
    "Unlock Keyring",
    "Set as default browser",
    "Default Browser",
    "Unlock Login Keyring",
)


def _resolve_browser_binary() -> tuple[str, list[str]] | None:
    """Return (binary, extra_flags) for the first available browser.

    Preference: google-chrome > chromium-browser > chromium > firefox.
    Returns None when no browser is found.
    """
    chrome_candidates = ("google-chrome", "google-chrome-stable",
                         "chromium-browser", "chromium")
    for name in chrome_candidates:
        path = shutil.which(name)
        if path:
            return (path, list(_CHROME_FLAGS))

    # Firefox fallback — different flag set
    for name in ("firefox", "firefox-esr"):
        path = shutil.which(name)
        if path:
            return (path, [
                "--new-window",
                f"--width={SCREEN_WIDTH}",
                f"--height={SCREEN_HEIGHT}",
            ])

    return None


def _dismiss_known_modals() -> list[str]:
    """Detect and close known first-run / keyring modal windows via wmctrl.

    Returns a list of window titles that were closed.
    """
    dismissed: list[str] = []
    try:
        result = subprocess.run(
            ["wmctrl", "-l"],
            capture_output=True, text=True, timeout=5,
        )
        if result.returncode != 0:
            return dismissed

        for line in result.stdout.strip().splitlines():
            # wmctrl -l format: <wid> <desktop> <host> <title>
            parts = line.split(None, 3)
            if len(parts) < 4:
                continue
            title = parts[3]
            for modal_title in _KNOWN_MODAL_TITLES:
                if modal_title.lower() in title.lower():
                    subprocess.run(
                        ["wmctrl", "-c", title],
                        capture_output=True, text=True, timeout=3,
                    )
                    dismissed.append(title)
                    logger.info("Auto-dismissed modal window: %s", title)
                    break
    except Exception as exc:
        logger.warning("Modal dismissal scan failed: %s", exc)
    return dismissed


def _open_url_in_browser(url: str) -> dict:
    """Open *url* in a real browser with deterministic, modal-free startup.

    1. Resolve browser binary + flags.
    2. Launch with first-run / keyring suppression.
    3. Wait for window, normalise geometry.
    4. Auto-dismiss any residual modal dialogs.
    5. Fall back to xdg-open only as last resort.
    """
    if not url.startswith(("http://", "https://")):
        url = "https://" + url

    browser = _resolve_browser_binary()
    if browser is None:
        # Ultimate fallback
        logger.warning("No browser binary found — falling back to xdg-open")
        subprocess.Popen(
            ["xdg-open", url],
            stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL,
        )
        return {"success": True, "message": f"Opened URL (xdg-open fallback): {url}"}

    binary, flags = browser
    cmd = [binary] + flags + [url]
    logger.info("Launching browser: %s", " ".join(cmd))

    try:
        subprocess.Popen(
            cmd,
            stdout=subprocess.DEVNULL,
            stderr=subprocess.DEVNULL,
            env={**os.environ, "DISPLAY": ":99"},
        )
    except Exception as exc:
        logger.error("Browser launch failed: %s — falling back to xdg-open", exc)
        subprocess.Popen(
            ["xdg-open", url],
            stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL,
        )
        return {"success": True, "message": f"Opened URL (xdg-open fallback after error): {url}"}

    # Give the browser time to create its window
    time.sleep(2.0)

    # Auto-dismiss any first-run / keyring modals
    dismissed = _dismiss_known_modals()
    if dismissed:
        time.sleep(0.5)
        # Dismiss again in case closing one modal spawned another
        _dismiss_known_modals()

    # Find and normalise the browser window
    norm_msg = ""
    for hint in ("chrome", "chromium", "firefox", "mozilla", "navigator"):
        wids = _xdo_search_window_ids(hint)
        if wids:
            wid = wids[-1]
            try:
                _xdo(["windowactivate", "--sync", wid])
                time.sleep(0.3)
                norm_msg = _xdo_normalize_window(wid)
            except Exception:
                norm_msg = "window normalisation skipped"
            break

    dismiss_info = f" (dismissed modals: {dismissed})" if dismissed else ""
    return {
        "success": True,
        "message": f"Opened URL: {url} via {binary}. {norm_msg}{dismiss_info}",
    }


def _xdo_open_url(url: str) -> dict:
    """Open URL in a deterministic browser — avoids xdg-open first-run problems."""
    return _open_url_in_browser(url)


def _xdo_hover(x: int, y: int) -> dict:
    """Move the mouse to (x,y) without clicking."""
    _xdo(["mousemove", "--sync", str(x), str(y)])
    return {"success": True, "message": f"Hovered at ({x}, {y})"}


def _xdo_middle_click(x: int, y: int) -> dict:
    """Middle-click at (x,y) via xdotool."""
    _xdo(["mousemove", "--sync", str(x), str(y)])
    _xdo(["click", "2"])
    return {"success": True, "message": f"Middle-clicked at ({x}, {y})"}


def _is_terminal_focused() -> bool:
    """Check if the currently focused window is a terminal emulator.

    Terminals interpret Ctrl+V as a literal control character; paste
    requires Ctrl+Shift+V instead.
    """
    try:
        name = subprocess.run(
            ["xdotool", "getwindowfocus", "getwindowname"],
            capture_output=True, text=True, timeout=3,
        ).stdout.strip().lower()
        _TERMINAL_HINTS = (
            "terminal", "xterm", "konsole", "alacritty", "kitty",
            "tmux", "bash", "zsh", "sh —", "fish",
        )
        return any(hint in name for hint in _TERMINAL_HINTS)
    except Exception:
        return False


def _xdo_paste(text: str) -> dict:
    """Copy text to clipboard then paste via Ctrl+V (or Ctrl+Shift+V in terminals)."""
    subprocess.run(
        ["xclip", "-selection", "clipboard"],
        input=text.encode(), check=True, timeout=5,
    )
    if _is_terminal_focused():
        _xdo(["key", "--clearmodifiers", "ctrl+shift+v"])
    else:
        _xdo(["key", "--clearmodifiers", "ctrl+v"])
    return {"success": True, "message": f"Pasted: {text[:50]}"}


def _xdo_copy() -> dict:
    """Copy the current selection to clipboard via xdotool."""
    _xdo(["key", "--clearmodifiers", "ctrl+c"])
    return {"success": True, "message": "Copied selection to clipboard"}


def _xdo_hotkey(keys: list[str]) -> dict:
    """Press a multi-key combo via xdotool."""
    combo = "+".join(_map_key_combo_xdotool(k) for k in keys)
    _xdo(["key", "--clearmodifiers", combo])
    return {"success": True, "message": f"Hotkey: {'+'.join(keys)}"}


def _xdo_focus_window(identifier: str) -> dict:
    """Focus a window by name or class."""
    wids = _xdo_search_window_ids(identifier)
    if wids:
        wid = wids[0]
        _xdo(["windowactivate", "--sync", wid])
        normalization_msg = _xdo_normalize_window(wid)
        return {
            "success": True,
            "message": f"Focused window: {identifier} (wid={wid}; {normalization_msg})",
        }
    return {"success": False, "message": f"Window not found: {identifier}"}


def _xdo_open_app(app_name: str) -> dict:
    """Launch an application by command name.

    After a successful launch the new window is automatically found,
    activated, and normalised to a deterministic geometry so that
    subsequent coordinate-based actions are reliable.
    """
    candidates = _expand_app_launch_candidates(app_name)
    if not candidates:
        return {"success": False, "message": "open_app requires a non-empty app command"}

    failures: list[str] = []
    for candidate in candidates:
        parts = shlex.split(candidate)
        if not parts:
            continue
        binary = parts[0]
        if shutil.which(binary) is None:
            failures.append(f"{candidate}: command not found")
            continue
        try:
            subprocess.Popen(
                parts,
                stdout=subprocess.DEVNULL,
                stderr=subprocess.DEVNULL,
                env={**os.environ, "DISPLAY": ":99"},
            )
            time.sleep(1.5)  # give the window time to materialise

            # ── Post-launch: find, activate & normalise the new window ──
            norm_msg = _post_launch_normalize(candidate)
            return {"success": True, "message": f"Launched: {candidate}. {norm_msg}"}
        except Exception as e:
            failures.append(f"{candidate}: {e}")

    reason = "; ".join(failures) if failures else "no launch candidates"
    return {
        "success": False,
        "message": f"Failed to launch '{app_name}'. Tried: {', '.join(candidates)}. Reasons: {reason}",
    }


def _post_launch_normalize(hint: str) -> str:
    """Find the most-recently-created window, activate it, and normalise.

    *hint* is used for a name-based search first; if that fails the
    currently-active window is normalised instead.
    """
    wids = _xdo_search_window_ids(hint)
    if not wids:
        # Fallback: try the currently-active window
        try:
            wid = _xdo(["getactivewindow"]).strip()
            if wid:
                wids = [wid]
        except Exception:
            pass
    if not wids:
        return "window not found for normalisation"

    wid = wids[-1]  # most recent match
    try:
        _xdo(["windowactivate", "--sync", wid])
        # Brief extra settle time after activation
        time.sleep(0.3)
        norm_msg = _xdo_normalize_window(wid)
        return f"Window activated (wid={wid}); {norm_msg}"
    except Exception as e:
        return f"post-launch normalisation failed: {e}"


def _wmctrl_close_window(identifier: str) -> dict:
    """Gracefully close a window via EWMH using wmctrl -c."""
    result = subprocess.run(
        ["wmctrl", "-c", identifier],
        capture_output=True, text=True, timeout=5,
    )
    if result.returncode == 0:
        return {"success": True, "message": f"Closed window: {identifier}"}
    return {"success": False, "message": f"Failed to close window: {identifier} — {result.stderr.strip()}"}


def _xdo_screenshot_full() -> str:
    """Capture the full screen via scrot."""
    subprocess.run(
        ["scrot", "-z", "-o", "/tmp/full.png"],
        check=True, timeout=5,
    )
    with open("/tmp/full.png", "rb") as f:
        return base64.b64encode(f.read()).decode("ascii")


def _xdo_screenshot_region(x: int, y: int, w: int, h: int) -> str:
    """Capture a region of the screen via scrot."""
    subprocess.run(
        ["scrot", "-z", "-o", "-a", f"{x},{y},{w},{h}", "/tmp/region.png"],
        check=True, timeout=5,
    )
    with open("/tmp/region.png", "rb") as f:
        return base64.b64encode(f.read()).decode("ascii")


def _xdo_focus_click(identifier: str, x: int, y: int) -> dict:
    """Focus a window and then click at x, y."""
    # 1. Focus the window
    wids = _xdo(["search", "--name", identifier]).split("\n")
    if not wids or not wids[0]:
        return {"success": False, "message": f"Window not found: {identifier}"}
    
    _xdo(["windowactivate", "--sync", wids[0]])
    time.sleep(0.2)
    
    # 2. Click relative to that window (or absolute if just screen coords provided)
    # The command provided assumes x,y are screen coordinates.
    # To click safely after focus, we just move and click.
    _xdo(["mousemove", "--sync", str(x), str(y)])
    _xdo(["click", "1"])
    
    return {"success": True, "message": f"Focused {identifier} and clicked at ({x}, {y})"}


# ══════════════════════════════════════════════════════════════════════════════
#  Shared helpers
# ══════════════════════════════════════════════════════════════════════════════

# ── wmctrl-based window management (DISPLAY-independent layer) ────────────────

def _wmctrl_focus_window(identifier: str) -> dict:
    """Focus a window by partial title match using wmctrl (no xdotool)."""
    result = subprocess.run(
        ["wmctrl", "-a", identifier],
        capture_output=True, text=True, timeout=5,
    )
    if result.returncode == 0:
        return {"success": True, "message": f"Focused window: {identifier}"}
    return {"success": False, "message": f"Window not found: {identifier} — {result.stderr.strip()}"}


def _wmctrl_search_window(identifier: str) -> dict:
    """Search for windows by title via wmctrl -l."""
    result = subprocess.run(
        ["wmctrl", "-l"],
        capture_output=True, text=True, timeout=5,
    )
    if result.returncode != 0:
        return {"success": False, "message": f"wmctrl list failed: {result.stderr.strip()}"}
    matches = [line for line in result.stdout.splitlines() if identifier.lower() in line.lower()]
    if matches:
        return {"success": True, "message": f"Found {len(matches)} window(s) matching '{identifier}': {'; '.join(matches[:5])}"}
    return {"success": False, "message": f"No windows matching: {identifier}"}


def _wmctrl_minimize_window(identifier: str) -> dict:
    """Minimize a window by title using wmctrl."""
    # wmctrl doesn't have a direct minimize; use -b add,hidden
    result = subprocess.run(
        ["wmctrl", "-r", identifier, "-b", "add,hidden"],
        capture_output=True, text=True, timeout=5,
    )
    if result.returncode == 0:
        return {"success": True, "message": f"Minimized window: {identifier}"}
    return {"success": False, "message": f"Failed to minimize: {identifier} — {result.stderr.strip()}"}


def _wmctrl_maximize_window(identifier: str) -> dict:
    """Maximize a window by title using wmctrl."""
    result = subprocess.run(
        ["wmctrl", "-r", identifier, "-b", "add,maximized_vert,maximized_horz"],
        capture_output=True, text=True, timeout=5,
    )
    if result.returncode == 0:
        return {"success": True, "message": f"Maximized window: {identifier}"}
    return {"success": False, "message": f"Failed to maximize: {identifier} — {result.stderr.strip()}"}


def _wmctrl_move_window(identifier: str, x: int, y: int) -> dict:
    """Move a window to (x,y) using wmctrl."""
    # wmctrl -r <name> -e <gravity>,<x>,<y>,<w>,<h>  (-1 = unchanged)
    result = subprocess.run(
        ["wmctrl", "-r", identifier, "-e", f"0,{x},{y},-1,-1"],
        capture_output=True, text=True, timeout=5,
    )
    if result.returncode == 0:
        return {"success": True, "message": f"Moved window {identifier} to ({x},{y})"}
    return {"success": False, "message": f"Failed to move: {identifier} — {result.stderr.strip()}"}


def _wmctrl_resize_window(identifier: str, w: int, h: int) -> dict:
    """Resize a window using wmctrl."""
    result = subprocess.run(
        ["wmctrl", "-r", identifier, "-e", f"0,-1,-1,{w},{h}"],
        capture_output=True, text=True, timeout=5,
    )
    if result.returncode == 0:
        return {"success": True, "message": f"Resized window {identifier} to {w}x{h}"}
    return {"success": False, "message": f"Failed to resize: {identifier} — {result.stderr.strip()}"}



_KEY_MAP = {
    "enter": "Enter", "return": "Enter",
    "tab": "Tab",
    "escape": "Escape", "esc": "Escape",
    "backspace": "Backspace",
    "delete": "Delete",
    "space": " ",
    "up": "ArrowUp", "down": "ArrowDown",
    "left": "ArrowLeft", "right": "ArrowRight",
    "home": "Home", "end": "End",
    "pageup": "PageUp", "pagedown": "PageDown",
}

_KEY_MAP_XDO = {
    "enter": "Return", "return": "Return",
    "tab": "Tab",
    "escape": "Escape", "esc": "Escape",
    "backspace": "BackSpace",
    "delete": "Delete",
    "space": "space",
    "up": "Up", "down": "Down",
    "left": "Left", "right": "Right",
    "home": "Home", "end": "End",
    "pageup": "Prior", "pagedown": "Next",
}


def _map_key_combo(key: str) -> str:
    """Map a user key string to a Playwright key combo."""
    if "+" in key:
        parts = [p.strip() for p in key.split("+")]
        modifiers = []
        for p in parts[:-1]:
            pl = p.lower()
            if pl in ("ctrl", "control"):
                modifiers.append("Control")
            elif pl == "alt":
                modifiers.append("Alt")
            elif pl == "shift":
                modifiers.append("Shift")
            elif pl in ("meta", "super", "win", "cmd"):
                modifiers.append("Meta")
            else:
                # Add other modifiers if any, or just ignore
                pass
        
        # Handle the last part (the actual key)
        last_part = parts[-1]
        # Normalize last part
        final = _KEY_MAP.get(last_part.lower(), last_part)
        
        # Special case: ensure single char keys are proper case if needed?
        # Playwright usually handles "a", "A", etc.
        
        return "+".join(modifiers + [final])
    
    # Single key
    k = key.lower()
    if k in ("ctrl", "control"): return "Control"
    if k == "alt": return "Alt"
    if k == "shift": return "Shift"
    if k in ("meta", "super", "win", "cmd"): return "Meta"
    
    return _KEY_MAP.get(k, key)


def _map_key_combo_xdotool(key: str) -> str:
    """Map a user key string to an xdotool key combo."""
    if "+" in key:
        parts = [p.strip() for p in key.split("+")]
        mapped = []
        for p in parts:
            pl = p.lower()
            if pl in ("ctrl", "control"):
                mapped.append("ctrl")
            elif pl == "alt":
                mapped.append("alt")
            elif pl == "shift":
                mapped.append("shift")
            elif pl in ("meta", "super", "win", "cmd"):
                mapped.append("super")
            else:
                mapped.append(_KEY_MAP_XDO.get(pl, p))
        return "+".join(mapped)
    return _KEY_MAP_XDO.get(key.lower(), key)


def _do_wait(duration: float) -> dict:
    """Sleep for *duration* seconds (clamped to 0.1–10s)."""
    capped = min(max(duration, 0.1), 10.0)
    time.sleep(capped)
    return {"success": True, "message": f"Waited {capped:.1f}s"}


# ══════════════════════════════════════════════════════════════════════════════
#  HTTP Server
# ══════════════════════════════════════════════════════════════════════════════

class AgentHandler(BaseHTTPRequestHandler):
    """HTTP handler supporting both browser and desktop modes."""

    def log_message(self, fmt, *args):
        """Redirect HTTP request logging to the module logger."""
        logger.debug("HTTP %s", fmt % args)

    def _respond(self, code: int, data: dict):
        """Send a JSON response with the given status code."""
        body = json.dumps(data).encode()
        self.send_response(code)
        self.send_header("Content-Type", "application/json")
        self.send_header("Content-Length", str(len(body)))
        self.end_headers()
        self.wfile.write(body)

    def _read_body(self) -> dict:
        """Parse the JSON request body, enforcing a size limit."""
        length = int(self.headers.get("Content-Length", 0))
        if length == 0:
            return {}
        if length > _MAX_BODY_SIZE:
            raise ValueError(f"Request body too large: {length} bytes (max {_MAX_BODY_SIZE})")
        raw = self.rfile.read(length)
        return json.loads(raw)

    # ── GET ───────────────────────────────────────────────────────────────

    def do_GET(self):
        """Handle GET requests (/health, /health/a11y, /screenshot)."""
        if self.path == "/health":
            self._respond(200, {
                "status": "ok",
                "browser": _browser is not None,
                "default_mode": DEFAULT_MODE,
                "cdp_url": "http://127.0.0.1:9223",
            })
            return

        if self.path == "/health/a11y":
            # AT-SPI accessibility bus health check.
            # GObject Introspection / AT-SPI internally spins a GLib main
            # loop which conflicts with the HTTPServer thread.  To avoid
            # "Cannot run the event loop while another loop is running",
            # we run the check in a **separate subprocess**.
            import subprocess, sys
            _a11y_script = (
                "import sys;"
                "try:\n"
                "    import gi; gi.require_version('Atspi', '2.0');"
                "    from gi.repository import Atspi;"
                "    apps = Atspi.get_desktop(0).get_child_count();"
                "    print(f'OK:{apps}')\n"
                "except ImportError as e:\n"
                "    print(f'NOIMPORT:{e}'); sys.exit(1)\n"
                "except Exception as e:\n"
                "    print(f'ERR:{e}'); sys.exit(2)"
            )
            try:
                proc = subprocess.run(
                    [sys.executable, "-c", _a11y_script],
                    capture_output=True, text=True, timeout=10,
                )
                stdout = proc.stdout.strip()
                if proc.returncode == 0 and stdout.startswith("OK:"):
                    app_count = int(stdout.split(":")[1])
                    self._respond(200, {"healthy": app_count > 0, "bindings": True, "apps": app_count})
                elif stdout.startswith("NOIMPORT:"):
                    self._respond(200, {"healthy": False, "bindings": False, "error": stdout})
                else:
                    self._respond(200, {"healthy": False, "bindings": True, "error": stdout or proc.stderr.strip()})
            except subprocess.TimeoutExpired:
                self._respond(200, {"healthy": False, "bindings": False, "error": "AT-SPI health check timed out"})
            except Exception as e:
                self._respond(200, {"healthy": False, "bindings": False, "error": str(e)})
            return

        if self.path.startswith("/screenshot"):
            # Parse ?mode=desktop|browser from query string
            mode = self._parse_mode_from_query()
            with _lock:
                try:
                    if mode == "desktop":
                        b64 = _screenshot_desktop()
                        self._respond(200, {"screenshot": b64, "method": "desktop"})
                    else:
                        try:
                            b64 = _screenshot_playwright()
                            self._respond(200, {"screenshot": b64, "method": "playwright"})
                        except Exception:
                            b64 = _screenshot_desktop()
                            self._respond(200, {"screenshot": b64, "method": "desktop_fallback"})
                except Exception as e:
                    self._respond(500, {"error": str(e)})
            return

        self._respond(404, {"error": "not found"})

    # ── POST ──────────────────────────────────────────────────────────────

    def do_POST(self):
        """Handle POST requests (/action, /mode)."""
        body = self._read_body()

        if self.path == "/action":
            # Handle 'wait' outside the lock so it doesn't block
            # screenshots and other concurrent requests for up to 10 s.
            raw_action = body.get("action", "")
            resolved = resolve_action(raw_action)
            if resolved == "wait":
                dur = 2.0
                t = body.get("text", "")
                if t:
                    try:
                        dur = float(t)
                    except ValueError:
                        pass
                self._respond(200, _do_wait(dur))
                return

            with _lock:
                try:
                    result = self._dispatch_action(body)
                    self._respond(200, result)
                except Exception as e:
                    logger.exception("Action failed")
                    self._respond(500, {"success": False, "message": str(e)})
            return

        if self.path == "/mode":
            # Switch default mode at runtime
            new_mode = body.get("mode", "").lower()
            if new_mode in ("browser", "desktop"):
                global DEFAULT_MODE
                DEFAULT_MODE = new_mode
                logger.info("Default mode switched to: %s", DEFAULT_MODE)
                self._respond(200, {"mode": DEFAULT_MODE})
            else:
                self._respond(400, {"error": "mode must be 'browser' or 'desktop'"})
            return

        self._respond(404, {"error": "not found"})

    # ── Helpers ───────────────────────────────────────────────────────────

    def _parse_mode_from_query(self) -> str:
        """Extract mode from ?mode=... query param, or use DEFAULT_MODE."""
        if "?" in self.path:
            qs = self.path.split("?", 1)[1]
            for part in qs.split("&"):
                if part.startswith("mode="):
                    val = part.split("=", 1)[1].lower()
                    if val in ("browser", "desktop"):
                        return val
        return DEFAULT_MODE

    def _dispatch_action(self, body: dict) -> dict:
        """Route an incoming action to the correct engine dispatcher."""
        start_time = time.time()
        
        # 1. Resolve alias
        raw_action = body.get("action", "")
        action = resolve_action(raw_action)
        
        coords = body.get("coordinates", [])
        text = body.get("text", "")
        target = body.get("target", "")
        mode = body.get("mode", DEFAULT_MODE).lower()

        x = coords[0] if len(coords) >= 1 else SCREEN_WIDTH // 2
        y = coords[1] if len(coords) >= 2 else SCREEN_HEIGHT // 2

        result = {"success": False, "message": "Unknown error"}

        try:
            if action == "wait":
                duration = 2.0
                if text:
                    try:
                        duration = float(text)
                    except ValueError:
                        pass
                result = _do_wait(duration)
            
            # Dispatch to mode-specific handlers
            elif mode == "omni_accessibility":
                result = self._dispatch_accessibility(action, text, target)
            elif mode == "desktop":
                result = self._dispatch_desktop(action, x, y, text, coords, target)
            else:
                result = self._dispatch_browser(action, x, y, text, coords, target)
        except Exception as e:
            logger.exception(f"Action {action} failed")
            result = {"success": False, "message": str(e)}

        # Structured logging
        latency = (time.time() - start_time) * 1000
        log_entry = {
            "action": action,
            "engine": mode,
            "success": result.get("success", False),
            "latency_ms": latency,
            "raw_action": raw_action
        }
        logger.info(json.dumps(log_entry))
        
        return result

    def _dispatch_accessibility(self, action: str, text: str, target: str) -> dict:
        """Dispatch a single action to the AT-SPI accessibility engine.

        The accessibility_engine module uses async functions internally
        (asyncio.to_thread for AT-SPI GI calls), so we run them in a
        dedicated event loop.  We use ``asyncio.new_event_loop()`` inside
        a thread-isolated context to avoid conflicts with GLib's main loop.
        """
        import asyncio
        import threading
        try:
            from backend.engines.accessibility_engine import execute_accessibility_action
        except ImportError as exc:
            return {
                "success": False,
                "message": f"Accessibility engine not available: {exc}",
            }

        result_container = [None]
        error_container = [None]

        def _run_in_thread():
            """Run the async accessibility action in a fresh event loop on a separate thread."""
            try:
                loop = asyncio.new_event_loop()
                asyncio.set_event_loop(loop)
                try:
                    result_container[0] = loop.run_until_complete(
                        execute_accessibility_action(action, text=text, target=target)
                    )
                finally:
                    loop.close()
            except Exception as exc:
                error_container[0] = exc

        t = threading.Thread(target=_run_in_thread, daemon=True)
        t.start()
        t.join(timeout=30.0)

        if error_container[0] is not None:
            return {"success": False, "message": f"Accessibility action failed: {error_container[0]}"}
        if result_container[0] is None:
            return {"success": False, "message": "Accessibility action timed out (30s)"}
        return result_container[0]

    def _dispatch_browser(self, action: str, x: int, y: int, text: str, coords: list, target: str = "") -> dict:
        """Dispatch a single action to the Playwright browser engine."""
        # ── Mouse / Interaction ───────────────────────────────────────
        if action == "click":
            return _pw_click(x, y)
        elif action == "double_click":
            return _pw_double_click(x, y)
        elif action == "right_click":
            return _pw_right_click(x, y)
        elif action == "middle_click":
            return _pw_middle_click(x, y)
        elif action == "hover":
            return _pw_hover(x, y)
        elif action == "drag":
            if len(coords) >= 4:
                return _pw_drag(coords[0], coords[1], coords[2], coords[3])
            return {"success": False, "message": "drag requires 4 coordinates [x1, y1, x2, y2]"}
        # ── Input ─────────────────────────────────────────────────────
        elif action == "type":
            selector = target or (text.split("|")[0] if "|" in text else "")
            value = text.split("|")[1] if "|" in text else text
            if not target and not "|" in text:
                 return _pw_type(text, coords)
            return _pw_type(value, coords, selector)
        elif action == "fill":
            selector = target or (text.split("|")[0] if "|" in text else "")
            value = text.split("|")[1] if "|" in text else text
            if not selector:
                return {"success": False, "message": "fill requires target (CSS selector)"}
            return _pw_fill(selector, value)
        elif action == "key":
            return _pw_key(text)
        elif action == "hotkey":
            keys = [k.strip() for k in text.split("+")]
            return _pw_hotkey(keys)
        elif action == "clear_input":
            selector = target or text
            if not selector:
                return {"success": False, "message": "clear_input requires target (CSS selector)"}
            return _pw_clear_input(selector)
        elif action == "select_option":
            selector = target or ""
            if not selector:
                return {"success": False, "message": "select_option requires target (CSS selector)"}
            return _pw_select_option(selector, text)
        elif action == "paste":
            return _pw_paste(text)
        elif action == "copy":
            return _pw_copy()
        # ── Navigation ────────────────────────────────────────────────
        elif action == "open_url":
            return _pw_navigate(text)
        elif action == "reload":
            return _pw_reload()
        elif action == "go_back":
            return _pw_go_back()
        elif action == "go_forward":
            return _pw_go_forward()
        # ── Tabs ──────────────────────────────────────────────────────
        elif action == "new_tab":
            return _pw_new_tab(text)
        elif action == "close_tab":
            return _pw_close_tab()
        elif action == "switch_tab":
            return _pw_switch_tab(text or target)
        # ── Scrolling ─────────────────────────────────────────────────
        elif action == "scroll":
            direction = text.lower() if text else "down"
            return _pw_scroll(x, y, direction)
        elif action == "scroll_to":
            selector = target or text
            if not selector:
                return {"success": False, "message": "scroll_to requires target (CSS selector)"}
            return _pw_scroll_to(selector)
        elif action == "scroll_into_view":
            selector = target or text
            if not selector:
                return {"success": False, "message": "scroll_into_view requires target (CSS selector)"}
            return _pw_scroll_into_view(selector)
        # ── DOM / Semantic ────────────────────────────────────────────
        elif action == "get_text":
            selector = target or text
            if not selector:
                return {"success": False, "message": "get_text requires target (CSS selector)"}
            return _pw_get_text(selector)
        elif action == "find_element":
            description = target or text
            if not description:
                return {"success": False, "message": "find_element requires target (text description)"}
            return _pw_find_element(description)
        # ── JavaScript ────────────────────────────────────────────────
        elif action == "evaluate_js":
            if not text:
                return {"success": False, "message": "evaluate_js requires text (JS code)"}
            return _pw_evaluate_js(text)
        # ── Control ───────────────────────────────────────────────────
        elif action == "wait_for":
            selector = target or text
            if not selector:
                return {"success": False, "message": "wait_for requires target (CSS selector)"}
            return _pw_wait_for(selector)
        elif action == "wait_for_navigation":
            return _pw_wait_for_navigation()
        elif action == "screenshot_region":
            if len(coords) >= 4:
                b64 = _pw_screenshot_region(coords[0], coords[1], coords[2], coords[3])
                return {"success": True, "message": f"Region screenshot captured", "screenshot": b64}
            return {"success": False, "message": "screenshot_region needs 4 coords [x, y, width, height]"}
        elif action == "screenshot_viewport":
            b64 = _pw_screenshot_viewport()
            return {"success": True, "message": "Viewport screenshot captured", "screenshot": b64}
        elif action == "screenshot_element":
            selector = target or text
            if not selector:
                return {"success": False, "message": "screenshot_element requires target"}
            b64 = _pw_screenshot_element(selector)
            if b64:
                return {"success": True, "message": "Element screenshot captured", "screenshot": b64}
            return {"success": False, "message": "Failed to capture element screenshot"}
        elif action == "get_html":
            selector = target or text
            return _pw_get_html(selector)
        elif action == "get_attribute":
            selector = target or (text.split("|")[0] if "|" in text else "")
            attribute = text.split("|")[1] if "|" in text else ""
            if not selector or not attribute:
                return {"success": False, "message": "get_attribute requires target (selector) and attribute (in text as selector|attribute)"}
            return _pw_get_attribute(selector, attribute)
        elif action == "query_selector":
            selector = target or text
            if not selector:
                return {"success": False, "message": "query_selector requires target (CSS selector)"}
            return _pw_query_selector(selector)
        elif action == "query_all":
            selector = target or text
            if not selector:
                return {"success": False, "message": "query_all requires target (CSS selector)"}
            return _pw_query_all(selector)
        elif action == "get_bounding_box":
            selector = target or text
            if not selector:
                return {"success": False, "message": "get_bounding_box requires target (CSS selector)"}
            return _pw_get_bounding_box(selector)
        elif action == "get_visible_elements":
            selector = target or text
            if not selector:
                return {"success": False, "message": "get_visible_elements requires target (CSS selector)"}
            return _pw_get_visible_elements(selector)
        elif action == "evaluate_on_selector":
            selector = target or (text.split("|")[0] if "|" in text else "")
            script = text.split("|")[1] if "|" in text else ""
            if not selector or not script:
                 return {"success": False, "message": "evaluate_on_selector requires target and script"}
            return _pw_evaluate_on_selector(selector, script)
        elif action == "upload_file":
            selector = target or (text.split("|")[0] if "|" in text else "")
            file_path = text.split("|")[1] if "|" in text else text
            if not selector or not file_path:
                 return {"success": False, "message": "upload_file requires target and file path"}
            return _pw_upload_file(selector, file_path)
        elif action == "download_file":
            selector = target or text
            return _pw_download_file(selector)
        elif action == "handle_file_chooser":
            return {"success": False, "message": "Not implemented yet"}
        elif action == "set_cookies":
            try:
                cookies = json.loads(text)
                return _pw_set_cookies(cookies)
            except Exception as e:
                return {"success": False, "message": f"set_cookies requires JSON text: {e}"}
        elif action == "get_cookies":
            return _pw_get_cookies()
        elif action == "clear_cookies":
            return _pw_clear_cookies()
        elif action == "storage_state":
            return {"success": False, "message": "Not implemented yet"}
        elif action == "new_context":
            return _pw_new_context()
        elif action == "switch_context":
            identifier = target or text
            return _pw_switch_context(identifier)
        elif action == "close_context":
            return _pw_close_context()
        elif action == "intercept_request":
            return {"success": False, "message": "Not implemented yet"}
        elif action == "monitor_requests":
            return {"success": False, "message": "Not implemented yet"}
        elif action == "get_response_body":
            return {"success": False, "message": "Not implemented yet"}
        elif action == "assert_element_present":
            selector = target or text
            if not selector:
                return {"success": False, "message": "assert_element_present requires target (CSS selector)"}
            return _pw_query_selector(selector)
        elif action == "verify_text":
            selector = target or "body"
            needle = text or ""
            if not needle:
                return {"success": False, "message": "verify_text requires text to verify"}
            res = _pw_get_text(selector)
            if not res.get("success"):
                return res
            found = needle.lower() in res.get("message", "").lower()
            return {"success": found, "message": f"verify_text={'ok' if found else 'not found'}: {needle[:120]}"}
        elif action == "retry_last_action":
            return {"success": False, "message": "Not implemented yet"}
        elif action == "fallback_strategy":
            return {"success": False, "message": "Not implemented yet"}
        elif action == "set_viewport":
            if len(coords) >= 2:
                return _pw_set_viewport(coords[0], coords[1])
            return {"success": False, "message": "set_viewport requires width and height"}
        elif action == "zoom":
            try:
                level = float(text)
                return _pw_zoom(level)
            except ValueError:
                return {"success": False, "message": "zoom requires a float value"}
        elif action == "block_resource":
            return _pw_block_resource(text)
        elif action == "type_at":
            if not coords or len(coords) < 2:
                 return {"success": False, "message": "type_at requires coordinates [x, y]"}
            return _pw_type(text, coords)
        elif action == "press_sequential":
            # Stub: treat as type
            return _pw_type(text, coords)
        elif action == "scroll_to_selector":
            selector = target or text
            if not selector:
                return {"success": False, "message": "scroll_to_selector requires target"}
            return _pw_scroll_to(selector)
        elif action == "export_page_pdf":
            b64 = _pw_export_page_pdf()
            return {"success": True, "message": "PDF exported", "pdf": b64}
        else:
            return {"success": False, "message": f"Unsupported action '{action}' in browser mode", "hint": "Check engine capability mapping"}

    def _dispatch_desktop(self, action: str, x: int, y: int, text: str, coords: list, target: str = "") -> dict:
        """Dispatch a single action to the xdotool desktop engine."""
        # ── Mouse / Interaction ───────────────────────────────────────
        if action == "click":
            return _xdo_click(x, y)
        elif action == "double_click":
            return _xdo_double_click(x, y)
        elif action == "right_click":
            return _xdo_right_click(x, y)
        elif action == "middle_click":
            return _xdo_middle_click(x, y)
        elif action == "hover":
            return _xdo_hover(x, y)
        elif action == "drag":
            if len(coords) >= 4:
                return _xdo_drag(coords[0], coords[1], coords[2], coords[3])
            return {"success": False, "message": "drag requires 4 coordinates [x1, y1, x2, y2]"}
        # ── Input ─────────────────────────────────────────────────────
        elif action == "type":
            if coords and len(coords) >= 2:
                _xdo_click(coords[0], coords[1])
                time.sleep(0.1)
            try:
                return _xdo_type(text)
            except Exception:
                logger.warning("xdotool type failed, trying paste fallback")
                return _xdo_paste(text)
        elif action == "key":
            return _xdo_key(text)
        elif action == "keydown":
            return _xdo_keydown(text)
        elif action == "keyup":
            return _xdo_keyup(text)
        elif action == "type_slow":
            if coords and len(coords) >= 2:
                _xdo_click(coords[0], coords[1])
                time.sleep(0.1)
            try:
                return _xdo_type_slow(text)
            except Exception:
                logger.warning("xdotool type_slow failed, trying paste fallback")
                return _xdo_paste(text)
        elif action == "hotkey":
            keys = [k.strip() for k in text.split("+")]
            return _xdo_hotkey(keys)
        elif action == "paste":
            return _xdo_paste(text)
        elif action == "copy":
            return _xdo_copy()
        # ── Navigation ────────────────────────────────────────────────
        elif action == "open_url":
            return _xdo_open_url(text)
        # ── Scrolling ─────────────────────────────────────────────────
        elif action == "scroll":
            direction = text.lower() if text else "down"
            return _xdo_scroll(x, y, direction)
        elif action == "scroll_up":
            return _xdo_scroll_up()
        elif action == "scroll_down":
            return _xdo_scroll_down()
        # ── Desktop / Window ──────────────────────────────────────────
        elif action == "focus_window":
            identifier = target or text
            if not identifier:
                return {"success": False, "message": "focus_window requires target (window name)"}
            return _xdo_focus_window(identifier)
        elif action == "window_activate":
            identifier = target or text
            if not identifier:
                return {"success": False, "message": "window_activate requires target"}
            return _xdo_focus_window(identifier)
        elif action == "focus_mouse":
            _xdo(["mousemove", "--sync", str(x), str(y)])
            return {"success": True, "message": f"Focused mouse at ({x}, {y})"}
        elif action == "mousemove":
            if len(coords) >= 2:
                _xdo(["mousemove", "--sync", str(coords[0]), str(coords[1])])
                return {"success": True, "message": f"Moved mouse to ({coords[0]}, {coords[1]})"}
            return {"success": False, "message": "mousemove requires coordinates [x, y]"}
        elif action == "open_app":
            app = target or text
            if not app:
                return {"success": False, "message": "open_app requires target (app command)"}
            return _xdo_open_app(app)
        elif action == "close_window":
            identifier = target or text
            if not identifier:
                return {"success": False, "message": "close_window requires target (window title or class)"}
            return _wmctrl_close_window(identifier)
        elif action == "window_minimize":
            identifier = target or text
            if not identifier:
                return {"success": False, "message": "window_minimize requires target"}
            return _xdo_window_minimize(identifier)
        elif action == "window_maximize":
            identifier = target or text
            if not identifier:
                return {"success": False, "message": "window_maximize requires target"}
            return _xdo_window_maximize(identifier)
        elif action == "window_move":
            identifier = target or text
            if not identifier or not coords or len(coords) < 2:
                return {"success": False, "message": "window_move requires target and 2 coordinates"}
            return _xdo_window_move(identifier, coords[0], coords[1])
        elif action == "window_resize":
            identifier = target or text
            if not identifier or not coords or len(coords) < 2:
                return {"success": False, "message": "window_resize requires target and 2 coordinates"}
            return _xdo_window_resize(identifier, coords[0], coords[1])
        elif action == "search_window":
            identifier = target or text
            if not identifier:
                return {"success": False, "message": "search_window requires target"}
            return _xdo_search_window(identifier)
        elif action == "focus_click":
            identifier = target or text
            if not identifier:
                 return {"success": False, "message": "focus_click requires target (window name)"}
            return _xdo_focus_click(identifier, x, y)
        # ── Fill / Clear (desktop approximation via keyboard) ─────────
        elif action == "fill":
            # In desktop mode, fill = click + wait + clear stuck mods + select all + delete + type
            if coords and len(coords) >= 2:
                _xdo_click(coords[0], coords[1])
                time.sleep(0.1)
            try:
                _xdo(["keyup", "shift"])
                _xdo(["keyup", "ctrl"])
                _xdo(["keyup", "alt"])
            except Exception:
                pass
            time.sleep(0.05)
            _xdo(["key", "--clearmodifiers", "ctrl+a"])
            time.sleep(0.05)
            _xdo(["key", "--clearmodifiers", "Delete"])
            time.sleep(0.1)
            value = text or ""
            try:
                _xdo(["type", "--clearmodifiers", "--delay", "25", "--", value])
            except Exception:
                logger.warning("Desktop fill type failed, using paste fallback")
                return _xdo_paste(value)
            return {"success": True, "message": f"Filled (desktop): {value[:50]}"}
        elif action == "clear_input":
            _xdo(["key", "--clearmodifiers", "ctrl+a"])
            time.sleep(0.05)
            _xdo(["key", "--clearmodifiers", "Delete"])
            return {"success": True, "message": "Cleared input (desktop)"}
        elif action == "select_option":
            return {"success": False, "message": "select_option not supported in desktop mode — use click"}
        # ── Browser-like navigation via keyboard shortcuts ─────────────
        elif action == "reload":
            _xdo(["key", "--clearmodifiers", "F5"])
            return {"success": True, "message": "Reloaded (F5)"}
        elif action == "go_back":
            _xdo(["key", "--clearmodifiers", "alt+Left"])
            return {"success": True, "message": "Navigated back (Alt+Left)"}
        elif action == "go_forward":
            _xdo(["key", "--clearmodifiers", "alt+Right"])
            return {"success": True, "message": "Navigated forward (Alt+Right)"}
        elif action == "new_tab":
            _xdo(["key", "--clearmodifiers", "ctrl+t"])
            time.sleep(0.5)
            if text:
                _xdo(["type", "--clearmodifiers", "--delay", "30", "--", text])
                _xdo(["key", "--clearmodifiers", "Return"])
            return {"success": True, "message": f"New tab (Ctrl+T){': ' + text[:50] if text else ''}"}
        elif action == "close_tab":
            _xdo(["key", "--clearmodifiers", "ctrl+w"])
            return {"success": True, "message": "Closed tab (Ctrl+W)"}
        elif action == "switch_tab":
            identifier = target or text or ""
            try:
                idx = int(identifier)
                # Ctrl+1..9 to switch by tab index
                if 1 <= idx <= 9:
                    _xdo(["key", "--clearmodifiers", f"ctrl+{idx}"])
                    return {"success": True, "message": f"Switched to tab {idx}"}
            except ValueError:
                pass
            _xdo(["key", "--clearmodifiers", "ctrl+Next"])
            return {"success": True, "message": "Switched to next tab (Ctrl+PageDown)"}
        # ── Scroll to (approximate via scroll) ────────────────────────
        elif action == "scroll_to":
            return {"success": False, "message": "scroll_to not supported in desktop mode — use scroll"}
        # ── DOM / Semantic (not available in desktop mode) ─────────────
        elif action == "get_text":
            return {"success": False, "message": "get_text not supported in desktop mode"}
        elif action == "find_element":
            return {"success": False, "message": "find_element not supported in desktop mode"}
        elif action == "evaluate_js":
            return {"success": False, "message": "evaluate_js not supported in desktop mode"}
        elif action == "wait_for":
            return _do_wait(3.0)  # Approximate: just wait a few seconds
        # ── Shell / Terminal ────────────────────────────────────────────
        elif action == "run_command":
            cmd = text or target
            if not cmd:
                return {"success": False, "message": "run_command requires text (shell command)"}
            try:
                args = shlex.split(cmd)
            except ValueError as e:
                return {"success": False, "message": f"Invalid command syntax: {e}"}
            if not args:
                return {"success": False, "message": "Empty command"}
            if args[0] not in _ALLOWED_COMMANDS:
                return {"success": False, "message": f"Command not allowed: {args[0]}. Permitted: {', '.join(sorted(_ALLOWED_COMMANDS))}"}
            try:
                result = subprocess.run(
                    args, shell=False, capture_output=True, text=True, timeout=30,
                    env={**os.environ, "DISPLAY": ":99"},
                )
                output = (result.stdout + result.stderr).strip()[:2000]
                return {"success": result.returncode == 0, "message": output or f"Command exited with code {result.returncode}"}
            except subprocess.TimeoutExpired:
                return {"success": False, "message": "Command timed out after 30s"}
        elif action == "open_terminal":
            return _open_terminal()
        # ── Vision ────────────────────────────────────────────────────
        elif action in ("screenshot", "screenshot_full"):
            b64 = _xdo_screenshot_full()
            return {"success": True, "message": "Full screenshot captured", "screenshot": b64}
        elif action == "screenshot_region":
            if len(coords) >= 4:
                b64 = _xdo_screenshot_region(coords[0], coords[1], coords[2], coords[3])
                return {"success": True, "message": "Region screenshot captured", "screenshot": b64}
            return {"success": False, "message": "screenshot_region needs 4 coords [x, y, width, height]"}
        else:
            return {"success": False, "message": f"Unsupported action '{action}' in desktop engine"}


def main():
    """Start the HTTP agent service and initialise the browser."""
    logger.info("Starting agent service on port %d (default_mode=%s)", SERVICE_PORT, DEFAULT_MODE)

    # Initialize Playwright browser (available even in desktop mode for fallback)
    try:
        _init_browser()
    except Exception as e:
        logger.warning("Playwright init failed (desktop mode still works): %s", e)

    server = HTTPServer(("0.0.0.0", SERVICE_PORT), AgentHandler)
    logger.info("Agent service listening on 0.0.0.0:%d", SERVICE_PORT)

    def _handle_signal(sig, frame):
        """Gracefully shut down the server on SIGTERM/SIGINT."""
        logger.info("Shutting down...")
        _shutdown_browser()
        server.shutdown()
        sys.exit(0)

    signal.signal(signal.SIGTERM, _handle_signal)
    signal.signal(signal.SIGINT, _handle_signal)

    try:
        server.serve_forever()
    except KeyboardInterrupt:
        pass
    finally:
        _shutdown_browser()
        server.server_close()


if __name__ == "__main__":
    main()
--- EXPLANATION ---
This is the source code for docker/agent_service.py.
Line 1: """Internal agent service — runs INSIDE the Docker container.
Line 3: Provides a lightweight HTTP API with two automation modes:
Line 5: • browser  — Playwright (page.mouse.click, page.keyboard.type, page.goto)
Line 6: • desktop  — xdotool + scrot (works with ANY X11 application)
Line 8: The backend selects the mode per-request via a `mode` field.  Screenshots
Line 9: and actions are dispatched to the appropriate handler automatically.
Line 10: """
Line 12: from __future__ import annotations
Line 14: import base64
Line 15: import json
Line 16: import logging
Line 17: import os
Line 18: import shlex
Line 19: import shutil
Line 20: import signal
Line 21: import subprocess
Line 22: import sys
Line 23: import time
Line 24: from http.server import HTTPServer, BaseHTTPRequestHandler
Line 25: from threading import Lock
Line 27: logging.basicConfig(
Line 28: level=logging.INFO,
Line 29: format='{"timestamp": "%(asctime)s", "level": "%(levelname)s", "message": "%(message)s"}',
Line 30: )
Line 31: logger = logging.getLogger("agent_service")
Line 33: try:
Line 34: from backend.tools.action_aliases import resolve_action
Line 35: except ImportError:
Line 36: # Fallback if backend not available (e.g. local dev outside docker)
Line 37: logger.warning("backend.tools not found, alias resolution disabled")
Line 39: def resolve_action(a):
Line 40: """Identity fallback when backend.tools is unavailable."""
Line 41: return a
Line 43: # ── Globals ───────────────────────────────────────────────────────────────────
Line 45: _playwright = None
Line 46: _browser = None
Line 47: _context = None
Line 48: _page = None
Line 49: _lock = Lock()
Line 51: SCREEN_WIDTH = int(os.environ.get("SCREEN_WIDTH", "1440"))
Line 52: SCREEN_HEIGHT = int(os.environ.get("SCREEN_HEIGHT", "900"))
Line 53: SERVICE_PORT = int(os.environ.get("AGENT_SERVICE_PORT", "9222"))
Line 54: DEFAULT_MODE = os.environ.get("AGENT_MODE", "browser")  # "browser" or "desktop"
Line 55: ACTION_DELAY = float(os.environ.get("ACTION_DELAY", "0.05"))
Line 58: def _env_bool(name: str, default: bool = True) -> bool:
Line 59: """Parse a boolean-like environment variable."""
Line 60: raw = os.environ.get(name)
Line 61: if raw is None:
Line 62: return default
Line 63: return raw.strip().lower() in {"1", "true", "yes", "on"}
Line 66: WINDOW_NORMALIZE_ENABLED = _env_bool("CUA_WINDOW_NORMALIZE", True)
Line 67: WINDOW_NORMALIZE_X = int(os.environ.get("CUA_WINDOW_X", "100"))
Line 68: WINDOW_NORMALIZE_Y = int(os.environ.get("CUA_WINDOW_Y", "80"))
Line 69: WINDOW_NORMALIZE_W = int(os.environ.get("CUA_WINDOW_W", "560"))
Line 70: WINDOW_NORMALIZE_H = int(os.environ.get("CUA_WINDOW_H", "760"))
Line 72: # ── Security constants ────────────────────────────────────────────────────────
Line 74: _MAX_BODY_SIZE = 1_000_000  # 1 MB request body limit
Line 76: # Dangerous shell patterns blocked in run_command (defense-in-depth)
Line 77: _BLOCKED_CMD_PATTERNS = (
Line 78: "rm -rf /",
Line 79: "rm -rf /*",
Line 80: "mkfs.",
Line 81: "dd if=/dev/",
Line 82: ":(){",
Line 83: "shutdown",
Line 84: "reboot",
Line 85: "halt ",
Line 86: "poweroff",
Line 87: "chmod -R 777 /",
Line 88: "> /dev/sda",
Line 89: "mv /* ",
Line 90: "mv / ",
Line 91: )
Line 93: # Allowed directories for file upload operations
Line 94: _UPLOAD_ALLOWED_PREFIXES = ("/tmp", "/app", "/home")
Line 96: # Strict allowlist of commands permitted in run_command
Line 97: _ALLOWED_COMMANDS = frozenset({
Line 98: "ls", "cat", "head", "tail", "grep", "find", "wc", "echo",
Line 99: "pwd", "whoami", "id", "date", "env", "printenv",
Line 100: "which", "file", "stat", "df", "du", "free",
Line 101: "uname", "hostname", "uptime",
Line 102: "python3", "python", "pip", "pip3", "node", "npm", "npx",
Line 103: "curl", "wget",
Line 104: "xdg-open", "xdotool", "xclip", "scrot", "wmctrl",
Line 105: "xfce4-terminal", "xterm",
Line 106: # Desktop apps accessible via accessibility / run_command
Line 107: "gnome-control-center", "gnome-settings", "gnome-calculator",
Line 108: "gnome-text-editor", "gedit", "gnome-system-monitor",
Line 109: "xfce4-settings-manager", "xfce4-settings-editor",
Line 110: "xfce4-taskmanager", "thunar", "mousepad",
Line 111: "firefox", "google-chrome",
Line 112: # Browsers added via Dockerfile
Line 113: "brave-browser", "microsoft-edge", "microsoft-edge-stable",
Line 114: # Desktop apps added via Dockerfile
Line 115: "vlc", "libreoffice", "soffice",
Line 116: "evince", "gnome-terminal", "flameshot", "xournalpp",
Line 117: "htop",
Line 118: })
Line 120: # Ensure DISPLAY is set for all subprocesses (Critical Desktop Fix)
Line 121: os.environ["DISPLAY"] = ":99"
Line 125: # ── Playwright lifecycle ──────────────────────────────────────────────────────
Line 127: def _init_browser():
Line 128: """Launch Playwright + Chromium synchronously."""
Line 129: global _playwright, _browser, _context, _page
Line 131: try:
Line 132: subprocess.run(["xclip", "-version"], stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
Line 133: except FileNotFoundError:
Line 134: logger.warning("xclip not found - clipboard actions (paste/copy) in desktop mode may fail")
Line 136: from playwright.sync_api import sync_playwright
Line 138: logger.info("Initializing Playwright...")
Line 139: _playwright = sync_playwright().start()
Line 141: _browser = _playwright.chromium.launch(
Line 142: headless=False,  # Visible on Xvfb
Line 143: args=[
Line 144: "--no-sandbox",
Line 145: "--disable-dev-shm-usage",
Line 146: "--disable-gpu",
Line 147: f"--window-size={SCREEN_WIDTH - 100},{SCREEN_HEIGHT - 80}",
Line 148: "--window-position=50,10",
Line 149: "--disable-extensions",
Line 150: "--disable-background-timer-throttling",
Line 151: "--disable-backgrounding-occluded-windows",
Line 152: "--disable-renderer-backgrounding",
Line 153: "--force-device-scale-factor=1",
Line 154: "--remote-debugging-port=9223",
Line 155: ],
Line 156: )
Line 158: _context = _browser.new_context(
Line 159: viewport={"width": SCREEN_WIDTH - 100, "height": SCREEN_HEIGHT - 80},
Line 160: device_scale_factor=1,
Line 161: )
Line 163: _page = _context.new_page()
Line 164: _page.goto("about:blank")
Line 166: logger.info("Playwright browser ready: %dx%d", SCREEN_WIDTH, SCREEN_HEIGHT)
Line 169: def _get_page():
Line 170: """Return the active page, creating if needed."""
Line 171: global _page
Line 172: if _page is None or _page.is_closed():
Line 173: if _context:
Line 174: _page = _context.new_page()
Line 175: _page.goto("about:blank")
Line 176: return _page
Line 179: def _shutdown_browser():
Line 180: """Tear down Playwright browser, context, and page."""
Line 181: global _playwright, _browser, _context, _page
Line 182: try:
Line 183: if _page and not _page.is_closed():
Line 184: _page.close()
Line 185: if _context:
Line 186: _context.close()
Line 187: if _browser:
Line 188: _browser.close()
Line 189: if _playwright:
Line 190: _playwright.stop()
Line 191: except Exception as e:
Line 192: logger.warning("Shutdown error: %s", e)
Line 195: # ══════════════════════════════════════════════════════════════════════════════
Line 196: #  Screenshots
Line 197: # ══════════════════════════════════════════════════════════════════════════════
Line 199: def _screenshot_playwright() -> str:
Line 200: """Capture current browser tab as PNG via Playwright."""
Line 201: page = _get_page()
Line 202: png_bytes = page.screenshot(type="png", full_page=False)
Line 203: return base64.b64encode(png_bytes).decode("ascii")
Line 206: def _screenshot_desktop() -> str:
Line 207: """Capture the full Xvfb display via scrot (works with any app)."""
Line 208: subprocess.run(
Line 209: ["scrot", "-z", "-o", "/tmp/screenshot.png"],
Line 210: check=True, timeout=5,
Line 211: )
Line 212: with open("/tmp/screenshot.png", "rb") as f:
Line 213: return base64.b64encode(f.read()).decode("ascii")
Line 216: # ══════════════════════════════════════════════════════════════════════════════
Line 217: #  Actions — Playwright (browser mode)
Line 218: # ══════════════════════════════════════════════════════════════════════════════
Line 220: def _pw_click(x: int, y: int) -> dict:
Line 221: """Click at pixel coordinates via Playwright."""
Line 222: page = _get_page()
Line 223: page.mouse.click(x, y)
Line 224: return {"success": True, "message": f"Clicked at ({x}, {y})"}
Line 227: def _pw_double_click(x: int, y: int) -> dict:
Line 228: """Double-click at pixel coordinates via Playwright."""
Line 229: page = _get_page()
Line 230: page.mouse.dblclick(x, y)
Line 231: return {"success": True, "message": f"Double-clicked at ({x}, {y})"}
Line 234: def _pw_right_click(x: int, y: int) -> dict:
Line 235: """Right-click at pixel coordinates via Playwright."""
Line 236: page = _get_page()
Line 237: page.mouse.click(x, y, button="right")
Line 238: return {"success": True, "message": f"Right-clicked at ({x}, {y})"}
Line 241: def _pw_type(text: str, coords: list = None, selector: str = None) -> dict:
Line 242: """Type text, ensuring focus if coordinates or selector are provided."""
Line 243: page = _get_page()
Line 245: # 1. Try to click coordinates or selector if provided to ensure focus
Line 246: if selector:
Line 247: try:
Line 248: page.click(selector)
Line 249: time.sleep(0.1)
Line 250: except Exception as e:
Line 251: logger.warning(f"Safe type click selector failed: {e}")
Line 252: elif coords and len(coords) >= 2:
Line 253: try:
Line 254: page.mouse.click(coords[0], coords[1])
Line 255: time.sleep(0.1)
Line 256: except Exception as e:
Line 257: logger.warning(f"Safe type click coords failed: {e}")
Line 259: # 2. Verify focus (optional but good for debugging)
Line 260: try:
Line 261: active_tag = page.evaluate("document.activeElement ? document.activeElement.tagName : ''")
Line 262: if active_tag == 'BODY':
Line 263: logger.warning("Typing with focus on BODY - input might fail")
Line 264: except Exception:
Line 265: pass
Line 267: # 3. Type directly (retry/fallback handled by executor)
Line 268: page.keyboard.type(text, delay=50)
Line 269: time.sleep(ACTION_DELAY)
Line 270: return {"success": True, "message": f"Typed: {text[:50]}", "error_type": None}
Line 273: def _pw_scroll(x: int, y: int, direction: str) -> dict:
Line 274: """Scroll at the given coordinates in the specified direction."""
Line 275: page = _get_page()
Line 276: page.mouse.move(x, y)
Line 277: delta = -300 if direction == "up" else 300
Line 278: page.mouse.wheel(0, delta)
Line 279: return {"success": True, "message": f"Scrolled {direction} at ({x}, {y})"}
Line 282: def _pw_navigate(url: str) -> dict:
Line 283: """Navigate the browser to *url*, prepending https:// if needed."""
Line 284: page = _get_page()
Line 285: if not url.startswith(("http://", "https://")):
Line 286: url = "https://" + url
Line 287: page.goto(url, wait_until="domcontentloaded", timeout=30000)
Line 288: return {"success": True, "message": f"Navigated to {url}"}
Line 291: def _pw_key(key: str) -> dict:
Line 292: """Press a keyboard key or combo via Playwright."""
Line 293: page = _get_page()
Line 294: combo = _map_key_combo(key)
Line 295: page.keyboard.press(combo)
Line 296: return {"success": True, "message": f"Pressed key: {key}"}
Line 299: def _pw_drag(x1: int, y1: int, x2: int, y2: int) -> dict:
Line 300: """Drag from (x1,y1) to (x2,y2) via Playwright mouse."""
Line 301: page = _get_page()
Line 302: page.mouse.move(x1, y1)
Line 303: page.mouse.down()
Line 304: page.mouse.move(x2, y2, steps=10)
Line 305: page.mouse.up()
Line 306: return {"success": True, "message": f"Dragged ({x1},{y1}) → ({x2},{y2})"}
Line 309: def _pw_hover(x: int, y: int) -> dict:
Line 310: """Move the mouse to (x,y) without clicking."""
Line 311: page = _get_page()
Line 312: page.mouse.move(x, y)
Line 313: return {"success": True, "message": f"Hovered at ({x}, {y})"}
Line 316: def _pw_middle_click(x: int, y: int) -> dict:
Line 317: """Middle-click at pixel coordinates via Playwright."""
Line 318: page = _get_page()
Line 319: page.mouse.click(x, y, button="middle")
Line 320: return {"success": True, "message": f"Middle-clicked at ({x}, {y})"}
Line 323: def _pw_fill(selector: str, text: str) -> dict:
Line 324: """Fill an input using Playwright's reliable fill() — clears first.
Line 326: On selector failure, auto-discovers available form fields and returns
Line 327: them in the error message so the model can retry with the correct selector.
Line 328: """
Line 329: page = _get_page()
Line 330: try:
Line 331: page.fill(selector, text, timeout=5000)
Line 332: return {"success": True, "message": f"Filled '{selector}' with: {text[:50]}"}
Line 333: except Exception as fill_err:
Line 334: # Auto-discover available form fields so the model can self-correct
Line 335: try:
Line 336: fields_json = page.evaluate(
Line 337: "[...document.querySelectorAll('input,textarea,select')]"
Line 338: ".map(e=>({tag:e.tagName,name:e.name,id:e.id,type:e.type,"
Line 339: "placeholder:e.placeholder||''}))"
Line 340: )
Line 341: field_hints = json.dumps(fields_json, separators=(",", ":"))
Line 342: if len(field_hints) > 1500:
Line 343: field_hints = field_hints[:1500] + "…]"
Line 344: except Exception:
Line 345: field_hints = "(could not discover fields)"
Line 346: return {
Line 347: "success": False,
Line 348: "message": (
Line 349: f"fill('{selector}') failed: {fill_err}. "
Line 350: f"Available fields: {field_hints}"
Line 351: ),
Line 352: }
Line 355: def _pw_hotkey(keys: list[str]) -> dict:
Line 356: """Press multiple keys simultaneously (e.g. ['ctrl','shift','t'])."""
Line 357: page = _get_page()
Line 358: combo = "+".join(_map_key_combo(k) for k in keys)
Line 359: page.keyboard.press(combo)
Line 360: return {"success": True, "message": f"Hotkey: {'+'.join(keys)}"}
Line 363: def _pw_clear_input(selector: str) -> dict:
Line 364: """Clear an input field by filling it with an empty string."""
Line 365: page = _get_page()
Line 366: page.locator(selector).fill("")
Line 367: return {"success": True, "message": f"Cleared input: {selector}"}
Line 370: def _pw_select_option(selector: str, value: str) -> dict:
Line 371: """Select a <select> option by value via Playwright."""
Line 372: page = _get_page()
Line 373: page.select_option(selector, value, timeout=5000)
Line 374: return {"success": True, "message": f"Selected '{value}' in {selector}"}
Line 377: def _pw_paste(text: str) -> dict:
Line 378: """Clipboard-based paste (reliable fallback for typing)."""
Line 379: page = _get_page()
Line 380: page.evaluate(f"navigator.clipboard.writeText({json.dumps(text)})")
Line 381: page.keyboard.press("Control+v")
Line 382: time.sleep(0.1)
Line 383: return {"success": True, "message": f"Pasted: {text[:50]}"}
Line 386: def _pw_copy() -> dict:
Line 387: """Copy the current selection to clipboard via Ctrl+C."""
Line 388: page = _get_page()
Line 389: page.keyboard.press("Control+c")
Line 390: return {"success": True, "message": "Copied selection to clipboard"}
Line 393: def _pw_reload() -> dict:
Line 394: """Reload the current page."""
Line 395: page = _get_page()
Line 396: page.reload(wait_until="domcontentloaded", timeout=30000)
Line 397: return {"success": True, "message": "Page reloaded"}
Line 400: def _pw_go_back() -> dict:
Line 401: """Navigate back one page."""
Line 402: page = _get_page()
Line 403: page.go_back(wait_until="domcontentloaded", timeout=30000)
Line 404: return {"success": True, "message": "Navigated back"}
Line 407: def _pw_go_forward() -> dict:
Line 408: """Navigate forward one page."""
Line 409: page = _get_page()
Line 410: page.go_forward(wait_until="domcontentloaded", timeout=30000)
Line 411: return {"success": True, "message": "Navigated forward"}
Line 414: def _pw_new_tab(url: str = "") -> dict:
Line 415: """Open a new browser tab, optionally navigating to *url*."""
Line 416: global _page
Line 417: target = url or "about:blank"
Line 418: if url and not url.startswith(("http://", "https://")):
Line 419: target = "https://" + url
Line 420: page = _context.new_page()
Line 421: page.goto(target)
Line 422: _page = page
Line 423: return {"success": True, "message": f"Opened new tab: {target}"}
Line 426: def _pw_close_tab() -> dict:
Line 427: """Close the currently active browser tab."""
Line 428: global _page
Line 429: page = _get_page()
Line 430: page.close()
Line 431: pages = _context.pages
Line 432: _page = pages[-1] if pages else None
Line 433: return {"success": True, "message": "Closed current tab"}
Line 436: def _pw_switch_tab(identifier: str) -> dict:
Line 437: """Switch tab by index (0-based) or partial title/URL match."""
Line 438: global _page
Line 439: pages = _context.pages
Line 440: # Try numeric index first
Line 441: try:
Line 442: idx = int(identifier)
Line 443: if 0 <= idx < len(pages):
Line 444: _page = pages[idx]
Line 445: _page.bring_to_front()
Line 446: return {"success": True, "message": f"Switched to tab {idx}: {_page.url}"}
Line 447: except ValueError:
Line 448: pass
Line 449: # Search by title or URL
Line 450: needle = identifier.lower()
Line 451: for p in pages:
Line 452: if needle in p.title().lower() or needle in p.url.lower():
Line 453: _page = p
Line 454: _page.bring_to_front()
Line 455: return {"success": True, "message": f"Switched to tab: {_page.url}"}
Line 456: return {"success": False, "message": f"No tab matching '{identifier}'"}
Line 459: def _pw_scroll_to(selector: str) -> dict:
Line 460: """Scroll the element matching *selector* into view."""
Line 461: page = _get_page()
Line 462: page.evaluate(f"document.querySelector({json.dumps(selector)})?.scrollIntoView({{behavior:'smooth',block:'center'}})")
Line 463: return {"success": True, "message": f"Scrolled to: {selector}"}
Line 466: def _pw_get_text(selector: str) -> dict:
Line 467: """Extract text content from the element matching *selector*."""
Line 468: page = _get_page()
Line 469: text = page.text_content(selector, timeout=5000) or ""
Line 470: return {"success": True, "message": f"Text: {text[:500]}"}
Line 473: def _pw_find_element(description: str) -> dict:
Line 474: """Find element by text content and return its bounding box."""
Line 475: page = _get_page()
Line 476: loc = page.get_by_text(description, exact=False).first
Line 477: box = loc.bounding_box(timeout=5000)
Line 478: if box:
Line 479: cx = int(box["x"] + box["width"] / 2)
Line 480: cy = int(box["y"] + box["height"] / 2)
Line 481: return {"success": True, "message": f"Found '{description}' at center ({cx}, {cy}), box={box}"}
Line 482: return {"success": False, "message": f"Element '{description}' not found"}
Line 485: def _pw_evaluate_js(script: str) -> dict:
Line 486: """Evaluate arbitrary JavaScript in the page context."""
Line 487: page = _get_page()
Line 488: result = page.evaluate(script)
Line 489: return {"success": True, "message": f"JS result: {str(result)[:500]}"}
Line 492: def _pw_wait_for(selector: str) -> dict:
Line 493: """Wait up to 10 s for a DOM element matching *selector* to appear."""
Line 494: page = _get_page()
Line 495: page.wait_for_selector(selector, timeout=10000)
Line 496: return {"success": True, "message": f"Element appeared: {selector}"}
Line 499: def _pw_screenshot_region(x: int, y: int, w: int, h: int) -> str:
Line 500: """Capture a region of the page as base64 PNG."""
Line 501: page = _get_page()
Line 502: png_bytes = page.screenshot(
Line 503: type="png",
Line 504: clip={"x": x, "y": y, "width": w, "height": h},
Line 505: )
Line 506: return base64.b64encode(png_bytes).decode("ascii")
Line 509: def _pw_screenshot_viewport() -> str:
Line 510: """Capture the current viewport as base64 PNG."""
Line 511: page = _get_page()
Line 512: png_bytes = page.screenshot(type="png", full_page=False)
Line 513: return base64.b64encode(png_bytes).decode("ascii")
Line 516: def _pw_screenshot_element(selector: str) -> str:
Line 517: """Capture a specific element as base64 PNG."""
Line 518: page = _get_page()
Line 519: try:
Line 520: element = page.locator(selector).first
Line 521: if element.is_visible():
Line 522: png_bytes = element.screenshot(type="png")
Line 523: return base64.b64encode(png_bytes).decode("ascii")
Line 524: except Exception as e:
Line 525: logger.warning(f"Failed to screenshot element {selector}: {e}")
Line 526: return ""  # Return empty string on failure, handled by caller
Line 529: def _pw_scroll_into_view(selector: str) -> dict:
Line 530: """Scroll the element into the visible viewport if needed."""
Line 531: page = _get_page()
Line 532: try:
Line 533: page.locator(selector).first.scroll_into_view_if_needed(timeout=5000)
Line 534: return {"success": True, "message": f"Scrolled into view: {selector}"}
Line 535: except Exception as e:
Line 536: return {"success": False, "message": f"Failed to scroll to {selector}: {e}"}
Line 539: def _pw_set_viewport(width: int, height: int) -> dict:
Line 540: """Resize the Playwright viewport to *width* x *height*."""
Line 541: page = _get_page()
Line 542: page.set_viewport_size({"width": width, "height": height})
Line 543: return {"success": True, "message": f"Viewport set to {width}x{height}"}
Line 546: def _pw_zoom(level: float) -> dict:
Line 547: """Set zoom level (e.g. 1.0, 1.5, 0.8). Playwright uses CSS zoom or scale."""
Line 548: page = _get_page()
Line 549: page.evaluate(f"document.body.style.zoom = '{level}'")
Line 550: return {"success": True, "message": f"Zoom set to {level}"}
Line 553: def _pw_block_resource(resource_type: str) -> dict:
Line 554: """Block resource types (image, font, css, etc.)."""
Line 555: page = _get_page()
Line 556: normalized = (resource_type or "").strip().lower()
Line 557: if not normalized:
Line 558: return {"success": False, "message": "block_resource requires a resource type"}
Line 560: page.route(
Line 561: "**/*",
Line 562: lambda route: route.abort()
Line 563: if route.request.resource_type == normalized
Line 564: else route.continue_(),
Line 565: )
Line 566: return {"success": True, "message": f"Blocked resource type: {resource_type}"}
Line 569: def _pw_export_page_pdf() -> str:
Line 570: """Export page as PDF (base64)."""
Line 571: page = _get_page()
Line 572: pdf_bytes = page.pdf()
Line 573: return base64.b64encode(pdf_bytes).decode("ascii")
Line 576: def _pw_get_html(selector: str = None) -> dict:
Line 577: """Return inner HTML of *selector*, or full page HTML if omitted."""
Line 578: page = _get_page()
Line 579: if selector:
Line 580: html = page.inner_html(selector, timeout=5000)
Line 581: else:
Line 582: html = page.content()
Line 583: return {"success": True, "message": f"HTML: {html[:500]}"}
Line 586: def _pw_get_attribute(selector: str, attribute: str) -> dict:
Line 587: """Read a DOM attribute from the element matching *selector*."""
Line 588: page = _get_page()
Line 589: val = page.get_attribute(selector, attribute, timeout=5000)
Line 590: return {"success": True, "message": f"Attribute {attribute}: {val}"}
Line 593: def _pw_query_selector(selector: str) -> dict:
Line 594: """Check whether a DOM element matching *selector* exists."""
Line 595: page = _get_page()
Line 596: element = page.query_selector(selector)
Line 597: if element:
Line 598: return {"success": True, "message": f"Element found: {selector}"}
Line 599: return {"success": False, "message": f"Element not found: {selector}"}
Line 602: def _pw_query_all(selector: str) -> dict:
Line 603: """Count all DOM elements matching *selector*."""
Line 604: page = _get_page()
Line 605: elements = page.query_selector_all(selector)
Line 606: return {"success": True, "message": f"Found {len(elements)} elements matching: {selector}"}
Line 609: def _pw_get_bounding_box(selector: str) -> dict:
Line 610: """Return the bounding box of the first element matching *selector*."""
Line 611: page = _get_page()
Line 612: element = page.query_selector(selector)
Line 613: if element:
Line 614: box = element.bounding_box()
Line 615: return {"success": True, "message": f"Bounding box for {selector}: {box}"}
Line 616: return {"success": False, "message": f"Element not found: {selector}"}
Line 619: def _pw_get_visible_elements(selector: str) -> dict:
Line 620: """Count visible elements matching *selector*."""
Line 621: page = _get_page()
Line 622: elements = page.query_selector_all(selector)
Line 623: visible_count = sum(1 for el in elements if el.is_visible())
Line 624: return {"success": True, "message": f"Found {visible_count} visible elements matching: {selector}"}
Line 627: def _pw_evaluate_on_selector(selector: str, script: str) -> dict:
Line 628: """Evaluate *script* in the context of the element matching *selector*."""
Line 629: page = _get_page()
Line 630: try:
Line 631: result = page.eval_on_selector(selector, script)
Line 632: return {"success": True, "message": f"JS result on {selector}: {str(result)[:500]}"}
Line 633: except Exception as e:
Line 634: return {"success": False, "message": f"Failed to evaluate on {selector}: {e}"}
Line 637: def _pw_wait_for_navigation() -> dict:
Line 638: """Block until the page reaches the 'load' state."""
Line 639: page = _get_page()
Line 640: page.wait_for_load_state("load", timeout=30000)
Line 641: return {"success": True, "message": "Navigation complete"}
Line 644: def _pw_upload_file(selector: str, file_path: str) -> dict:
Line 645: """Set input file on the element matching *selector* (path-restricted)."""
Line 646: page = _get_page()
Line 647: # Directory traversal protection
Line 648: resolved = os.path.realpath(file_path)
Line 649: if not any(resolved.startswith(p) for p in _UPLOAD_ALLOWED_PREFIXES):
Line 650: return {"success": False, "message": f"Upload restricted to {_UPLOAD_ALLOWED_PREFIXES}"}
Line 651: page.set_input_files(selector, file_path)
Line 652: return {"success": True, "message": f"Uploaded {file_path} to {selector}"}
Line 655: def _pw_download_file(selector: str) -> dict:
Line 656: """Download a file (stub — not yet implemented)."""
Line 657: return {"success": False, "message": "Not implemented yet"}
Line 660: def _pw_set_cookies(cookies: list) -> dict:
Line 661: """Add cookies to the current browser context."""
Line 662: global _context
Line 663: _context.add_cookies(cookies)
Line 664: return {"success": True, "message": "Cookies set"}
Line 667: def _pw_get_cookies() -> dict:
Line 668: """Retrieve all cookies from the current browser context."""
Line 669: global _context
Line 670: cookies = _context.cookies()
Line 671: return {"success": True, "message": f"Got {len(cookies)} cookies"}
Line 674: def _pw_clear_cookies() -> dict:
Line 675: """Clear all cookies from the current browser context."""
Line 676: global _context
Line 677: _context.clear_cookies()
Line 678: return {"success": True, "message": "Cookies cleared"}
Line 681: def _pw_new_context() -> dict:
Line 682: """Create a fresh Playwright browser context (clears state)."""
Line 683: global _playwright, _browser, _context, _page
Line 684: if _context:
Line 685: _context.close()
Line 686: _context = _browser.new_context(
Line 687: viewport={"width": SCREEN_WIDTH - 100, "height": SCREEN_HEIGHT - 80},
Line 688: device_scale_factor=1,
Line 689: )
Line 690: _page = _context.new_page()
Line 691: _page.goto("about:blank")
Line 692: return {"success": True, "message": "New context created"}
Line 695: def _pw_switch_context(identifier: str) -> dict:
Line 696: """Switch browser context (stub — not yet implemented)."""
Line 697: return {"success": False, "message": "Not implemented yet"}
Line 700: def _pw_close_context() -> dict:
Line 701: """Close the current browser context and release its page."""
Line 702: global _context, _page
Line 703: if _context:
Line 704: _context.close()
Line 705: _context = None
Line 706: _page = None
Line 707: return {"success": True, "message": "Context closed"}
Line 710: # ══════════════════════════════════════════════════════════════════════════════
Line 711: #  Actions — xdotool (desktop mode, works with any X11 app)
Line 712: # ══════════════════════════════════════════════════════════════════════════════
Line 714: def _xdo(args: list[str]) -> str:
Line 715: """Run an xdotool command, return stdout.
Line 717: The ``--sync`` flag is automatically stripped because it hangs
Line 718: indefinitely in Xvfb environments that lack a compositor (the X
Line 719: event that ``--sync`` waits for is never delivered).  A small
Line 720: ``time.sleep`` replaces it so callers stay unchanged.
Line 721: """
Line 722: had_sync = "--sync" in args
Line 723: if had_sync:
Line 724: args = [a for a in args if a != "--sync"]
Line 726: result = subprocess.run(
Line 727: ["xdotool"] + args,
Line 728: capture_output=True, text=True, timeout=10,
Line 729: )
Line 730: if result.returncode != 0:
Line 731: raise RuntimeError(f"xdotool {' '.join(args)} failed: {result.stderr.strip()}")
Line 733: # Compensate for removed --sync with a short delay
Line 734: if had_sync:
Line 735: cmd = args[0] if args else ""
Line 736: if cmd == "windowactivate":
Line 737: time.sleep(0.3)   # window activation needs more time
Line 738: else:
Line 739: time.sleep(0.05)  # mousemove / other commands
Line 741: return result.stdout.strip()
Line 744: def _xdo_search_window_ids(identifier: str) -> list[str]:
Line 745: """Return xdotool window IDs matching *identifier* by name."""
Line 746: if not identifier:
Line 747: return []
Line 748: result = subprocess.run(
Line 749: ["xdotool", "search", "--name", identifier],
Line 750: capture_output=True,
Line 751: text=True,
Line 752: timeout=5,
Line 753: )
Line 754: if result.returncode != 0:
Line 755: return []
Line 756: return [wid.strip() for wid in result.stdout.splitlines() if wid.strip()]
Line 759: def _xdo_get_window_geometry(wid: str) -> dict:
Line 760: """Get window geometry via xdotool getwindowgeometry --shell."""
Line 761: raw = _xdo(["getwindowgeometry", "--shell", wid])
Line 762: geometry: dict[str, int] = {}
Line 763: for line in raw.splitlines():
Line 764: if "=" not in line:
Line 765: continue
Line 766: key, value = line.split("=", 1)
Line 767: if key in {"X", "Y", "WIDTH", "HEIGHT"}:
Line 768: try:
Line 769: geometry[key] = int(value.strip())
Line 770: except ValueError:
Line 771: continue
Line 772: return geometry
Line 775: def _xdo_normalize_window(wid: str) -> str:
Line 776: """Move/resize window to deterministic geometry for stable coordinates."""
Line 777: if not WINDOW_NORMALIZE_ENABLED:
Line 778: return "window normalization disabled"
Line 779: try:
Line 780: subprocess.run(
Line 781: ["wmctrl", "-ir", wid, "-b", "remove,maximized_vert,maximized_horz"],
Line 782: check=False,
Line 783: capture_output=True,
Line 784: text=True,
Line 785: timeout=5,
Line 786: )
Line 787: _xdo(["windowmove", wid, str(WINDOW_NORMALIZE_X), str(WINDOW_NORMALIZE_Y)])
Line 788: _xdo(["windowsize", wid, str(WINDOW_NORMALIZE_W), str(WINDOW_NORMALIZE_H)])
Line 789: geo = _xdo_get_window_geometry(wid)
Line 790: if geo:
Line 791: return (
Line 792: f"normalized to x={geo.get('X')}, y={geo.get('Y')}, "
Line 793: f"w={geo.get('WIDTH')}, h={geo.get('HEIGHT')}"
Line 794: )
Line 795: return "normalized"
Line 796: except Exception as e:
Line 797: return f"normalization skipped: {e}"
Line 800: def _expand_app_launch_candidates(app_name: str) -> list[str]:
Line 801: """Expand semantic app names into concrete launch candidates."""
Line 802: requested = (app_name or "").strip()
Line 803: if not requested:
Line 804: return []
Line 806: lowered = requested.lower()
Line 807: candidates: list[str] = [requested]
Line 809: if any(term in lowered for term in ("calculator", "calc", "xcalc", "kcalc", "galculator")):
Line 810: candidates.extend([
Line 811: "gnome-calculator",
Line 812: "galculator",
Line 813: "xfce4-calculator",
Line 814: "mate-calc",
Line 815: "kcalc",
Line 816: "xcalc",
Line 817: ])
Line 819: if any(term in lowered for term in ("file explorer", "files", "file manager", "explorer", "nautilus", "thunar", "pcmanfm")):
Line 820: candidates.extend([
Line 821: "nautilus",
Line 822: "thunar",
Line 823: "pcmanfm",
Line 824: "dolphin",
Line 825: "nemo",
Line 826: "xfe",
Line 827: ])
Line 829: # Preserve order while deduping
Line 830: seen = set()
Line 831: deduped: list[str] = []
Line 832: for item in candidates:
Line 833: key = item.strip().lower()
Line 834: if key and key not in seen:
Line 835: deduped.append(item.strip())
Line 836: seen.add(key)
Line 837: return deduped
Line 840: def _xdo_click(x: int, y: int) -> dict:
Line 841: """Click at (x,y) via xdotool."""
Line 842: _xdo(["mousemove", "--sync", str(x), str(y)])
Line 843: _xdo(["click", "1"])
Line 844: return {"success": True, "message": f"Clicked at ({x}, {y})"}
Line 847: def _xdo_double_click(x: int, y: int) -> dict:
Line 848: """Double-click at (x,y) via xdotool."""
Line 849: _xdo(["mousemove", "--sync", str(x), str(y)])
Line 850: _xdo(["click", "--repeat", "2", "--delay", "80", "1"])
Line 851: return {"success": True, "message": f"Double-clicked at ({x}, {y})"}
Line 854: def _xdo_right_click(x: int, y: int) -> dict:
Line 855: """Right-click at (x,y) via xdotool."""
Line 856: _xdo(["mousemove", "--sync", str(x), str(y)])
Line 857: _xdo(["click", "3"])
Line 858: return {"success": True, "message": f"Right-clicked at ({x}, {y})"}
Line 861: def _xdo_type(text: str) -> dict:
Line 862: """Type text via xdotool with modifier-key safety.
Line 864: Strategy:
Line 865: 1. Try ``xdotool type`` (sends KeyPress/KeyRelease per character).
Line 866: 2. If the focused window is an Athena-widget app (e.g. xcalc) that
Line 867: ignores synthetic type events, fall back to sending individual
Line 868: ``xdotool key`` events per character which uses keysym dispatch
Line 869: and works more reliably with legacy X11 toolkit widgets.
Line 870: """
Line 871: # 1. Ensure window focus
Line 872: wid = ""
Line 873: try:
Line 874: wid = _xdo(["getwindowfocus"]).strip()
Line 875: if wid:
Line 876: _xdo(["windowactivate", "--sync", wid])
Line 877: except Exception:
Line 878: pass
Line 880: # 2. Clear potentially stuck modifier keys
Line 881: try:
Line 882: _xdo(["keyup", "shift"])
Line 883: _xdo(["keyup", "ctrl"])
Line 884: _xdo(["keyup", "alt"])
Line 885: except Exception:
Line 886: pass
Line 887: time.sleep(ACTION_DELAY)
Line 889: # 3. Try normal xdotool type first
Line 890: try:
Line 891: _xdo(["type", "--clearmodifiers", "--delay", "25", "--", text])
Line 892: except Exception as exc:
Line 893: logger.warning("xdotool type failed (%s), falling back to key-per-char", exc)
Line 894: _xdo_type_key_per_char(text)
Line 895: return {"success": True, "message": f"Typed (key-per-char fallback): {text[:50]}"}
Line 897: # 4. Post-type verification: send key-per-char as reinforcement for
Line 898: #    Athena/Xaw widget apps (xcalc, xedit, etc.) which silently
Line 899: #    ignore xdotool-type events.  This is cheap and idempotent for
Line 900: #    apps that already accepted the type events (the duplicate input
Line 901: #    can be cleared by the agent if needed).
Line 902: #    We only do this when the text is short (≤40 chars) to avoid
Line 903: #    doubling long pastes.
Line 904: if len(text) <= 40:
Line 905: try:
Line 906: win_name = _xdo(["getwindowfocus", "getwindowname"]).strip().lower()
Line 907: except Exception:
Line 908: win_name = ""
Line 909: # Heuristic: Athena-widget apps typically have generic titles
Line 910: _ATHENA_HINTS = ("xcalc", "calculator", "xedit", "bitmap", "editres")
Line 911: if any(h in win_name for h in _ATHENA_HINTS):
Line 912: logger.info("Athena-widget window detected ('%s') — reinforcing with key-per-char", win_name)
Line 913: _xdo_type_key_per_char(text)
Line 915: return {"success": True, "message": f"Typed: {text[:50]}"}
Line 918: # Character-to-xdotool-keysym map for key-per-char fallback
Line 919: _CHAR_KEYSYM: dict[str, str] = {
Line 920: " ": "space", "!": "exclam", '"': "quotedbl", "#": "numbersign",
Line 921: "$": "dollar", "%": "percent", "&": "ampersand", "'": "apostrophe",
Line 922: "(": "parenleft", ")": "parenright", "*": "asterisk", "+": "plus",
Line 923: ",": "comma", "-": "minus", ".": "period", "/": "slash",
Line 924: ":": "colon", ";": "semicolon", "<": "less", "=": "equal",
Line 925: ">": "greater", "?": "question", "@": "at", "[": "bracketleft",
Line 926: "\\": "backslash", "]": "bracketright", "^": "asciicircum",
Line 927: "_": "underscore", "`": "grave", "{": "braceleft", "|": "bar",
Line 928: "}": "braceright", "~": "asciitilde",
Line 929: "\n": "Return", "\t": "Tab",
Line 930: }
Line 933: def _xdo_type_key_per_char(text: str) -> None:
Line 934: """Send *text* one character at a time via ``xdotool key``.
Line 936: This bypasses the ``xdotool type`` path which relies on XStringToKeysym
Line 937: translation that some legacy Athena/Xaw widgets silently ignore.
Line 938: """
Line 939: for ch in text:
Line 940: if ch in _CHAR_KEYSYM:
Line 941: keysym = _CHAR_KEYSYM[ch]
Line 942: elif ch.isalpha():
Line 943: # xdotool key accepts lowercase letter names directly
Line 944: keysym = ch.lower()
Line 945: elif ch.isdigit():
Line 946: keysym = ch  # xdotool handles "0".."9" directly
Line 947: else:
Line 948: keysym = ch  # last resort — pass through
Line 949: try:
Line 950: _xdo(["key", "--clearmodifiers", keysym])
Line 951: except Exception:
Line 952: logger.warning("key-per-char: failed to send keysym '%s' for char '%s'", keysym, ch)
Line 953: time.sleep(0.03)  # 30 ms inter-key delay
Line 956: def _open_terminal() -> dict:
Line 957: """Launch a terminal emulator (xfce4-terminal or xterm fallback)."""
Line 958: terminal_candidates = ["xfce4-terminal", "xterm"]
Line 959: for terminal in terminal_candidates:
Line 960: try:
Line 961: subprocess.Popen(
Line 962: [terminal],
Line 963: stdout=subprocess.DEVNULL,
Line 964: stderr=subprocess.DEVNULL,
Line 965: env={**os.environ, "DISPLAY": ":99"},
Line 966: )
Line 967: time.sleep(1)
Line 968: return {"success": True, "message": f"Opened terminal ({terminal})"}
Line 969: except FileNotFoundError:
Line 970: continue
Line 971: except Exception as e:
Line 972: logger.warning("Failed to open %s: %s", terminal, e)
Line 973: continue
Line 974: return {"success": False, "message": "No terminal emulator available (tried xfce4-terminal, xterm)"}
Line 977: def _xdo_scroll(x: int, y: int, direction: str) -> dict:
Line 978: """Scroll at (x,y) via xdotool button events."""
Line 979: _xdo(["mousemove", "--sync", str(x), str(y)])
Line 980: btn = "4" if direction == "up" else "5"
Line 981: _xdo(["click", "--repeat", "5", "--delay", "40", btn])
Line 982: return {"success": True, "message": f"Scrolled {direction} at ({x}, {y})"}
Line 985: def _xdo_scroll_up() -> dict:
Line 986: """Scroll up at the current cursor position."""
Line 987: _xdo(["click", "--repeat", "5", "--delay", "40", "4"])
Line 988: return {"success": True, "message": "Scrolled up"}
Line 991: def _xdo_scroll_down() -> dict:
Line 992: """Scroll down at the current cursor position."""
Line 993: _xdo(["click", "--repeat", "5", "--delay", "40", "5"])
Line 994: return {"success": True, "message": "Scrolled down"}
Line 997: def _xdo_window_minimize(identifier: str) -> dict:
Line 998: """Minimise the window matching *identifier*."""
Line 999: wids = _xdo(["search", "--name", identifier]).split("\n")
Line 1000: if wids and wids[0]:
Line 1001: _xdo(["windowminimize", wids[0]])
Line 1002: return {"success": True, "message": f"Minimized window: {identifier}"}
Line 1003: return {"success": False, "message": f"Window not found: {identifier}"}
Line 1006: def _xdo_window_maximize(identifier: str) -> dict:
Line 1007: """Maximise the window matching *identifier* via wmctrl."""
Line 1008: wids = _xdo(["search", "--name", identifier]).split("\n")
Line 1009: if wids and wids[0]:
Line 1010: subprocess.run(["wmctrl", "-ir", wids[0], "-b", "add,maximized_vert,maximized_horz"], check=False, timeout=5)
Line 1011: return {"success": True, "message": f"Maximized window: {identifier}"}
Line 1012: return {"success": False, "message": f"Window not found: {identifier}"}
Line 1015: def _xdo_window_move(identifier: str, x: int, y: int) -> dict:
Line 1016: """Move the window matching *identifier* to (x,y)."""
Line 1017: wids = _xdo(["search", "--name", identifier]).split("\n")
Line 1018: if wids and wids[0]:
Line 1019: _xdo(["windowmove", wids[0], str(x), str(y)])
Line 1020: return {"success": True, "message": f"Moved window {identifier} to {x},{y}"}
Line 1021: return {"success": False, "message": f"Window not found: {identifier}"}
Line 1024: def _xdo_window_resize(identifier: str, w: int, h: int) -> dict:
Line 1025: """Resize the window matching *identifier* to w x h."""
Line 1026: wids = _xdo(["search", "--name", identifier]).split("\n")
Line 1027: if wids and wids[0]:
Line 1028: _xdo(["windowsize", wids[0], str(w), str(h)])
Line 1029: return {"success": True, "message": f"Resized window {identifier} to {w}x{h}"}
Line 1030: return {"success": False, "message": f"Window not found: {identifier}"}
Line 1033: def _xdo_search_window(identifier: str) -> dict:
Line 1034: """Search for a window by name and return its window IDs."""
Line 1035: try:
Line 1036: wids = _xdo(["search", "--name", identifier]).split("\n")
Line 1037: if wids and wids[0]:
Line 1038: return {"success": True, "message": f"Found window: {identifier} (wids: {wids})", "wids": wids}
Line 1039: except Exception:
Line 1040: pass
Line 1041: return {"success": False, "message": f"Window not found: {identifier}"}
Line 1044: def _xdo_keydown(key: str) -> dict:
Line 1045: """Hold a key down via xdotool."""
Line 1046: combo = _map_key_combo_xdotool(key)
Line 1047: _xdo(["keydown", combo])
Line 1048: return {"success": True, "message": f"Key down: {key}"}
Line 1051: def _xdo_keyup(key: str) -> dict:
Line 1052: """Release a held key via xdotool."""
Line 1053: combo = _map_key_combo_xdotool(key)
Line 1054: _xdo(["keyup", combo])
Line 1055: return {"success": True, "message": f"Key up: {key}"}
Line 1058: def _xdo_type_slow(text: str) -> dict:
Line 1059: """Type text with a larger inter-key delay (150 ms)."""
Line 1060: _xdo(["type", "--clearmodifiers", "--delay", "150", "--", text])
Line 1061: return {"success": True, "message": f"Typed slow: {text[:50]}"}
Line 1064: def _xdo_key(key: str) -> dict:
Line 1065: """Press and release a key combo via xdotool."""
Line 1066: combo = _map_key_combo_xdotool(key)
Line 1067: _xdo(["key", "--clearmodifiers", combo])
Line 1068: return {"success": True, "message": f"Pressed key: {key}"}
Line 1071: def _xdo_drag(x1: int, y1: int, x2: int, y2: int) -> dict:
Line 1072: """Drag from (x1,y1) to (x2,y2) via xdotool."""
Line 1073: _xdo(["mousemove", "--sync", str(x1), str(y1)])
Line 1074: _xdo(["mousedown", "1"])
Line 1075: _xdo(["mousemove", "--sync", str(x2), str(y2)])
Line 1076: _xdo(["mouseup", "1"])
Line 1077: return {"success": True, "message": f"Dragged ({x1},{y1}) → ({x2},{y2})"}
Line 1080: # ── Deterministic browser launch (shared by xdotool open_url) ─────────────
Line 1082: # Pre-created profile directory (seeded at build-time in Dockerfile)
Line 1083: _CHROME_PROFILE_DIR = "/tmp/chrome-profile"
Line 1085: # Chrome flags that suppress ALL first-run UI, keyring dialogs, and sync prompts
Line 1086: _CHROME_FLAGS: list[str] = [
Line 1087: "--no-sandbox",
Line 1088: "--no-first-run",
Line 1089: "--disable-first-run-ui",
Line 1090: "--disable-sync",
Line 1091: "--disable-extensions",
Line 1092: "--disable-default-apps",
Line 1093: "--disable-popup-blocking",
Line 1094: "--disable-translate",
Line 1095: "--disable-background-networking",
Line 1096: "--password-store=basic",          # avoid gnome-keyring / kwallet prompts
Line 1097: "--disable-infobars",
Line 1098: "--no-default-browser-check",
Line 1099: f"--user-data-dir={_CHROME_PROFILE_DIR}",
Line 1100: f"--window-size={SCREEN_WIDTH},{SCREEN_HEIGHT}",
Line 1101: ]
Line 1103: # Known modal window titles that should be auto-dismissed after browser launch
Line 1104: _KNOWN_MODAL_TITLES = (
Line 1105: "Welcome to Google Chrome",
Line 1106: "Sign in to Chrome",
Line 1107: "Chrome is being controlled by automated test software",
Line 1108: "Choose password for new keyring",
Line 1109: "Unlock Keyring",
Line 1110: "Set as default browser",
Line 1111: "Default Browser",
Line 1112: "Unlock Login Keyring",
Line 1113: )
Line 1116: def _resolve_browser_binary() -> tuple[str, list[str]] | None:
Line 1117: """Return (binary, extra_flags) for the first available browser.
Line 1119: Preference: google-chrome > chromium-browser > chromium > firefox.
Line 1120: Returns None when no browser is found.
Line 1121: """
Line 1122: chrome_candidates = ("google-chrome", "google-chrome-stable",
Line 1123: "chromium-browser", "chromium")
Line 1124: for name in chrome_candidates:
Line 1125: path = shutil.which(name)
Line 1126: if path:
Line 1127: return (path, list(_CHROME_FLAGS))
Line 1129: # Firefox fallback — different flag set
Line 1130: for name in ("firefox", "firefox-esr"):
Line 1131: path = shutil.which(name)
Line 1132: if path:
Line 1133: return (path, [
Line 1134: "--new-window",
Line 1135: f"--width={SCREEN_WIDTH}",
Line 1136: f"--height={SCREEN_HEIGHT}",
Line 1137: ])
Line 1139: return None
Line 1142: def _dismiss_known_modals() -> list[str]:
Line 1143: """Detect and close known first-run / keyring modal windows via wmctrl.
Line 1145: Returns a list of window titles that were closed.
Line 1146: """
Line 1147: dismissed: list[str] = []
Line 1148: try:
Line 1149: result = subprocess.run(
Line 1150: ["wmctrl", "-l"],
Line 1151: capture_output=True, text=True, timeout=5,
Line 1152: )
Line 1153: if result.returncode != 0:
Line 1154: return dismissed
Line 1156: for line in result.stdout.strip().splitlines():
Line 1157: # wmctrl -l format: <wid> <desktop> <host> <title>
Line 1158: parts = line.split(None, 3)
Line 1159: if len(parts) < 4:
Line 1160: continue
Line 1161: title = parts[3]
Line 1162: for modal_title in _KNOWN_MODAL_TITLES:
Line 1163: if modal_title.lower() in title.lower():
Line 1164: subprocess.run(
Line 1165: ["wmctrl", "-c", title],
Line 1166: capture_output=True, text=True, timeout=3,
Line 1167: )
Line 1168: dismissed.append(title)
Line 1169: logger.info("Auto-dismissed modal window: %s", title)
Line 1170: break
Line 1171: except Exception as exc:
Line 1172: logger.warning("Modal dismissal scan failed: %s", exc)
Line 1173: return dismissed
Line 1176: def _open_url_in_browser(url: str) -> dict:
Line 1177: """Open *url* in a real browser with deterministic, modal-free startup.
Line 1179: 1. Resolve browser binary + flags.
Line 1180: 2. Launch with first-run / keyring suppression.
Line 1181: 3. Wait for window, normalise geometry.
Line 1182: 4. Auto-dismiss any residual modal dialogs.
Line 1183: 5. Fall back to xdg-open only as last resort.
Line 1184: """
Line 1185: if not url.startswith(("http://", "https://")):
Line 1186: url = "https://" + url
Line 1188: browser = _resolve_browser_binary()
Line 1189: if browser is None:
Line 1190: # Ultimate fallback
Line 1191: logger.warning("No browser binary found — falling back to xdg-open")
Line 1192: subprocess.Popen(
Line 1193: ["xdg-open", url],
Line 1194: stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL,
Line 1195: )
Line 1196: return {"success": True, "message": f"Opened URL (xdg-open fallback): {url}"}
Line 1198: binary, flags = browser
Line 1199: cmd = [binary] + flags + [url]
Line 1200: logger.info("Launching browser: %s", " ".join(cmd))
Line 1202: try:
Line 1203: subprocess.Popen(
Line 1204: cmd,
Line 1205: stdout=subprocess.DEVNULL,
Line 1206: stderr=subprocess.DEVNULL,
Line 1207: env={**os.environ, "DISPLAY": ":99"},
Line 1208: )
Line 1209: except Exception as exc:
Line 1210: logger.error("Browser launch failed: %s — falling back to xdg-open", exc)
Line 1211: subprocess.Popen(
Line 1212: ["xdg-open", url],
Line 1213: stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL,
Line 1214: )
Line 1215: return {"success": True, "message": f"Opened URL (xdg-open fallback after error): {url}"}
Line 1217: # Give the browser time to create its window
Line 1218: time.sleep(2.0)
Line 1220: # Auto-dismiss any first-run / keyring modals
Line 1221: dismissed = _dismiss_known_modals()
Line 1222: if dismissed:
Line 1223: time.sleep(0.5)
Line 1224: # Dismiss again in case closing one modal spawned another
Line 1225: _dismiss_known_modals()
Line 1227: # Find and normalise the browser window
Line 1228: norm_msg = ""
Line 1229: for hint in ("chrome", "chromium", "firefox", "mozilla", "navigator"):
Line 1230: wids = _xdo_search_window_ids(hint)
Line 1231: if wids:
Line 1232: wid = wids[-1]
Line 1233: try:
Line 1234: _xdo(["windowactivate", "--sync", wid])
Line 1235: time.sleep(0.3)
Line 1236: norm_msg = _xdo_normalize_window(wid)
Line 1237: except Exception:
Line 1238: norm_msg = "window normalisation skipped"
Line 1239: break
Line 1241: dismiss_info = f" (dismissed modals: {dismissed})" if dismissed else ""
Line 1242: return {
Line 1243: "success": True,
Line 1244: "message": f"Opened URL: {url} via {binary}. {norm_msg}{dismiss_info}",
Line 1245: }
Line 1248: def _xdo_open_url(url: str) -> dict:
Line 1249: """Open URL in a deterministic browser — avoids xdg-open first-run problems."""
Line 1250: return _open_url_in_browser(url)
Line 1253: def _xdo_hover(x: int, y: int) -> dict:
Line 1254: """Move the mouse to (x,y) without clicking."""
Line 1255: _xdo(["mousemove", "--sync", str(x), str(y)])
Line 1256: return {"success": True, "message": f"Hovered at ({x}, {y})"}
Line 1259: def _xdo_middle_click(x: int, y: int) -> dict:
Line 1260: """Middle-click at (x,y) via xdotool."""
Line 1261: _xdo(["mousemove", "--sync", str(x), str(y)])
Line 1262: _xdo(["click", "2"])
Line 1263: return {"success": True, "message": f"Middle-clicked at ({x}, {y})"}
Line 1266: def _is_terminal_focused() -> bool:
Line 1267: """Check if the currently focused window is a terminal emulator.
Line 1269: Terminals interpret Ctrl+V as a literal control character; paste
Line 1270: requires Ctrl+Shift+V instead.
Line 1271: """
Line 1272: try:
Line 1273: name = subprocess.run(
Line 1274: ["xdotool", "getwindowfocus", "getwindowname"],
Line 1275: capture_output=True, text=True, timeout=3,
Line 1276: ).stdout.strip().lower()
Line 1277: _TERMINAL_HINTS = (
Line 1278: "terminal", "xterm", "konsole", "alacritty", "kitty",
Line 1279: "tmux", "bash", "zsh", "sh —", "fish",
Line 1280: )
Line 1281: return any(hint in name for hint in _TERMINAL_HINTS)
Line 1282: except Exception:
Line 1283: return False
Line 1286: def _xdo_paste(text: str) -> dict:
Line 1287: """Copy text to clipboard then paste via Ctrl+V (or Ctrl+Shift+V in terminals)."""
Line 1288: subprocess.run(
Line 1289: ["xclip", "-selection", "clipboard"],
Line 1290: input=text.encode(), check=True, timeout=5,
Line 1291: )
Line 1292: if _is_terminal_focused():
Line 1293: _xdo(["key", "--clearmodifiers", "ctrl+shift+v"])
Line 1294: else:
Line 1295: _xdo(["key", "--clearmodifiers", "ctrl+v"])
Line 1296: return {"success": True, "message": f"Pasted: {text[:50]}"}
Line 1299: def _xdo_copy() -> dict:
Line 1300: """Copy the current selection to clipboard via xdotool."""
Line 1301: _xdo(["key", "--clearmodifiers", "ctrl+c"])
Line 1302: return {"success": True, "message": "Copied selection to clipboard"}
Line 1305: def _xdo_hotkey(keys: list[str]) -> dict:
Line 1306: """Press a multi-key combo via xdotool."""
Line 1307: combo = "+".join(_map_key_combo_xdotool(k) for k in keys)
Line 1308: _xdo(["key", "--clearmodifiers", combo])
Line 1309: return {"success": True, "message": f"Hotkey: {'+'.join(keys)}"}
Line 1312: def _xdo_focus_window(identifier: str) -> dict:
Line 1313: """Focus a window by name or class."""
Line 1314: wids = _xdo_search_window_ids(identifier)
Line 1315: if wids:
Line 1316: wid = wids[0]
Line 1317: _xdo(["windowactivate", "--sync", wid])
Line 1318: normalization_msg = _xdo_normalize_window(wid)
Line 1319: return {
Line 1320: "success": True,
Line 1321: "message": f"Focused window: {identifier} (wid={wid}; {normalization_msg})",
Line 1322: }
Line 1323: return {"success": False, "message": f"Window not found: {identifier}"}
Line 1326: def _xdo_open_app(app_name: str) -> dict:
Line 1327: """Launch an application by command name.
Line 1329: After a successful launch the new window is automatically found,
Line 1330: activated, and normalised to a deterministic geometry so that
Line 1331: subsequent coordinate-based actions are reliable.
Line 1332: """
Line 1333: candidates = _expand_app_launch_candidates(app_name)
Line 1334: if not candidates:
Line 1335: return {"success": False, "message": "open_app requires a non-empty app command"}
Line 1337: failures: list[str] = []
Line 1338: for candidate in candidates:
Line 1339: parts = shlex.split(candidate)
Line 1340: if not parts:
Line 1341: continue
Line 1342: binary = parts[0]
Line 1343: if shutil.which(binary) is None:
Line 1344: failures.append(f"{candidate}: command not found")
Line 1345: continue
Line 1346: try:
Line 1347: subprocess.Popen(
Line 1348: parts,
Line 1349: stdout=subprocess.DEVNULL,
Line 1350: stderr=subprocess.DEVNULL,
Line 1351: env={**os.environ, "DISPLAY": ":99"},
Line 1352: )
Line 1353: time.sleep(1.5)  # give the window time to materialise
Line 1355: # ── Post-launch: find, activate & normalise the new window ──
Line 1356: norm_msg = _post_launch_normalize(candidate)
Line 1357: return {"success": True, "message": f"Launched: {candidate}. {norm_msg}"}
Line 1358: except Exception as e:
Line 1359: failures.append(f"{candidate}: {e}")
Line 1361: reason = "; ".join(failures) if failures else "no launch candidates"
Line 1362: return {
Line 1363: "success": False,
Line 1364: "message": f"Failed to launch '{app_name}'. Tried: {', '.join(candidates)}. Reasons: {reason}",
Line 1365: }
Line 1368: def _post_launch_normalize(hint: str) -> str:
Line 1369: """Find the most-recently-created window, activate it, and normalise.
Line 1371: *hint* is used for a name-based search first; if that fails the
Line 1372: currently-active window is normalised instead.
Line 1373: """
Line 1374: wids = _xdo_search_window_ids(hint)
Line 1375: if not wids:
Line 1376: # Fallback: try the currently-active window
Line 1377: try:
Line 1378: wid = _xdo(["getactivewindow"]).strip()
Line 1379: if wid:
Line 1380: wids = [wid]
Line 1381: except Exception:
Line 1382: pass
Line 1383: if not wids:
Line 1384: return "window not found for normalisation"
Line 1386: wid = wids[-1]  # most recent match
Line 1387: try:
Line 1388: _xdo(["windowactivate", "--sync", wid])
Line 1389: # Brief extra settle time after activation
Line 1390: time.sleep(0.3)
Line 1391: norm_msg = _xdo_normalize_window(wid)
Line 1392: return f"Window activated (wid={wid}); {norm_msg}"
Line 1393: except Exception as e:
Line 1394: return f"post-launch normalisation failed: {e}"
Line 1397: def _wmctrl_close_window(identifier: str) -> dict:
Line 1398: """Gracefully close a window via EWMH using wmctrl -c."""
Line 1399: result = subprocess.run(
Line 1400: ["wmctrl", "-c", identifier],
Line 1401: capture_output=True, text=True, timeout=5,
Line 1402: )
Line 1403: if result.returncode == 0:
Line 1404: return {"success": True, "message": f"Closed window: {identifier}"}
Line 1405: return {"success": False, "message": f"Failed to close window: {identifier} — {result.stderr.strip()}"}
Line 1408: def _xdo_screenshot_full() -> str:
Line 1409: """Capture the full screen via scrot."""
Line 1410: subprocess.run(
Line 1411: ["scrot", "-z", "-o", "/tmp/full.png"],
Line 1412: check=True, timeout=5,
Line 1413: )
Line 1414: with open("/tmp/full.png", "rb") as f:
Line 1415: return base64.b64encode(f.read()).decode("ascii")
Line 1418: def _xdo_screenshot_region(x: int, y: int, w: int, h: int) -> str:
Line 1419: """Capture a region of the screen via scrot."""
Line 1420: subprocess.run(
Line 1421: ["scrot", "-z", "-o", "-a", f"{x},{y},{w},{h}", "/tmp/region.png"],
Line 1422: check=True, timeout=5,
Line 1423: )
Line 1424: with open("/tmp/region.png", "rb") as f:
Line 1425: return base64.b64encode(f.read()).decode("ascii")
Line 1428: def _xdo_focus_click(identifier: str, x: int, y: int) -> dict:
Line 1429: """Focus a window and then click at x, y."""
Line 1430: # 1. Focus the window
Line 1431: wids = _xdo(["search", "--name", identifier]).split("\n")
Line 1432: if not wids or not wids[0]:
Line 1433: return {"success": False, "message": f"Window not found: {identifier}"}
Line 1435: _xdo(["windowactivate", "--sync", wids[0]])
Line 1436: time.sleep(0.2)
Line 1438: # 2. Click relative to that window (or absolute if just screen coords provided)
Line 1439: # The command provided assumes x,y are screen coordinates.
Line 1440: # To click safely after focus, we just move and click.
Line 1441: _xdo(["mousemove", "--sync", str(x), str(y)])
Line 1442: _xdo(["click", "1"])
Line 1444: return {"success": True, "message": f"Focused {identifier} and clicked at ({x}, {y})"}
Line 1447: # ══════════════════════════════════════════════════════════════════════════════
Line 1448: #  Shared helpers
Line 1449: # ══════════════════════════════════════════════════════════════════════════════
Line 1451: # ── wmctrl-based window management (DISPLAY-independent layer) ────────────────
Line 1453: def _wmctrl_focus_window(identifier: str) -> dict:
Line 1454: """Focus a window by partial title match using wmctrl (no xdotool)."""
Line 1455: result = subprocess.run(
Line 1456: ["wmctrl", "-a", identifier],
Line 1457: capture_output=True, text=True, timeout=5,
Line 1458: )
Line 1459: if result.returncode == 0:
Line 1460: return {"success": True, "message": f"Focused window: {identifier}"}
Line 1461: return {"success": False, "message": f"Window not found: {identifier} — {result.stderr.strip()}"}
Line 1464: def _wmctrl_search_window(identifier: str) -> dict:
Line 1465: """Search for windows by title via wmctrl -l."""
Line 1466: result = subprocess.run(
Line 1467: ["wmctrl", "-l"],
Line 1468: capture_output=True, text=True, timeout=5,
Line 1469: )
Line 1470: if result.returncode != 0:
Line 1471: return {"success": False, "message": f"wmctrl list failed: {result.stderr.strip()}"}
Line 1472: matches = [line for line in result.stdout.splitlines() if identifier.lower() in line.lower()]
Line 1473: if matches:
Line 1474: return {"success": True, "message": f"Found {len(matches)} window(s) matching '{identifier}': {'; '.join(matches[:5])}"}
Line 1475: return {"success": False, "message": f"No windows matching: {identifier}"}
Line 1478: def _wmctrl_minimize_window(identifier: str) -> dict:
Line 1479: """Minimize a window by title using wmctrl."""
Line 1480: # wmctrl doesn't have a direct minimize; use -b add,hidden
Line 1481: result = subprocess.run(
Line 1482: ["wmctrl", "-r", identifier, "-b", "add,hidden"],
Line 1483: capture_output=True, text=True, timeout=5,
Line 1484: )
Line 1485: if result.returncode == 0:
Line 1486: return {"success": True, "message": f"Minimized window: {identifier}"}
Line 1487: return {"success": False, "message": f"Failed to minimize: {identifier} — {result.stderr.strip()}"}
Line 1490: def _wmctrl_maximize_window(identifier: str) -> dict:
Line 1491: """Maximize a window by title using wmctrl."""
Line 1492: result = subprocess.run(
Line 1493: ["wmctrl", "-r", identifier, "-b", "add,maximized_vert,maximized_horz"],
Line 1494: capture_output=True, text=True, timeout=5,
Line 1495: )
Line 1496: if result.returncode == 0:
Line 1497: return {"success": True, "message": f"Maximized window: {identifier}"}
Line 1498: return {"success": False, "message": f"Failed to maximize: {identifier} — {result.stderr.strip()}"}
Line 1501: def _wmctrl_move_window(identifier: str, x: int, y: int) -> dict:
Line 1502: """Move a window to (x,y) using wmctrl."""
Line 1503: # wmctrl -r <name> -e <gravity>,<x>,<y>,<w>,<h>  (-1 = unchanged)
Line 1504: result = subprocess.run(
Line 1505: ["wmctrl", "-r", identifier, "-e", f"0,{x},{y},-1,-1"],
Line 1506: capture_output=True, text=True, timeout=5,
Line 1507: )
Line 1508: if result.returncode == 0:
Line 1509: return {"success": True, "message": f"Moved window {identifier} to ({x},{y})"}
Line 1510: return {"success": False, "message": f"Failed to move: {identifier} — {result.stderr.strip()}"}
Line 1513: def _wmctrl_resize_window(identifier: str, w: int, h: int) -> dict:
Line 1514: """Resize a window using wmctrl."""
Line 1515: result = subprocess.run(
Line 1516: ["wmctrl", "-r", identifier, "-e", f"0,-1,-1,{w},{h}"],
Line 1517: capture_output=True, text=True, timeout=5,
Line 1518: )
Line 1519: if result.returncode == 0:
Line 1520: return {"success": True, "message": f"Resized window {identifier} to {w}x{h}"}
Line 1521: return {"success": False, "message": f"Failed to resize: {identifier} — {result.stderr.strip()}"}
Line 1525: _KEY_MAP = {
Line 1526: "enter": "Enter", "return": "Enter",
Line 1527: "tab": "Tab",
Line 1528: "escape": "Escape", "esc": "Escape",
Line 1529: "backspace": "Backspace",
Line 1530: "delete": "Delete",
Line 1531: "space": " ",
Line 1532: "up": "ArrowUp", "down": "ArrowDown",
Line 1533: "left": "ArrowLeft", "right": "ArrowRight",
Line 1534: "home": "Home", "end": "End",
Line 1535: "pageup": "PageUp", "pagedown": "PageDown",
Line 1536: }
Line 1538: _KEY_MAP_XDO = {
Line 1539: "enter": "Return", "return": "Return",
Line 1540: "tab": "Tab",
Line 1541: "escape": "Escape", "esc": "Escape",
Line 1542: "backspace": "BackSpace",
Line 1543: "delete": "Delete",
Line 1544: "space": "space",
Line 1545: "up": "Up", "down": "Down",
Line 1546: "left": "Left", "right": "Right",
Line 1547: "home": "Home", "end": "End",
Line 1548: "pageup": "Prior", "pagedown": "Next",
Line 1549: }
Line 1552: def _map_key_combo(key: str) -> str:
Line 1553: """Map a user key string to a Playwright key combo."""
Line 1554: if "+" in key:
Line 1555: parts = [p.strip() for p in key.split("+")]
Line 1556: modifiers = []
Line 1557: for p in parts[:-1]:
Line 1558: pl = p.lower()
Line 1559: if pl in ("ctrl", "control"):
Line 1560: modifiers.append("Control")
Line 1561: elif pl == "alt":
Line 1562: modifiers.append("Alt")
Line 1563: elif pl == "shift":
Line 1564: modifiers.append("Shift")
Line 1565: elif pl in ("meta", "super", "win", "cmd"):
Line 1566: modifiers.append("Meta")
Line 1567: else:
Line 1568: # Add other modifiers if any, or just ignore
Line 1569: pass
Line 1571: # Handle the last part (the actual key)
Line 1572: last_part = parts[-1]
Line 1573: # Normalize last part
Line 1574: final = _KEY_MAP.get(last_part.lower(), last_part)
Line 1576: # Special case: ensure single char keys are proper case if needed?
Line 1577: # Playwright usually handles "a", "A", etc.
Line 1579: return "+".join(modifiers + [final])
Line 1581: # Single key
Line 1582: k = key.lower()
Line 1583: if k in ("ctrl", "control"): return "Control"
Line 1584: if k == "alt": return "Alt"
Line 1585: if k == "shift": return "Shift"
Line 1586: if k in ("meta", "super", "win", "cmd"): return "Meta"
Line 1588: return _KEY_MAP.get(k, key)
Line 1591: def _map_key_combo_xdotool(key: str) -> str:
Line 1592: """Map a user key string to an xdotool key combo."""
Line 1593: if "+" in key:
Line 1594: parts = [p.strip() for p in key.split("+")]
Line 1595: mapped = []
Line 1596: for p in parts:
Line 1597: pl = p.lower()
Line 1598: if pl in ("ctrl", "control"):
Line 1599: mapped.append("ctrl")
Line 1600: elif pl == "alt":
Line 1601: mapped.append("alt")
Line 1602: elif pl == "shift":
Line 1603: mapped.append("shift")
Line 1604: elif pl in ("meta", "super", "win", "cmd"):
Line 1605: mapped.append("super")
Line 1606: else:
Line 1607: mapped.append(_KEY_MAP_XDO.get(pl, p))
Line 1608: return "+".join(mapped)
Line 1609: return _KEY_MAP_XDO.get(key.lower(), key)
Line 1612: def _do_wait(duration: float) -> dict:
Line 1613: """Sleep for *duration* seconds (clamped to 0.1–10s)."""
Line 1614: capped = min(max(duration, 0.1), 10.0)
Line 1615: time.sleep(capped)
Line 1616: return {"success": True, "message": f"Waited {capped:.1f}s"}
Line 1619: # ══════════════════════════════════════════════════════════════════════════════
Line 1620: #  HTTP Server
Line 1621: # ══════════════════════════════════════════════════════════════════════════════
Line 1623: class AgentHandler(BaseHTTPRequestHandler):
Line 1624: """HTTP handler supporting both browser and desktop modes."""
Line 1626: def log_message(self, fmt, *args):
Line 1627: """Redirect HTTP request logging to the module logger."""
Line 1628: logger.debug("HTTP %s", fmt % args)
Line 1630: def _respond(self, code: int, data: dict):
Line 1631: """Send a JSON response with the given status code."""
Line 1632: body = json.dumps(data).encode()
Line 1633: self.send_response(code)
Line 1634: self.send_header("Content-Type", "application/json")
Line 1635: self.send_header("Content-Length", str(len(body)))
Line 1636: self.end_headers()
Line 1637: self.wfile.write(body)
Line 1639: def _read_body(self) -> dict:
Line 1640: """Parse the JSON request body, enforcing a size limit."""
Line 1641: length = int(self.headers.get("Content-Length", 0))
Line 1642: if length == 0:
Line 1643: return {}
Line 1644: if length > _MAX_BODY_SIZE:
Line 1645: raise ValueError(f"Request body too large: {length} bytes (max {_MAX_BODY_SIZE})")
Line 1646: raw = self.rfile.read(length)
Line 1647: return json.loads(raw)
Line 1649: # ── GET ───────────────────────────────────────────────────────────────
Line 1651: def do_GET(self):
Line 1652: """Handle GET requests (/health, /health/a11y, /screenshot)."""
Line 1653: if self.path == "/health":
Line 1654: self._respond(200, {
Line 1655: "status": "ok",
Line 1656: "browser": _browser is not None,
Line 1657: "default_mode": DEFAULT_MODE,
Line 1658: "cdp_url": "http://127.0.0.1:9223",
Line 1659: })
Line 1660: return
Line 1662: if self.path == "/health/a11y":
Line 1663: # AT-SPI accessibility bus health check.
Line 1664: # GObject Introspection / AT-SPI internally spins a GLib main
Line 1665: # loop which conflicts with the HTTPServer thread.  To avoid
Line 1666: # "Cannot run the event loop while another loop is running",
Line 1667: # we run the check in a **separate subprocess**.
Line 1668: import subprocess, sys
Line 1669: _a11y_script = (
Line 1670: "import sys;"
Line 1671: "try:\n"
Line 1672: "    import gi; gi.require_version('Atspi', '2.0');"
Line 1673: "    from gi.repository import Atspi;"
Line 1674: "    apps = Atspi.get_desktop(0).get_child_count();"
Line 1675: "    print(f'OK:{apps}')\n"
Line 1676: "except ImportError as e:\n"
Line 1677: "    print(f'NOIMPORT:{e}'); sys.exit(1)\n"
Line 1678: "except Exception as e:\n"
Line 1679: "    print(f'ERR:{e}'); sys.exit(2)"
Line 1680: )
Line 1681: try:
Line 1682: proc = subprocess.run(
Line 1683: [sys.executable, "-c", _a11y_script],
Line 1684: capture_output=True, text=True, timeout=10,
Line 1685: )
Line 1686: stdout = proc.stdout.strip()
Line 1687: if proc.returncode == 0 and stdout.startswith("OK:"):
Line 1688: app_count = int(stdout.split(":")[1])
Line 1689: self._respond(200, {"healthy": app_count > 0, "bindings": True, "apps": app_count})
Line 1690: elif stdout.startswith("NOIMPORT:"):
Line 1691: self._respond(200, {"healthy": False, "bindings": False, "error": stdout})
Line 1692: else:
Line 1693: self._respond(200, {"healthy": False, "bindings": True, "error": stdout or proc.stderr.strip()})
Line 1694: except subprocess.TimeoutExpired:
Line 1695: self._respond(200, {"healthy": False, "bindings": False, "error": "AT-SPI health check timed out"})
Line 1696: except Exception as e:
Line 1697: self._respond(200, {"healthy": False, "bindings": False, "error": str(e)})
Line 1698: return
Line 1700: if self.path.startswith("/screenshot"):
Line 1701: # Parse ?mode=desktop|browser from query string
Line 1702: mode = self._parse_mode_from_query()
Line 1703: with _lock:
Line 1704: try:
Line 1705: if mode == "desktop":
Line 1706: b64 = _screenshot_desktop()
Line 1707: self._respond(200, {"screenshot": b64, "method": "desktop"})
Line 1708: else:
Line 1709: try:
Line 1710: b64 = _screenshot_playwright()
Line 1711: self._respond(200, {"screenshot": b64, "method": "playwright"})
Line 1712: except Exception:
Line 1713: b64 = _screenshot_desktop()
Line 1714: self._respond(200, {"screenshot": b64, "method": "desktop_fallback"})
Line 1715: except Exception as e:
Line 1716: self._respond(500, {"error": str(e)})
Line 1717: return
Line 1719: self._respond(404, {"error": "not found"})
Line 1721: # ── POST ──────────────────────────────────────────────────────────────
Line 1723: def do_POST(self):
Line 1724: """Handle POST requests (/action, /mode)."""
Line 1725: body = self._read_body()
Line 1727: if self.path == "/action":
Line 1728: # Handle 'wait' outside the lock so it doesn't block
Line 1729: # screenshots and other concurrent requests for up to 10 s.
Line 1730: raw_action = body.get("action", "")
Line 1731: resolved = resolve_action(raw_action)
Line 1732: if resolved == "wait":
Line 1733: dur = 2.0
Line 1734: t = body.get("text", "")
Line 1735: if t:
Line 1736: try:
Line 1737: dur = float(t)
Line 1738: except ValueError:
Line 1739: pass
Line 1740: self._respond(200, _do_wait(dur))
Line 1741: return
Line 1743: with _lock:
Line 1744: try:
Line 1745: result = self._dispatch_action(body)
Line 1746: self._respond(200, result)
Line 1747: except Exception as e:
Line 1748: logger.exception("Action failed")
Line 1749: self._respond(500, {"success": False, "message": str(e)})
Line 1750: return
Line 1752: if self.path == "/mode":
Line 1753: # Switch default mode at runtime
Line 1754: new_mode = body.get("mode", "").lower()
Line 1755: if new_mode in ("browser", "desktop"):
Line 1756: global DEFAULT_MODE
Line 1757: DEFAULT_MODE = new_mode
Line 1758: logger.info("Default mode switched to: %s", DEFAULT_MODE)
Line 1759: self._respond(200, {"mode": DEFAULT_MODE})
Line 1760: else:
Line 1761: self._respond(400, {"error": "mode must be 'browser' or 'desktop'"})
Line 1762: return
Line 1764: self._respond(404, {"error": "not found"})
Line 1766: # ── Helpers ───────────────────────────────────────────────────────────
Line 1768: def _parse_mode_from_query(self) -> str:
Line 1769: """Extract mode from ?mode=... query param, or use DEFAULT_MODE."""
Line 1770: if "?" in self.path:
Line 1771: qs = self.path.split("?", 1)[1]
Line 1772: for part in qs.split("&"):
Line 1773: if part.startswith("mode="):
Line 1774: val = part.split("=", 1)[1].lower()
Line 1775: if val in ("browser", "desktop"):
Line 1776: return val
Line 1777: return DEFAULT_MODE
Line 1779: def _dispatch_action(self, body: dict) -> dict:
Line 1780: """Route an incoming action to the correct engine dispatcher."""
Line 1781: start_time = time.time()
Line 1783: # 1. Resolve alias
Line 1784: raw_action = body.get("action", "")
Line 1785: action = resolve_action(raw_action)
Line 1787: coords = body.get("coordinates", [])
Line 1788: text = body.get("text", "")
Line 1789: target = body.get("target", "")
Line 1790: mode = body.get("mode", DEFAULT_MODE).lower()
Line 1792: x = coords[0] if len(coords) >= 1 else SCREEN_WIDTH // 2
Line 1793: y = coords[1] if len(coords) >= 2 else SCREEN_HEIGHT // 2
Line 1795: result = {"success": False, "message": "Unknown error"}
Line 1797: try:
Line 1798: if action == "wait":
Line 1799: duration = 2.0
Line 1800: if text:
Line 1801: try:
Line 1802: duration = float(text)
Line 1803: except ValueError:
Line 1804: pass
Line 1805: result = _do_wait(duration)
Line 1807: # Dispatch to mode-specific handlers
Line 1808: elif mode == "omni_accessibility":
Line 1809: result = self._dispatch_accessibility(action, text, target)
Line 1810: elif mode == "desktop":
Line 1811: result = self._dispatch_desktop(action, x, y, text, coords, target)
Line 1812: else:
Line 1813: result = self._dispatch_browser(action, x, y, text, coords, target)
Line 1814: except Exception as e:
Line 1815: logger.exception(f"Action {action} failed")
Line 1816: result = {"success": False, "message": str(e)}
Line 1818: # Structured logging
Line 1819: latency = (time.time() - start_time) * 1000
Line 1820: log_entry = {
Line 1821: "action": action,
Line 1822: "engine": mode,
Line 1823: "success": result.get("success", False),
Line 1824: "latency_ms": latency,
Line 1825: "raw_action": raw_action
Line 1826: }
Line 1827: logger.info(json.dumps(log_entry))
Line 1829: return result
Line 1831: def _dispatch_accessibility(self, action: str, text: str, target: str) -> dict:
Line 1832: """Dispatch a single action to the AT-SPI accessibility engine.
Line 1834: The accessibility_engine module uses async functions internally
Line 1835: (asyncio.to_thread for AT-SPI GI calls), so we run them in a
Line 1836: dedicated event loop.  We use ``asyncio.new_event_loop()`` inside
Line 1837: a thread-isolated context to avoid conflicts with GLib's main loop.
Line 1838: """
Line 1839: import asyncio
Line 1840: import threading
Line 1841: try:
Line 1842: from backend.engines.accessibility_engine import execute_accessibility_action
Line 1843: except ImportError as exc:
Line 1844: return {
Line 1845: "success": False,
Line 1846: "message": f"Accessibility engine not available: {exc}",
Line 1847: }
Line 1849: result_container = [None]
Line 1850: error_container = [None]
Line 1852: def _run_in_thread():
Line 1853: """Run the async accessibility action in a fresh event loop on a separate thread."""
Line 1854: try:
Line 1855: loop = asyncio.new_event_loop()
Line 1856: asyncio.set_event_loop(loop)
Line 1857: try:
Line 1858: result_container[0] = loop.run_until_complete(
Line 1859: execute_accessibility_action(action, text=text, target=target)
Line 1860: )
Line 1861: finally:
Line 1862: loop.close()
Line 1863: except Exception as exc:
Line 1864: error_container[0] = exc
Line 1866: t = threading.Thread(target=_run_in_thread, daemon=True)
Line 1867: t.start()
Line 1868: t.join(timeout=30.0)
Line 1870: if error_container[0] is not None:
Line 1871: return {"success": False, "message": f"Accessibility action failed: {error_container[0]}"}
Line 1872: if result_container[0] is None:
Line 1873: return {"success": False, "message": "Accessibility action timed out (30s)"}
Line 1874: return result_container[0]
Line 1876: def _dispatch_browser(self, action: str, x: int, y: int, text: str, coords: list, target: str = "") -> dict:
Line 1877: """Dispatch a single action to the Playwright browser engine."""
Line 1878: # ── Mouse / Interaction ───────────────────────────────────────
Line 1879: if action == "click":
Line 1880: return _pw_click(x, y)
Line 1881: elif action == "double_click":
Line 1882: return _pw_double_click(x, y)
Line 1883: elif action == "right_click":
Line 1884: return _pw_right_click(x, y)
Line 1885: elif action == "middle_click":
Line 1886: return _pw_middle_click(x, y)
Line 1887: elif action == "hover":
Line 1888: return _pw_hover(x, y)
Line 1889: elif action == "drag":
Line 1890: if len(coords) >= 4:
Line 1891: return _pw_drag(coords[0], coords[1], coords[2], coords[3])
Line 1892: return {"success": False, "message": "drag requires 4 coordinates [x1, y1, x2, y2]"}
Line 1893: # ── Input ─────────────────────────────────────────────────────
Line 1894: elif action == "type":
Line 1895: selector = target or (text.split("|")[0] if "|" in text else "")
Line 1896: value = text.split("|")[1] if "|" in text else text
Line 1897: if not target and not "|" in text:
Line 1898: return _pw_type(text, coords)
Line 1899: return _pw_type(value, coords, selector)
Line 1900: elif action == "fill":
Line 1901: selector = target or (text.split("|")[0] if "|" in text else "")
Line 1902: value = text.split("|")[1] if "|" in text else text
Line 1903: if not selector:
Line 1904: return {"success": False, "message": "fill requires target (CSS selector)"}
Line 1905: return _pw_fill(selector, value)
Line 1906: elif action == "key":
Line 1907: return _pw_key(text)
Line 1908: elif action == "hotkey":
Line 1909: keys = [k.strip() for k in text.split("+")]
Line 1910: return _pw_hotkey(keys)
Line 1911: elif action == "clear_input":
Line 1912: selector = target or text
Line 1913: if not selector:
Line 1914: return {"success": False, "message": "clear_input requires target (CSS selector)"}
Line 1915: return _pw_clear_input(selector)
Line 1916: elif action == "select_option":
Line 1917: selector = target or ""
Line 1918: if not selector:
Line 1919: return {"success": False, "message": "select_option requires target (CSS selector)"}
Line 1920: return _pw_select_option(selector, text)
Line 1921: elif action == "paste":
Line 1922: return _pw_paste(text)
Line 1923: elif action == "copy":
Line 1924: return _pw_copy()
Line 1925: # ── Navigation ────────────────────────────────────────────────
Line 1926: elif action == "open_url":
Line 1927: return _pw_navigate(text)
Line 1928: elif action == "reload":
Line 1929: return _pw_reload()
Line 1930: elif action == "go_back":
Line 1931: return _pw_go_back()
Line 1932: elif action == "go_forward":
Line 1933: return _pw_go_forward()
Line 1934: # ── Tabs ──────────────────────────────────────────────────────
Line 1935: elif action == "new_tab":
Line 1936: return _pw_new_tab(text)
Line 1937: elif action == "close_tab":
Line 1938: return _pw_close_tab()
Line 1939: elif action == "switch_tab":
Line 1940: return _pw_switch_tab(text or target)
Line 1941: # ── Scrolling ─────────────────────────────────────────────────
Line 1942: elif action == "scroll":
Line 1943: direction = text.lower() if text else "down"
Line 1944: return _pw_scroll(x, y, direction)
Line 1945: elif action == "scroll_to":
Line 1946: selector = target or text
Line 1947: if not selector:
Line 1948: return {"success": False, "message": "scroll_to requires target (CSS selector)"}
Line 1949: return _pw_scroll_to(selector)
Line 1950: elif action == "scroll_into_view":
Line 1951: selector = target or text
Line 1952: if not selector:
Line 1953: return {"success": False, "message": "scroll_into_view requires target (CSS selector)"}
Line 1954: return _pw_scroll_into_view(selector)
Line 1955: # ── DOM / Semantic ────────────────────────────────────────────
Line 1956: elif action == "get_text":
Line 1957: selector = target or text
Line 1958: if not selector:
Line 1959: return {"success": False, "message": "get_text requires target (CSS selector)"}
Line 1960: return _pw_get_text(selector)
Line 1961: elif action == "find_element":
Line 1962: description = target or text
Line 1963: if not description:
Line 1964: return {"success": False, "message": "find_element requires target (text description)"}
Line 1965: return _pw_find_element(description)
Line 1966: # ── JavaScript ────────────────────────────────────────────────
Line 1967: elif action == "evaluate_js":
Line 1968: if not text:
Line 1969: return {"success": False, "message": "evaluate_js requires text (JS code)"}
Line 1970: return _pw_evaluate_js(text)
Line 1971: # ── Control ───────────────────────────────────────────────────
Line 1972: elif action == "wait_for":
Line 1973: selector = target or text
Line 1974: if not selector:
Line 1975: return {"success": False, "message": "wait_for requires target (CSS selector)"}
Line 1976: return _pw_wait_for(selector)
Line 1977: elif action == "wait_for_navigation":
Line 1978: return _pw_wait_for_navigation()
Line 1979: elif action == "screenshot_region":
Line 1980: if len(coords) >= 4:
Line 1981: b64 = _pw_screenshot_region(coords[0], coords[1], coords[2], coords[3])
Line 1982: return {"success": True, "message": f"Region screenshot captured", "screenshot": b64}
Line 1983: return {"success": False, "message": "screenshot_region needs 4 coords [x, y, width, height]"}
Line 1984: elif action == "screenshot_viewport":
Line 1985: b64 = _pw_screenshot_viewport()
Line 1986: return {"success": True, "message": "Viewport screenshot captured", "screenshot": b64}
Line 1987: elif action == "screenshot_element":
Line 1988: selector = target or text
Line 1989: if not selector:
Line 1990: return {"success": False, "message": "screenshot_element requires target"}
Line 1991: b64 = _pw_screenshot_element(selector)
Line 1992: if b64:
Line 1993: return {"success": True, "message": "Element screenshot captured", "screenshot": b64}
Line 1994: return {"success": False, "message": "Failed to capture element screenshot"}
Line 1995: elif action == "get_html":
Line 1996: selector = target or text
Line 1997: return _pw_get_html(selector)
Line 1998: elif action == "get_attribute":
Line 1999: selector = target or (text.split("|")[0] if "|" in text else "")
Line 2000: attribute = text.split("|")[1] if "|" in text else ""
Line 2001: if not selector or not attribute:
Line 2002: return {"success": False, "message": "get_attribute requires target (selector) and attribute (in text as selector|attribute)"}
Line 2003: return _pw_get_attribute(selector, attribute)
Line 2004: elif action == "query_selector":
Line 2005: selector = target or text
Line 2006: if not selector:
Line 2007: return {"success": False, "message": "query_selector requires target (CSS selector)"}
Line 2008: return _pw_query_selector(selector)
Line 2009: elif action == "query_all":
Line 2010: selector = target or text
Line 2011: if not selector:
Line 2012: return {"success": False, "message": "query_all requires target (CSS selector)"}
Line 2013: return _pw_query_all(selector)
Line 2014: elif action == "get_bounding_box":
Line 2015: selector = target or text
Line 2016: if not selector:
Line 2017: return {"success": False, "message": "get_bounding_box requires target (CSS selector)"}
Line 2018: return _pw_get_bounding_box(selector)
Line 2019: elif action == "get_visible_elements":
Line 2020: selector = target or text
Line 2021: if not selector:
Line 2022: return {"success": False, "message": "get_visible_elements requires target (CSS selector)"}
Line 2023: return _pw_get_visible_elements(selector)
Line 2024: elif action == "evaluate_on_selector":
Line 2025: selector = target or (text.split("|")[0] if "|" in text else "")
Line 2026: script = text.split("|")[1] if "|" in text else ""
Line 2027: if not selector or not script:
Line 2028: return {"success": False, "message": "evaluate_on_selector requires target and script"}
Line 2029: return _pw_evaluate_on_selector(selector, script)
Line 2030: elif action == "upload_file":
Line 2031: selector = target or (text.split("|")[0] if "|" in text else "")
Line 2032: file_path = text.split("|")[1] if "|" in text else text
Line 2033: if not selector or not file_path:
Line 2034: return {"success": False, "message": "upload_file requires target and file path"}
Line 2035: return _pw_upload_file(selector, file_path)
Line 2036: elif action == "download_file":
Line 2037: selector = target or text
Line 2038: return _pw_download_file(selector)
Line 2039: elif action == "handle_file_chooser":
Line 2040: return {"success": False, "message": "Not implemented yet"}
Line 2041: elif action == "set_cookies":
Line 2042: try:
Line 2043: cookies = json.loads(text)
Line 2044: return _pw_set_cookies(cookies)
Line 2045: except Exception as e:
Line 2046: return {"success": False, "message": f"set_cookies requires JSON text: {e}"}
Line 2047: elif action == "get_cookies":
Line 2048: return _pw_get_cookies()
Line 2049: elif action == "clear_cookies":
Line 2050: return _pw_clear_cookies()
Line 2051: elif action == "storage_state":
Line 2052: return {"success": False, "message": "Not implemented yet"}
Line 2053: elif action == "new_context":
Line 2054: return _pw_new_context()
Line 2055: elif action == "switch_context":
Line 2056: identifier = target or text
Line 2057: return _pw_switch_context(identifier)
Line 2058: elif action == "close_context":
Line 2059: return _pw_close_context()
Line 2060: elif action == "intercept_request":
Line 2061: return {"success": False, "message": "Not implemented yet"}
Line 2062: elif action == "monitor_requests":
Line 2063: return {"success": False, "message": "Not implemented yet"}
Line 2064: elif action == "get_response_body":
Line 2065: return {"success": False, "message": "Not implemented yet"}
Line 2066: elif action == "assert_element_present":
Line 2067: selector = target or text
Line 2068: if not selector:
Line 2069: return {"success": False, "message": "assert_element_present requires target (CSS selector)"}
Line 2070: return _pw_query_selector(selector)
Line 2071: elif action == "verify_text":
Line 2072: selector = target or "body"
Line 2073: needle = text or ""
Line 2074: if not needle:
Line 2075: return {"success": False, "message": "verify_text requires text to verify"}
Line 2076: res = _pw_get_text(selector)
Line 2077: if not res.get("success"):
Line 2078: return res
Line 2079: found = needle.lower() in res.get("message", "").lower()
Line 2080: return {"success": found, "message": f"verify_text={'ok' if found else 'not found'}: {needle[:120]}"}
Line 2081: elif action == "retry_last_action":
Line 2082: return {"success": False, "message": "Not implemented yet"}
Line 2083: elif action == "fallback_strategy":
Line 2084: return {"success": False, "message": "Not implemented yet"}
Line 2085: elif action == "set_viewport":
Line 2086: if len(coords) >= 2:
Line 2087: return _pw_set_viewport(coords[0], coords[1])
Line 2088: return {"success": False, "message": "set_viewport requires width and height"}
Line 2089: elif action == "zoom":
Line 2090: try:
Line 2091: level = float(text)
Line 2092: return _pw_zoom(level)
Line 2093: except ValueError:
Line 2094: return {"success": False, "message": "zoom requires a float value"}
Line 2095: elif action == "block_resource":
Line 2096: return _pw_block_resource(text)
Line 2097: elif action == "type_at":
Line 2098: if not coords or len(coords) < 2:
Line 2099: return {"success": False, "message": "type_at requires coordinates [x, y]"}
Line 2100: return _pw_type(text, coords)
Line 2101: elif action == "press_sequential":
Line 2102: # Stub: treat as type
Line 2103: return _pw_type(text, coords)
Line 2104: elif action == "scroll_to_selector":
Line 2105: selector = target or text
Line 2106: if not selector:
Line 2107: return {"success": False, "message": "scroll_to_selector requires target"}
Line 2108: return _pw_scroll_to(selector)
Line 2109: elif action == "export_page_pdf":
Line 2110: b64 = _pw_export_page_pdf()
Line 2111: return {"success": True, "message": "PDF exported", "pdf": b64}
Line 2112: else:
Line 2113: return {"success": False, "message": f"Unsupported action '{action}' in browser mode", "hint": "Check engine capability mapping"}
Line 2115: def _dispatch_desktop(self, action: str, x: int, y: int, text: str, coords: list, target: str = "") -> dict:
Line 2116: """Dispatch a single action to the xdotool desktop engine."""
Line 2117: # ── Mouse / Interaction ───────────────────────────────────────
Line 2118: if action == "click":
Line 2119: return _xdo_click(x, y)
Line 2120: elif action == "double_click":
Line 2121: return _xdo_double_click(x, y)
Line 2122: elif action == "right_click":
Line 2123: return _xdo_right_click(x, y)
Line 2124: elif action == "middle_click":
Line 2125: return _xdo_middle_click(x, y)
Line 2126: elif action == "hover":
Line 2127: return _xdo_hover(x, y)
Line 2128: elif action == "drag":
Line 2129: if len(coords) >= 4:
Line 2130: return _xdo_drag(coords[0], coords[1], coords[2], coords[3])
Line 2131: return {"success": False, "message": "drag requires 4 coordinates [x1, y1, x2, y2]"}
Line 2132: # ── Input ─────────────────────────────────────────────────────
Line 2133: elif action == "type":
Line 2134: if coords and len(coords) >= 2:
Line 2135: _xdo_click(coords[0], coords[1])
Line 2136: time.sleep(0.1)
Line 2137: try:
Line 2138: return _xdo_type(text)
Line 2139: except Exception:
Line 2140: logger.warning("xdotool type failed, trying paste fallback")
Line 2141: return _xdo_paste(text)
Line 2142: elif action == "key":
Line 2143: return _xdo_key(text)
Line 2144: elif action == "keydown":
Line 2145: return _xdo_keydown(text)
Line 2146: elif action == "keyup":
Line 2147: return _xdo_keyup(text)
Line 2148: elif action == "type_slow":
Line 2149: if coords and len(coords) >= 2:
Line 2150: _xdo_click(coords[0], coords[1])
Line 2151: time.sleep(0.1)
Line 2152: try:
Line 2153: return _xdo_type_slow(text)
Line 2154: except Exception:
Line 2155: logger.warning("xdotool type_slow failed, trying paste fallback")
Line 2156: return _xdo_paste(text)
Line 2157: elif action == "hotkey":
Line 2158: keys = [k.strip() for k in text.split("+")]
Line 2159: return _xdo_hotkey(keys)
Line 2160: elif action == "paste":
Line 2161: return _xdo_paste(text)
Line 2162: elif action == "copy":
Line 2163: return _xdo_copy()
Line 2164: # ── Navigation ────────────────────────────────────────────────
Line 2165: elif action == "open_url":
Line 2166: return _xdo_open_url(text)
Line 2167: # ── Scrolling ─────────────────────────────────────────────────
Line 2168: elif action == "scroll":
Line 2169: direction = text.lower() if text else "down"
Line 2170: return _xdo_scroll(x, y, direction)
Line 2171: elif action == "scroll_up":
Line 2172: return _xdo_scroll_up()
Line 2173: elif action == "scroll_down":
Line 2174: return _xdo_scroll_down()
Line 2175: # ── Desktop / Window ──────────────────────────────────────────
Line 2176: elif action == "focus_window":
Line 2177: identifier = target or text
Line 2178: if not identifier:
Line 2179: return {"success": False, "message": "focus_window requires target (window name)"}
Line 2180: return _xdo_focus_window(identifier)
Line 2181: elif action == "window_activate":
Line 2182: identifier = target or text
Line 2183: if not identifier:
Line 2184: return {"success": False, "message": "window_activate requires target"}
Line 2185: return _xdo_focus_window(identifier)
Line 2186: elif action == "focus_mouse":
Line 2187: _xdo(["mousemove", "--sync", str(x), str(y)])
Line 2188: return {"success": True, "message": f"Focused mouse at ({x}, {y})"}
Line 2189: elif action == "mousemove":
Line 2190: if len(coords) >= 2:
Line 2191: _xdo(["mousemove", "--sync", str(coords[0]), str(coords[1])])
Line 2192: return {"success": True, "message": f"Moved mouse to ({coords[0]}, {coords[1]})"}
Line 2193: return {"success": False, "message": "mousemove requires coordinates [x, y]"}
Line 2194: elif action == "open_app":
Line 2195: app = target or text
Line 2196: if not app:
Line 2197: return {"success": False, "message": "open_app requires target (app command)"}
Line 2198: return _xdo_open_app(app)
Line 2199: elif action == "close_window":
Line 2200: identifier = target or text
Line 2201: if not identifier:
Line 2202: return {"success": False, "message": "close_window requires target (window title or class)"}
Line 2203: return _wmctrl_close_window(identifier)
Line 2204: elif action == "window_minimize":
Line 2205: identifier = target or text
Line 2206: if not identifier:
Line 2207: return {"success": False, "message": "window_minimize requires target"}
Line 2208: return _xdo_window_minimize(identifier)
Line 2209: elif action == "window_maximize":
Line 2210: identifier = target or text
Line 2211: if not identifier:
Line 2212: return {"success": False, "message": "window_maximize requires target"}
Line 2213: return _xdo_window_maximize(identifier)
Line 2214: elif action == "window_move":
Line 2215: identifier = target or text
Line 2216: if not identifier or not coords or len(coords) < 2:
Line 2217: return {"success": False, "message": "window_move requires target and 2 coordinates"}
Line 2218: return _xdo_window_move(identifier, coords[0], coords[1])
Line 2219: elif action == "window_resize":
Line 2220: identifier = target or text
Line 2221: if not identifier or not coords or len(coords) < 2:
Line 2222: return {"success": False, "message": "window_resize requires target and 2 coordinates"}
Line 2223: return _xdo_window_resize(identifier, coords[0], coords[1])
Line 2224: elif action == "search_window":
Line 2225: identifier = target or text
Line 2226: if not identifier:
Line 2227: return {"success": False, "message": "search_window requires target"}
Line 2228: return _xdo_search_window(identifier)
Line 2229: elif action == "focus_click":
Line 2230: identifier = target or text
Line 2231: if not identifier:
Line 2232: return {"success": False, "message": "focus_click requires target (window name)"}
Line 2233: return _xdo_focus_click(identifier, x, y)
Line 2234: # ── Fill / Clear (desktop approximation via keyboard) ─────────
Line 2235: elif action == "fill":
Line 2236: # In desktop mode, fill = click + wait + clear stuck mods + select all + delete + type
Line 2237: if coords and len(coords) >= 2:
Line 2238: _xdo_click(coords[0], coords[1])
Line 2239: time.sleep(0.1)
Line 2240: try:
Line 2241: _xdo(["keyup", "shift"])
Line 2242: _xdo(["keyup", "ctrl"])
Line 2243: _xdo(["keyup", "alt"])
Line 2244: except Exception:
Line 2245: pass
Line 2246: time.sleep(0.05)
Line 2247: _xdo(["key", "--clearmodifiers", "ctrl+a"])
Line 2248: time.sleep(0.05)
Line 2249: _xdo(["key", "--clearmodifiers", "Delete"])
Line 2250: time.sleep(0.1)
Line 2251: value = text or ""
Line 2252: try:
Line 2253: _xdo(["type", "--clearmodifiers", "--delay", "25", "--", value])
Line 2254: except Exception:
Line 2255: logger.warning("Desktop fill type failed, using paste fallback")
Line 2256: return _xdo_paste(value)
Line 2257: return {"success": True, "message": f"Filled (desktop): {value[:50]}"}
Line 2258: elif action == "clear_input":
Line 2259: _xdo(["key", "--clearmodifiers", "ctrl+a"])
Line 2260: time.sleep(0.05)
Line 2261: _xdo(["key", "--clearmodifiers", "Delete"])
Line 2262: return {"success": True, "message": "Cleared input (desktop)"}
Line 2263: elif action == "select_option":
Line 2264: return {"success": False, "message": "select_option not supported in desktop mode — use click"}
Line 2265: # ── Browser-like navigation via keyboard shortcuts ─────────────
Line 2266: elif action == "reload":
Line 2267: _xdo(["key", "--clearmodifiers", "F5"])
Line 2268: return {"success": True, "message": "Reloaded (F5)"}
Line 2269: elif action == "go_back":
Line 2270: _xdo(["key", "--clearmodifiers", "alt+Left"])
Line 2271: return {"success": True, "message": "Navigated back (Alt+Left)"}
Line 2272: elif action == "go_forward":
Line 2273: _xdo(["key", "--clearmodifiers", "alt+Right"])
Line 2274: return {"success": True, "message": "Navigated forward (Alt+Right)"}
Line 2275: elif action == "new_tab":
Line 2276: _xdo(["key", "--clearmodifiers", "ctrl+t"])
Line 2277: time.sleep(0.5)
Line 2278: if text:
Line 2279: _xdo(["type", "--clearmodifiers", "--delay", "30", "--", text])
Line 2280: _xdo(["key", "--clearmodifiers", "Return"])
Line 2281: return {"success": True, "message": f"New tab (Ctrl+T){': ' + text[:50] if text else ''}"}
Line 2282: elif action == "close_tab":
Line 2283: _xdo(["key", "--clearmodifiers", "ctrl+w"])
Line 2284: return {"success": True, "message": "Closed tab (Ctrl+W)"}
Line 2285: elif action == "switch_tab":
Line 2286: identifier = target or text or ""
Line 2287: try:
Line 2288: idx = int(identifier)
Line 2289: # Ctrl+1..9 to switch by tab index
Line 2290: if 1 <= idx <= 9:
Line 2291: _xdo(["key", "--clearmodifiers", f"ctrl+{idx}"])
Line 2292: return {"success": True, "message": f"Switched to tab {idx}"}
Line 2293: except ValueError:
Line 2294: pass
Line 2295: _xdo(["key", "--clearmodifiers", "ctrl+Next"])
Line 2296: return {"success": True, "message": "Switched to next tab (Ctrl+PageDown)"}
Line 2297: # ── Scroll to (approximate via scroll) ────────────────────────
Line 2298: elif action == "scroll_to":
Line 2299: return {"success": False, "message": "scroll_to not supported in desktop mode — use scroll"}
Line 2300: # ── DOM / Semantic (not available in desktop mode) ─────────────
Line 2301: elif action == "get_text":
Line 2302: return {"success": False, "message": "get_text not supported in desktop mode"}
Line 2303: elif action == "find_element":
Line 2304: return {"success": False, "message": "find_element not supported in desktop mode"}
Line 2305: elif action == "evaluate_js":
Line 2306: return {"success": False, "message": "evaluate_js not supported in desktop mode"}
Line 2307: elif action == "wait_for":
Line 2308: return _do_wait(3.0)  # Approximate: just wait a few seconds
Line 2309: # ── Shell / Terminal ────────────────────────────────────────────
Line 2310: elif action == "run_command":
Line 2311: cmd = text or target
Line 2312: if not cmd:
Line 2313: return {"success": False, "message": "run_command requires text (shell command)"}
Line 2314: try:
Line 2315: args = shlex.split(cmd)
Line 2316: except ValueError as e:
Line 2317: return {"success": False, "message": f"Invalid command syntax: {e}"}
Line 2318: if not args:
Line 2319: return {"success": False, "message": "Empty command"}
Line 2320: if args[0] not in _ALLOWED_COMMANDS:
Line 2321: return {"success": False, "message": f"Command not allowed: {args[0]}. Permitted: {', '.join(sorted(_ALLOWED_COMMANDS))}"}
Line 2322: try:
Line 2323: result = subprocess.run(
Line 2324: args, shell=False, capture_output=True, text=True, timeout=30,
Line 2325: env={**os.environ, "DISPLAY": ":99"},
Line 2326: )
Line 2327: output = (result.stdout + result.stderr).strip()[:2000]
Line 2328: return {"success": result.returncode == 0, "message": output or f"Command exited with code {result.returncode}"}
Line 2329: except subprocess.TimeoutExpired:
Line 2330: return {"success": False, "message": "Command timed out after 30s"}
Line 2331: elif action == "open_terminal":
Line 2332: return _open_terminal()
Line 2333: # ── Vision ────────────────────────────────────────────────────
Line 2334: elif action in ("screenshot", "screenshot_full"):
Line 2335: b64 = _xdo_screenshot_full()
Line 2336: return {"success": True, "message": "Full screenshot captured", "screenshot": b64}
Line 2337: elif action == "screenshot_region":
Line 2338: if len(coords) >= 4:
Line 2339: b64 = _xdo_screenshot_region(coords[0], coords[1], coords[2], coords[3])
Line 2340: return {"success": True, "message": "Region screenshot captured", "screenshot": b64}
Line 2341: return {"success": False, "message": "screenshot_region needs 4 coords [x, y, width, height]"}
Line 2342: else:
Line 2343: return {"success": False, "message": f"Unsupported action '{action}' in desktop engine"}
Line 2346: def main():
Line 2347: """Start the HTTP agent service and initialise the browser."""
Line 2348: logger.info("Starting agent service on port %d (default_mode=%s)", SERVICE_PORT, DEFAULT_MODE)
Line 2350: # Initialize Playwright browser (available even in desktop mode for fallback)
Line 2351: try:
Line 2352: _init_browser()
Line 2353: except Exception as e:
Line 2354: logger.warning("Playwright init failed (desktop mode still works): %s", e)
Line 2356: server = HTTPServer(("0.0.0.0", SERVICE_PORT), AgentHandler)
Line 2357: logger.info("Agent service listening on 0.0.0.0:%d", SERVICE_PORT)
Line 2359: def _handle_signal(sig, frame):
Line 2360: """Gracefully shut down the server on SIGTERM/SIGINT."""
Line 2361: logger.info("Shutting down...")
Line 2362: _shutdown_browser()
Line 2363: server.shutdown()
Line 2364: sys.exit(0)
Line 2366: signal.signal(signal.SIGTERM, _handle_signal)
Line 2367: signal.signal(signal.SIGINT, _handle_signal)
Line 2369: try:
Line 2370: server.serve_forever()
Line 2371: except KeyboardInterrupt:
Line 2372: pass
Line 2373: finally:
Line 2374: _shutdown_browser()
Line 2375: server.server_close()
Line 2378: if __name__ == "__main__":
Line 2379: main()


=== FILE: docker/entrypoint.sh ===
--- SOURCE CODE ---
#!/bin/bash

set -e

export DISPLAY=:99
export SCREEN_WIDTH=${SCREEN_WIDTH:-1440}
export SCREEN_HEIGHT=${SCREEN_HEIGHT:-900}
export SCREEN_DEPTH=${SCREEN_DEPTH:-24}
export PATH="$PATH:/usr/bin:/usr/local/bin"
export PYTHONPATH=/app

echo "=== CUA Container Starting (XFCE4 Mode) ==="

# ─────────────────────────────────────────────
# 0. Ensure Chrome symlink for Playwright MCP
# ─────────────────────────────────────────────
if [ ! -x /opt/google/chrome/chrome ]; then
    echo "[Chrome] Symlink missing or broken — repairing..."
    mkdir -p /opt/google/chrome
    CHROMIUM=$(find /ms-playwright -name chrome -type f -executable 2>/dev/null | head -1)
    if [ -n "$CHROMIUM" ]; then
        ln -sf "$CHROMIUM" /opt/google/chrome/chrome
        echo "[Chrome] Linked /opt/google/chrome/chrome -> $CHROMIUM"
    else
        echo "[Chrome] WARNING: no Chromium binary found in /ms-playwright"
    fi
fi

# ─────────────────────────────────────────────
# 1. DBus (system + session)
# ─────────────────────────────────────────────
mkdir -p /var/run/dbus
dbus-daemon --system --fork 2>/dev/null || true
eval $(dbus-launch --sh-syntax)
export DBUS_SESSION_BUS_ADDRESS
echo "[DBus] Session bus: $DBUS_SESSION_BUS_ADDRESS"

# ─────────────────────────────────────────────
# 2. Xvfb (virtual framebuffer)
# ─────────────────────────────────────────────
rm -f /tmp/.X99-lock /tmp/.X11-unix/X99
Xvfb :99 -screen 0 ${SCREEN_WIDTH}x${SCREEN_HEIGHT}x${SCREEN_DEPTH} \
    -ac +extension GLX +render -noreset &
XVFB_PID=$!

# Wait for Xvfb to be ready (poll for DISPLAY)
for i in $(seq 1 20); do
    if xdpyinfo -display :99 >/dev/null 2>&1; then
        echo "[Xvfb] Display :99 ready"
        break
    fi
    sleep 0.25
done

if ! kill -0 $XVFB_PID 2>/dev/null; then
    echo "ERROR: Xvfb failed to start"
    exit 1
fi

# Verify X server is reachable (critical for xdotool)
if ! xdpyinfo -display :99 >/dev/null 2>&1; then
    echo "ERROR: X server on :99 not reachable"
    exit 1
fi

# ─────────────────────────────────────────────────
# 3a. AT-SPI accessibility bridge (BEFORE desktop — apps must see these vars)
# ─────────────────────────────────────────────────
export NO_AT_BRIDGE=0
export GTK_MODULES=gail:atk-bridge
export QT_ACCESSIBILITY=1
export ACCESSIBILITY_ENABLED=1

# Enable toolkit accessibility (gsettings needs dbus, already running)
gsettings set org.gnome.desktop.interface toolkit-accessibility true 2>/dev/null || true

# Start AT-SPI registry daemon (required for accessibility tree queries)
/usr/libexec/at-spi2-registryd --dbus-name=org.a11y.atspi.Registry &
ATSPI_PID=$!
sleep 0.5

# Quick import check (non-fatal)
if python3 -c "import gi; gi.require_version('Atspi', '2.0'); from gi.repository import Atspi; Atspi.init(); print('AT-SPI OK')" 2>/dev/null; then
    echo "[A11y] AT-SPI registry daemon running and bindings verified"
else
    echo "[A11y] WARNING: AT-SPI init check failed — accessibility engine may not work"
    echo "[A11y] Attempting start via dbus activation..."
    dbus-send --session --dest=org.a11y.Bus --print-reply /org/a11y/bus org.a11y.Bus.GetAddress 2>/dev/null || true
fi

# ─────────────────────────────────────────────
# 3b. XFCE4 Desktop + Window Manager
# ─────────────────────────────────────────────
echo "[Desktop] Starting XFCE4..."
startxfce4 &

# Wait for the window manager to be fully operational
for i in $(seq 1 30); do
    if xdotool getactivewindow >/dev/null 2>&1; then
        echo "[Desktop] Window manager ready"
        break
    fi
    sleep 0.5
done

# Give AT-SPI time to register desktop apps (XFCE panels, file manager, etc.)
sleep 1

# Verify AT-SPI sees desktop applications
A11Y_APPS=$(python3 -c "
import gi; gi.require_version('Atspi', '2.0')
from gi.repository import Atspi; Atspi.init()
d=Atspi.get_desktop(0); print(d.get_child_count())
" 2>/dev/null || echo "0")
echo "[A11y] AT-SPI registered applications: ${A11Y_APPS}"
if [ "$A11Y_APPS" = "0" ]; then
    echo "[A11y] WARNING: No applications registered with AT-SPI after desktop start"
fi

# ─────────────────────────────────────────────
# 4. x11vnc
# ─────────────────────────────────────────────
echo "[VNC] Starting x11vnc..."
if [ -n "$VNC_PASSWORD" ]; then
    mkdir -p /root/.vnc
    x11vnc -storepasswd "$VNC_PASSWORD" /root/.vnc/passwd
    x11vnc -display :99 -forever -rfbauth /root/.vnc/passwd -shared -rfbport 5900 -bg -o /var/log/x11vnc.log
else
    echo "[VNC] WARNING: No VNC_PASSWORD set — VNC access is unauthenticated"
    x11vnc -display :99 -forever -nopw -shared -rfbport 5900 -bg -o /var/log/x11vnc.log
fi

# ─────────────────────────────────────────────
# 5. noVNC (Web access)
# ─────────────────────────────────────────────
echo "[noVNC] Starting websockify..."
websockify --web=/usr/share/novnc/ 6080 localhost:5900 &

# ─────────────────────────────────────────────
# 5b. Browser bootstrap — default browser + pre-warm Chrome profile
# ─────────────────────────────────────────────
echo "[Browser] Configuring default browser..."
# Set Google Chrome as the default web browser for xdg-open
if command -v google-chrome >/dev/null 2>&1; then
    xdg-settings set default-web-browser google-chrome.desktop 2>/dev/null || true
    # Ensure Chrome profile directory exists (seeded at build time)
    mkdir -p /tmp/chrome-profile/Default
    echo "[Browser] ✓ Chrome set as default browser (profile seeded at build time)"
elif command -v firefox >/dev/null 2>&1; then
    xdg-settings set default-web-browser firefox.desktop 2>/dev/null || true
    echo "[Browser] ✓ Firefox set as default browser"
else
    echo "[Browser] WARNING: No browser found for xdg-settings"
fi

# ─────────────────────────────────────────────
# 6. Playwright MCP server (a11y-tree browser control)
# ─────────────────────────────────────────────
# All MCP config is driven by native env vars set in docker-compose.yml:
#   PLAYWRIGHT_MCP_PORT=8931          — activates HTTP/SSE transport
#   PLAYWRIGHT_MCP_HOST=0.0.0.0      — listen on all interfaces
#   PLAYWRIGHT_MCP_ALLOWED_HOSTS=*   — disable Host-header check (Docker NAT fix)
#   PLAYWRIGHT_MCP_BROWSER=chromium  — use Playwright-bundled Chromium
#   PLAYWRIGHT_MCP_NO_SANDBOX=true   — required when running as root in Docker
#   PLAYWRIGHT_MCP_HEADLESS=true     — avoid competing for X11 with agent_service
MCP_PORT=${PLAYWRIGHT_MCP_PORT:-8931}
MCP_LOG="/var/log/playwright-mcp.log"
echo "[MCP] Starting Playwright MCP server on port ${MCP_PORT} (HTTP transport, env-var config)..."

if command -v playwright-mcp >/dev/null 2>&1; then
    playwright-mcp 2>"${MCP_LOG}" &
    MCP_PID=$!
else
    # Fallback to npx with explicit package (-y to never prompt)
    npx -y @playwright/mcp@latest 2>"${MCP_LOG}" &
    MCP_PID=$!
fi

# Wait for MCP server — robust JSON-RPC POST health probe (~20s budget)
# NOTE: plain GET or "curl -I" returns 403 — that is expected because the
# MCP server only accepts POST with a JSON-RPC body.
MCP_READY=false
for i in $(seq 1 40); do
    # Primary probe: POST a JSON-RPC initialize request
    HTTP_CODE=$(curl -sf -o /dev/null -w '%{http_code}' \
        -X POST http://localhost:${MCP_PORT}/mcp \
        -H "Content-Type: application/json" \
        -H "Accept: application/json, text/event-stream" \
        -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"probe","version":"1.0"}}}' \
        2>/dev/null) || true
    if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "201" ]; then
        MCP_READY=true
        echo "[MCP] ✓ Playwright MCP server ready on http://0.0.0.0:${MCP_PORT}/mcp (JSON-RPC OK)"
        break
    fi
    sleep 0.5
done

if [ "$MCP_READY" = "false" ]; then
    echo "[MCP] WARNING: MCP server not responding after 20s (tried POST JSON-RPC to http://localhost:${MCP_PORT}/mcp)"
    echo "[MCP] NOTE: 'curl -I' returns 403 — that is expected. Only POST with JSON-RPC body works."
    if [ -f "${MCP_LOG}" ]; then
        echo "[MCP] Server stderr:"
        tail -20 "${MCP_LOG}"
    fi
    # Check for port-bind failure in logs
    if grep -qi 'EADDRINUSE\|address already in use\|bind.*failed' "${MCP_LOG}" 2>/dev/null; then
        echo "[MCP] ERROR: Port ${MCP_PORT} already in use — cannot bind MCP server"
    fi
    # Check if process is still alive
    if ! kill -0 $MCP_PID 2>/dev/null; then
        echo "[MCP] ERROR: MCP server process (PID $MCP_PID) died"
        echo "[MCP] Attempting restart..."
        npx @playwright/mcp@latest 2>"${MCP_LOG}" &
        MCP_PID=$!
        sleep 3
        if kill -0 $MCP_PID 2>/dev/null; then
            echo "[MCP] Restarted MCP server (PID $MCP_PID)"
        else
            echo "[MCP] ERROR: MCP server restart also failed"
            tail -20 "${MCP_LOG}" 2>/dev/null
        fi
    fi
fi

# ─────────────────────────────────────────────
# 8. Pre-flight verification
# ─────────────────────────────────────────────
echo "[Verify] Running pre-flight checks..."

# X server check
if xdotool getmouselocation >/dev/null 2>&1; then
    echo "[Verify] ✓ X server + xdotool operational"
else
    echo "[Verify] ✗ xdotool cannot reach X server on DISPLAY=$DISPLAY"
fi

# ─────────────────────────────────────────────
# 9. Agent service
# ─────────────────────────────────────────────
# ── Hard-check desktop tool binaries ─────────────────────────────────
command -v xdotool  || echo "ERROR: xdotool missing from PATH"
command -v wmctrl   || echo "ERROR: wmctrl missing from PATH"
command -v xclip    || echo "ERROR: xclip missing from PATH"

echo "[Agent] Starting internal agent service..."
PYTHONPATH=/app /opt/venv/bin/python /app/docker/agent_service.py &
AGENT_PID=$!

echo "=== XFCE4 Desktop Ready ==="
echo "Access via: http://localhost:6080"
echo "MCP server: http://localhost:${MCP_PORT}"

trap "kill $XVFB_PID $AGENT_PID $MCP_PID $ATSPI_PID 2>/dev/null; exit 0" SIGTERM SIGINT

wait $AGENT_PID
--- EXPLANATION ---
This is the source code for docker/entrypoint.sh.
Line 1: #!/bin/bash
Line 3: set -e
Line 5: export DISPLAY=:99
Line 6: export SCREEN_WIDTH=${SCREEN_WIDTH:-1440}
Line 7: export SCREEN_HEIGHT=${SCREEN_HEIGHT:-900}
Line 8: export SCREEN_DEPTH=${SCREEN_DEPTH:-24}
Line 9: export PATH="$PATH:/usr/bin:/usr/local/bin"
Line 10: export PYTHONPATH=/app
Line 12: echo "=== CUA Container Starting (XFCE4 Mode) ==="
Line 14: # ─────────────────────────────────────────────
Line 15: # 0. Ensure Chrome symlink for Playwright MCP
Line 16: # ─────────────────────────────────────────────
Line 17: if [ ! -x /opt/google/chrome/chrome ]; then
Line 18: echo "[Chrome] Symlink missing or broken — repairing..."
Line 19: mkdir -p /opt/google/chrome
Line 20: CHROMIUM=$(find /ms-playwright -name chrome -type f -executable 2>/dev/null | head -1)
Line 21: if [ -n "$CHROMIUM" ]; then
Line 22: ln -sf "$CHROMIUM" /opt/google/chrome/chrome
Line 23: echo "[Chrome] Linked /opt/google/chrome/chrome -> $CHROMIUM"
Line 24: else
Line 25: echo "[Chrome] WARNING: no Chromium binary found in /ms-playwright"
Line 26: fi
Line 27: fi
Line 29: # ─────────────────────────────────────────────
Line 30: # 1. DBus (system + session)
Line 31: # ─────────────────────────────────────────────
Line 32: mkdir -p /var/run/dbus
Line 33: dbus-daemon --system --fork 2>/dev/null || true
Line 34: eval $(dbus-launch --sh-syntax)
Line 35: export DBUS_SESSION_BUS_ADDRESS
Line 36: echo "[DBus] Session bus: $DBUS_SESSION_BUS_ADDRESS"
Line 38: # ─────────────────────────────────────────────
Line 39: # 2. Xvfb (virtual framebuffer)
Line 40: # ─────────────────────────────────────────────
Line 41: rm -f /tmp/.X99-lock /tmp/.X11-unix/X99
Line 42: Xvfb :99 -screen 0 ${SCREEN_WIDTH}x${SCREEN_HEIGHT}x${SCREEN_DEPTH} \
Line 43: -ac +extension GLX +render -noreset &
Line 44: XVFB_PID=$!
Line 46: # Wait for Xvfb to be ready (poll for DISPLAY)
Line 47: for i in $(seq 1 20); do
Line 48: if xdpyinfo -display :99 >/dev/null 2>&1; then
Line 49: echo "[Xvfb] Display :99 ready"
Line 50: break
Line 51: fi
Line 52: sleep 0.25
Line 53: done
Line 55: if ! kill -0 $XVFB_PID 2>/dev/null; then
Line 56: echo "ERROR: Xvfb failed to start"
Line 57: exit 1
Line 58: fi
Line 60: # Verify X server is reachable (critical for xdotool)
Line 61: if ! xdpyinfo -display :99 >/dev/null 2>&1; then
Line 62: echo "ERROR: X server on :99 not reachable"
Line 63: exit 1
Line 64: fi
Line 66: # ─────────────────────────────────────────────────
Line 67: # 3a. AT-SPI accessibility bridge (BEFORE desktop — apps must see these vars)
Line 68: # ─────────────────────────────────────────────────
Line 69: export NO_AT_BRIDGE=0
Line 70: export GTK_MODULES=gail:atk-bridge
Line 71: export QT_ACCESSIBILITY=1
Line 72: export ACCESSIBILITY_ENABLED=1
Line 74: # Enable toolkit accessibility (gsettings needs dbus, already running)
Line 75: gsettings set org.gnome.desktop.interface toolkit-accessibility true 2>/dev/null || true
Line 77: # Start AT-SPI registry daemon (required for accessibility tree queries)
Line 78: /usr/libexec/at-spi2-registryd --dbus-name=org.a11y.atspi.Registry &
Line 79: ATSPI_PID=$!
Line 80: sleep 0.5
Line 82: # Quick import check (non-fatal)
Line 83: if python3 -c "import gi; gi.require_version('Atspi', '2.0'); from gi.repository import Atspi; Atspi.init(); print('AT-SPI OK')" 2>/dev/null; then
Line 84: echo "[A11y] AT-SPI registry daemon running and bindings verified"
Line 85: else
Line 86: echo "[A11y] WARNING: AT-SPI init check failed — accessibility engine may not work"
Line 87: echo "[A11y] Attempting start via dbus activation..."
Line 88: dbus-send --session --dest=org.a11y.Bus --print-reply /org/a11y/bus org.a11y.Bus.GetAddress 2>/dev/null || true
Line 89: fi
Line 91: # ─────────────────────────────────────────────
Line 92: # 3b. XFCE4 Desktop + Window Manager
Line 93: # ─────────────────────────────────────────────
Line 94: echo "[Desktop] Starting XFCE4..."
Line 95: startxfce4 &
Line 97: # Wait for the window manager to be fully operational
Line 98: for i in $(seq 1 30); do
Line 99: if xdotool getactivewindow >/dev/null 2>&1; then
Line 100: echo "[Desktop] Window manager ready"
Line 101: break
Line 102: fi
Line 103: sleep 0.5
Line 104: done
Line 106: # Give AT-SPI time to register desktop apps (XFCE panels, file manager, etc.)
Line 107: sleep 1
Line 109: # Verify AT-SPI sees desktop applications
Line 110: A11Y_APPS=$(python3 -c "
Line 111: import gi; gi.require_version('Atspi', '2.0')
Line 112: from gi.repository import Atspi; Atspi.init()
Line 113: d=Atspi.get_desktop(0); print(d.get_child_count())
Line 114: " 2>/dev/null || echo "0")
Line 115: echo "[A11y] AT-SPI registered applications: ${A11Y_APPS}"
Line 116: if [ "$A11Y_APPS" = "0" ]; then
Line 117: echo "[A11y] WARNING: No applications registered with AT-SPI after desktop start"
Line 118: fi
Line 120: # ─────────────────────────────────────────────
Line 121: # 4. x11vnc
Line 122: # ─────────────────────────────────────────────
Line 123: echo "[VNC] Starting x11vnc..."
Line 124: if [ -n "$VNC_PASSWORD" ]; then
Line 125: mkdir -p /root/.vnc
Line 126: x11vnc -storepasswd "$VNC_PASSWORD" /root/.vnc/passwd
Line 127: x11vnc -display :99 -forever -rfbauth /root/.vnc/passwd -shared -rfbport 5900 -bg -o /var/log/x11vnc.log
Line 128: else
Line 129: echo "[VNC] WARNING: No VNC_PASSWORD set — VNC access is unauthenticated"
Line 130: x11vnc -display :99 -forever -nopw -shared -rfbport 5900 -bg -o /var/log/x11vnc.log
Line 131: fi
Line 133: # ─────────────────────────────────────────────
Line 134: # 5. noVNC (Web access)
Line 135: # ─────────────────────────────────────────────
Line 136: echo "[noVNC] Starting websockify..."
Line 137: websockify --web=/usr/share/novnc/ 6080 localhost:5900 &
Line 139: # ─────────────────────────────────────────────
Line 140: # 5b. Browser bootstrap — default browser + pre-warm Chrome profile
Line 141: # ─────────────────────────────────────────────
Line 142: echo "[Browser] Configuring default browser..."
Line 143: # Set Google Chrome as the default web browser for xdg-open
Line 144: if command -v google-chrome >/dev/null 2>&1; then
Line 145: xdg-settings set default-web-browser google-chrome.desktop 2>/dev/null || true
Line 146: # Ensure Chrome profile directory exists (seeded at build time)
Line 147: mkdir -p /tmp/chrome-profile/Default
Line 148: echo "[Browser] ✓ Chrome set as default browser (profile seeded at build time)"
Line 149: elif command -v firefox >/dev/null 2>&1; then
Line 150: xdg-settings set default-web-browser firefox.desktop 2>/dev/null || true
Line 151: echo "[Browser] ✓ Firefox set as default browser"
Line 152: else
Line 153: echo "[Browser] WARNING: No browser found for xdg-settings"
Line 154: fi
Line 156: # ─────────────────────────────────────────────
Line 157: # 6. Playwright MCP server (a11y-tree browser control)
Line 158: # ─────────────────────────────────────────────
Line 159: # All MCP config is driven by native env vars set in docker-compose.yml:
Line 160: #   PLAYWRIGHT_MCP_PORT=8931          — activates HTTP/SSE transport
Line 161: #   PLAYWRIGHT_MCP_HOST=0.0.0.0      — listen on all interfaces
Line 162: #   PLAYWRIGHT_MCP_ALLOWED_HOSTS=*   — disable Host-header check (Docker NAT fix)
Line 163: #   PLAYWRIGHT_MCP_BROWSER=chromium  — use Playwright-bundled Chromium
Line 164: #   PLAYWRIGHT_MCP_NO_SANDBOX=true   — required when running as root in Docker
Line 165: #   PLAYWRIGHT_MCP_HEADLESS=true     — avoid competing for X11 with agent_service
Line 166: MCP_PORT=${PLAYWRIGHT_MCP_PORT:-8931}
Line 167: MCP_LOG="/var/log/playwright-mcp.log"
Line 168: echo "[MCP] Starting Playwright MCP server on port ${MCP_PORT} (HTTP transport, env-var config)..."
Line 170: if command -v playwright-mcp >/dev/null 2>&1; then
Line 171: playwright-mcp 2>"${MCP_LOG}" &
Line 172: MCP_PID=$!
Line 173: else
Line 174: # Fallback to npx with explicit package (-y to never prompt)
Line 175: npx -y @playwright/mcp@latest 2>"${MCP_LOG}" &
Line 176: MCP_PID=$!
Line 177: fi
Line 179: # Wait for MCP server — robust JSON-RPC POST health probe (~20s budget)
Line 180: # NOTE: plain GET or "curl -I" returns 403 — that is expected because the
Line 181: # MCP server only accepts POST with a JSON-RPC body.
Line 182: MCP_READY=false
Line 183: for i in $(seq 1 40); do
Line 184: # Primary probe: POST a JSON-RPC initialize request
Line 185: HTTP_CODE=$(curl -sf -o /dev/null -w '%{http_code}' \
Line 186: -X POST http://localhost:${MCP_PORT}/mcp \
Line 187: -H "Content-Type: application/json" \
Line 188: -H "Accept: application/json, text/event-stream" \
Line 189: -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"probe","version":"1.0"}}}' \
Line 190: 2>/dev/null) || true
Line 191: if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "201" ]; then
Line 192: MCP_READY=true
Line 193: echo "[MCP] ✓ Playwright MCP server ready on http://0.0.0.0:${MCP_PORT}/mcp (JSON-RPC OK)"
Line 194: break
Line 195: fi
Line 196: sleep 0.5
Line 197: done
Line 199: if [ "$MCP_READY" = "false" ]; then
Line 200: echo "[MCP] WARNING: MCP server not responding after 20s (tried POST JSON-RPC to http://localhost:${MCP_PORT}/mcp)"
Line 201: echo "[MCP] NOTE: 'curl -I' returns 403 — that is expected. Only POST with JSON-RPC body works."
Line 202: if [ -f "${MCP_LOG}" ]; then
Line 203: echo "[MCP] Server stderr:"
Line 204: tail -20 "${MCP_LOG}"
Line 205: fi
Line 206: # Check for port-bind failure in logs
Line 207: if grep -qi 'EADDRINUSE\|address already in use\|bind.*failed' "${MCP_LOG}" 2>/dev/null; then
Line 208: echo "[MCP] ERROR: Port ${MCP_PORT} already in use — cannot bind MCP server"
Line 209: fi
Line 210: # Check if process is still alive
Line 211: if ! kill -0 $MCP_PID 2>/dev/null; then
Line 212: echo "[MCP] ERROR: MCP server process (PID $MCP_PID) died"
Line 213: echo "[MCP] Attempting restart..."
Line 214: npx @playwright/mcp@latest 2>"${MCP_LOG}" &
Line 215: MCP_PID=$!
Line 216: sleep 3
Line 217: if kill -0 $MCP_PID 2>/dev/null; then
Line 218: echo "[MCP] Restarted MCP server (PID $MCP_PID)"
Line 219: else
Line 220: echo "[MCP] ERROR: MCP server restart also failed"
Line 221: tail -20 "${MCP_LOG}" 2>/dev/null
Line 222: fi
Line 223: fi
Line 224: fi
Line 226: # ─────────────────────────────────────────────
Line 227: # 8. Pre-flight verification
Line 228: # ─────────────────────────────────────────────
Line 229: echo "[Verify] Running pre-flight checks..."
Line 231: # X server check
Line 232: if xdotool getmouselocation >/dev/null 2>&1; then
Line 233: echo "[Verify] ✓ X server + xdotool operational"
Line 234: else
Line 235: echo "[Verify] ✗ xdotool cannot reach X server on DISPLAY=$DISPLAY"
Line 236: fi
Line 238: # ─────────────────────────────────────────────
Line 239: # 9. Agent service
Line 240: # ─────────────────────────────────────────────
Line 241: # ── Hard-check desktop tool binaries ─────────────────────────────────
Line 242: command -v xdotool  || echo "ERROR: xdotool missing from PATH"
Line 243: command -v wmctrl   || echo "ERROR: wmctrl missing from PATH"
Line 244: command -v xclip    || echo "ERROR: xclip missing from PATH"
Line 246: echo "[Agent] Starting internal agent service..."
Line 247: PYTHONPATH=/app /opt/venv/bin/python /app/docker/agent_service.py &
Line 248: AGENT_PID=$!
Line 250: echo "=== XFCE4 Desktop Ready ==="
Line 251: echo "Access via: http://localhost:6080"
Line 252: echo "MCP server: http://localhost:${MCP_PORT}"
Line 254: trap "kill $XVFB_PID $AGENT_PID $MCP_PID $ATSPI_PID 2>/dev/null; exit 0" SIGTERM SIGINT
Line 256: wait $AGENT_PID


